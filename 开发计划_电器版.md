# 开发计划 · 家电批发进销存（电器版）

- 文档版本：**V3.76**（统一版本号：自 V3.1 起开发计划与 PRD 文档版本号统一，与软件「我的 → 关于」显示的版本号一致；本版升至 V3.76）
- 日期：2026-09-17
- 状态：**已确认开发中**（阶段 0–5 及问题 1–4、账户管理、阶段 6 管理员权限、阶段 7 账户收归、阶段 8 体验优化与云同步增强、阶段 9 客户管理页面、阶段 10 销售开单体验与单据打印、阶段 11 界面优化与适配、阶段 12 手机库存卡再缩 + 打印增强 + 蓝色配色 + 跨端库存合并、阶段 13 库存卡高度贴合 + 云同步账户/系统参数 + 价格体系 + 搜索下拉宽度、阶段 14 价格体系移入设置 + 只填成本自动定价 + 打印按钮放大、阶段 15 管理总控账户统一 + 登录界面改造 + 库存回滚修复、阶段 16 登录页双头像修复 + 密码框输入修复 + 账户名修正为 hawsystem + 侧边栏折叠按钮长条样式、阶段 17 记一笔模块上移 + 全列表斑马纹 + 侧边栏按钮基础样式 + 打印布局优化 + 清空数据bug修复、阶段 18 侧边栏宽度样式移到base.css + 表格行内输入框焦点修复、阶段 19 变动明细上移 + 商品条数一致 + 扫码按钮修复 + 蓝牙标签打印、阶段 20 侧边栏折叠按钮深度修复（断点600px+按钮撑满宽度）、阶段 21 蓝牙打印兼容性引导（平台检测+浏览器建议+复制链接）、阶段 22 取消蓝牙打印功能+登录头像位置调整+分页跳转输入框、阶段 23 库存管理标签间距+打印模板列宽调整与类型列、阶段 24 打印模板类型列空白修复+列宽二轮调整+进货单补付款分期结清、阶段 25 手机端搜索模块同行+打印列宽按汉字重排+进货单品字布局+销售单补回款、阶段 26 分页200条+打印列宽三轮调整与表头样式优化+打印内容居中与型号列折行、阶段 27 打印按钮颜色区分+所有取消按钮统一红色、阶段 28 所有关闭/返回按钮红色+商品档案搜索模块重排已开发完成并部署上线（V3.20），阶段 29 批量导入按型号比对更新已开发完成（V3.21 待部署）、阶段 30 批量导入多工作表支持与行数去向统计已开发完成（V3.22 待部署）、阶段 31 批量导入同型号多品牌匹配优先级修正已开发完成（V3.23 待部署）、阶段 32 批量导入规则重构（纯型号匹配/文件去重/合并保留）已开发完成（V3.24 待部署）、阶段 33 批量导入预演体检与纠错覆盖（未覆盖清单一键停售、合并库存转入）已开发完成（V3.25 待部署）、阶段 34 单据明细模块移到列表上方已开发完成并部署上线（V3.26）、阶段 35 批量导入操作按钮同排靠右已开发完成（V3.27 待部署）、阶段 36 扫码识别深度修复已开发完成并部署上线（V3.28）、阶段 37 扫码黑屏回归修复已开发完成并部署上线（V3.29）、阶段 38 扫码多通道解码重构已开发完成并部署上线（V3.30）、阶段 39 扫码解码链路加固已开发完成（V3.31 待部署）、阶段 40 扫码识别套件自研化已开发完成（V3.32 待部署）、阶段 41 扫码失效根治（SW 缓存 network-first + SHELL 补 ean13.js + 官方 ZXing ESM 重打包 + 自研旋转/光照鲁棒性）已开发完成（V3.33 待部署）、阶段 42 扫码场景打通（销售开单去开单修复 + 新建进货单选货扫码 + 新建商品条码扫码填写）已开发完成（V3.34 待部署）、阶段 43 扫码功能增强（UPC-A 12 位补 0 规范化 + 解码出口统一规范化 + resolve 双向变体匹配 + 竖排 90°/270° 转置重试，参考鞋服 V1.3-10 思路独立实现）已开发完成（V3.35 待部署）、阶段 44 公司内部自定义条码扫码识别（自研 Code39 解码通道：44 符号码表/宽窄比自适应/无校验容错/多行投票，解码顺序 native→ean13→code39→zxing）已开发完成（V3.36 待部署）、阶段 45 商品档案多选删除未使用档案（表头全选+行勾选+删除选中按钮，product.removeUnused 引用保护，flush __deleted 物理删除）已开发完成（V3.37 待部署）、阶段 46 手机版取消删除商品档案入口（desktop-only 标记 + ≤599px 隐藏，仅网页版保留）已开发完成（V3.38 待部署）、阶段 47 「加入」「选为原单」按钮统一橘色配色（4 处 btn-orange + .act 链接样式不覆盖 .btn 修复）已开发完成（V3.39 待部署）、阶段 48 商品多选删除勾选同步修复（搜索/筛选/翻页/扫码清空勾选，删除计数实时一致）已开发完成（V3.40 待部署）、阶段 49 全选与列表渲染统一分页管道（computePage 过滤→排序→分页共用，修复大库存全选勾错集合）已开发完成（V3.41 待部署）、阶段 50 商品档案同型号信息合并（product.mergeByModel：仅型号相同可合并/主档库存最大/库存相加/备注拼接/条码合并/保留主档成本品牌价格状态/副档 __deleted 物理删除/停售可合并；UI 合并按钮 desktop-only 手机版隐藏 + merge-selected 确认流程；测试 15 项 + 超期测试日期漂移修复）已开发完成（V3.42 待部署）、阶段 51 多选勾选局部刷新不再整页重渲染（refreshSelUI 原地更新按钮计数/禁用/表头全选/当前页行勾选，长列表多选滚动位置不跳顶；测试 3 项）已开发完成（V3.43 待部署）、阶段 52 修复 V3.43 勾选失效回归（根因：checkbox change 事件被第三方框架拦截到不了 document change 委托；修复：行勾选/表头全选改用 click 委托 data-act，去掉 data-change；测试 3 项）已开发完成（V3.44 待部署）、阶段 53 商品档案「导出全部」（批量导入前新增导出全部 CSV：品牌/型号/类型/单位/成本/批发/零售/库存/备注/条码/状态，金额按元导出、条码分号连接；测试 5 项）已开发完成（V3.45 待部署）、阶段 54 清空库存与档案（保留单据）（设置页数据管理新增按钮：仅清 products/stockLogs/stocktakes 三表，保留全部单据/记账/客户/日志/设置，二次确认+操作日志；测试 4 项）已开发完成（V3.46 待部署）、阶段 55 商品档案复选框勾选不再跳回顶部（框架 afterAction 整页重渲染+滚动根治：row-check/toggle-all-check 显式 return false 跳过框架重渲染，长列表勾选滚动位置保持；测试 2 项）已开发完成（V3.47 待部署）、阶段 56 根治全页面内操作跳顶并统一搜索框一键清除（app.js render 同路由保持滚动位置；全系统搜索框 type="search" 启用原生清除按钮，盘点/进货选货等补齐 data-live/debounce；测试 8 项）已开发完成（V3.48 已部署上线）；阶段 57 横向滚动容器（.table-wrap）重渲染保持 scrollLeft（手机端选货区左右滑动点加入不再跳回最左，render 同路由记录/恢复 scrollLeft，仅同路由恢复）已开发完成并部署上线（V3.49）；阶段 58 选货区两行卡片式排版（销售开单/新建进货单按商品加行/退换货换货三处选货区统一改为两行卡片式：pick-list/pick-item/pick-name/pick-sub/pick-price 容器，第一行品牌+型号，第二行类型/单位+批:¥xx/零:¥xx 或成本:¥xx，右侧保留库存与加入按钮大小位置不变；手机普通宽度即可容纳，无需横向滚动；base.css 新增 .pick-list/.pick-item/.pick-sub/.pick-stock（含 .pick-stock.low 红色）；测试 4 项）已开发完成并部署上线（V3.50，SW v79）；全量测试 **687/687** 通过；回滚基线 tag：**v3.49-baseline**；阶段 59 手机端选货区右侧排版优化（mobile.css @media max-width:599px 内 .pick-side 改 flex-direction:row 让库存数字前置到按钮左侧；.pick-side .btn 改 writing-mode:vertical-rl + text-orientation:upright 让「加入」两字从横排改为竖排；按钮 width:30px 收缩30%、height:45px 增大50%、padding:0、line-height:1，按钮排布位置不变；电脑端 ≥600px 不受影响沿用 V3.50 列排版；测试 1 项）已开发完成并部署上线（V3.51，SW v80）；全量测试 **688/688** 通过；阶段 60 选货区双布局切换（澄清需求：V3.50+V3.51 卡片式只应用于手机端，电脑端恢复 V3.49 横向表格；JS 同时渲染 pick-desktop 包装 V3.49 tbl-striped 5列表格 + pick-mobile 包装 V3.51 pick-list 两行卡片；base.css 默认 .pick-desktop 显示 .pick-mobile 隐藏；mobile.css ≤599px 反转；CSS-only 响应式切换无需 JS 重渲染；测试新增 2 项 + 重写 3 项 V3.50 选货区断言为双布局断言，ui-sale/ui-purchase 行数 15→30）已开发完成并部署上线（V3.52，SW v81）；全量测试 **690/690** 通过；阶段 61 取消「加入」按钮变换为「＋再加」的样式（新建进货单按商品加行选货区原会在商品已存在于进货明细时把按钮文案从「加入」换成「＋ 再加」；用户反馈该变换毫无意义——数量应在「进货明细列表/当前订单列表」中修改——且更长文案会撑宽按钮破坏页面宽度；本版删除该状态判断，桌面端 pick-desktop 表格与手机端 pick-mobile 卡片的选货按钮一律固定显示「加入」，宽度稳定不再抖动；仅改 js/ui/page-purchase.js，移除 hasD / has 两个 state.form.items.some(...) 判断分支；测试新增 tests/pick-list.test.js 1 项：先把商品加入 state.form.items 模拟「已加入」状态，断言渲染结果不含「＋再加」/「+再加」且按钮数量保持 4=2款商品×双布局）已开发完成（V3.53，SW v82）；全量测试 **691/691** 通过；阶段 62 修复「进货明细」宽表格撑破整页 + 手机端选货区残留「＋再加」的缓存问题（① 经典 CSS Grid 溢出：.purchase-form-grid 子项默认 min-width:auto 被内部 table.tbl td white-space:nowrap 的进货明细 5 列表撑到 min-content 宽度、把整条 grid 轨道与整页撑宽；修复 grid-template-columns 改 minmax(0,1fr)×2 + 子卡片 min-width:0，手机端单列同步 minmax(0,1fr)，宽表格只在 .table-wrap 内横向滚动；另 .card min-width:0 防御、.pick-name overflow-wrap:anywhere、手机端 .app-main overflow-x:clip 兜底；② GitHub Pages 静态资源 Cache-Control:max-age=600 使 network-first 的 fetch(req) 10 分钟内命中 HTTP 缓存返回旧 JS/CSS，导致手机端仍显示已删除的「＋再加」；修复 fetch(req,{cache:'no-cache'}) 强制重新校验；③ 「我的 → 关于」新增「🔄 检查更新」按钮=清空 SW 缓存 + registration.update() + 重新加载，作为旧缓存兜底自救入口；测试新增 3 项 + 更新 3 项）已开发完成并部署上线（V3.54，SW v83）；全量测试 **694/694** 通过；阶段 63 全系统商品列表斑马纹统一 + 「已加入」行高亮（需求①：排查全部商品列表在电脑/手机端的斑马纹覆盖——table.tbl 表格侧已由全局规则 table.tbl tbody tr:nth-child(even) 统一覆盖（不依赖 .tbl-striped），实测商品档案/库存管理/销售单列表/进货单列表/记账流水 6 页在 1200px 与 390px 均逐行交替正常，缺口只在手机端 .pick-item 选货卡片（此前完全无斑马纹）；修复：抽取 --c-stripe:#f6f7f9 变量供表格与卡片共用 + 新增 .pick-item:nth-child(even) 交替底色 + .pick-item padding 改 10px 8px 让色带铺满；需求②：凡带「加入」按钮的选货区商品列表（销售开单 pick-product / 新建进货单 add-item / 退换货 repl-add，3 处 ×桌面+手机 2 布局 = 6 个列表），商品已在目标明细时整行灰底 --c-picked:#e2e5ea（明显深于斑马纹 #f6f7f9）+ 「加入」按钮由橙转蓝新增 .btn-added（var(--c-primary) #2563eb 白字 + hover/active 三态），判定为状态派生（依据 state.form.items / state.replItems）故删除明细后高亮自动消失，按钮文案仍恒为「加入」延续 V3.53；table.tbl tbody tr.picked（含 td）用 !important 稳定压过同特异性的 nth-child(even)；进货页新增 formHas 助手，销售/退换货复用 findItem/findRepl；测试新增 6 项（含用相对明度公式断言 --c-picked 必须比 --c-stripe 更深）+ 更新 4 项（ui-theme 改断言变量、ui-button-style 三条改为「未加入 btn-orange / 已加入 btn-added，仍不用 btn-primary」+ 新增 .btn-added 三态样式 1 项）；Chromium 实测 1200px/390px 三页：已加入 #e2e5ea、斑马 #f6f7f9、未加入透明，按钮 #f97316↔#2563eb，390px 横向溢出 0）已开发完成（V3.55，SW v84）；全量测试 **701/701** 通过；阶段 64 库存管理「预警 / 盘点」深度性能优化（分页 + 区块局部刷新）（用户反馈「库存 → 预警/盘点 按钮切换加载卡顿、特别慢、没有分页，且闪屏并跳至页面头部」；**根因**：预警页固定只渲染前 300 行且无分页器，盘点页一次性渲染全部商品——实测 3000 商品 = 3000 行 + 3002 个实盘 input、单页 HTML 711 KB，且每次切标签/搜索/改实盘数都整页重建 DOM；预警页的搜索与分类筛选此前对该页完全无效。**改动**：① 三个标签统一分页并各自独立页码——库存查询 200/页（st.page，沿用）、预警 200/页（st.alertPage，新增分页器）、盘点 100/页（st.takePage，每行含输入框故页容量减半），page 动作按当前标签分流、搜索/筛选自动回第 1 页；实测盘点 3000 行+3002 input → 100 行+100 input（HTML 711 KB → 24 KB）；② 库存页 DOM 拆为 inv-head / inv-tabs / inv-search / inv-body 四区块，app.js render 同路由重渲染时优先调用页面新增的 update(ctx, state) 做字符串比对式的区块级局部刷新，未变化区块零写入、替换后恢复焦点与光标，update 返回 true 时跳过整页 innerHTML 重建且完全不调用 window.scrollTo（不闪屏、滚动位置天然保持，根治「跳至页面头部」）；其它页面未实现 update 时行为零变化，update 抛错自动回退整页渲染；实测切标签 盘点 124ms→5ms、预警 24ms→8ms、翻页 2ms 且滚动位置逐像素不变；③ 单渲染周期缓存：getAlerts（原一帧最多算 3 次）与「过滤+排序」各只算一次；④ 预警页搜索/分类筛选修复生效 + 空态文案区分；⑤ 盘点卡片新增「已填 N 项」提示，实盘数跨页保留（实测翻页往返仍保留）。测试新增 tests/inventory-perf-v356.test.js 10 项 + 更新 1 项；Chromium 390px/1200px 真机实测）已开发完成（V3.56，SW v85）；全量测试 **711/711** 通过；回滚基线 tag：**v3.55-baseline**），**阶段 65 手机端底部导航「库存」更换为「商品」**：app.js 的 navItems()（手机底部栏 .app-tabbar 唯一数据源）第二项由 { name:'inventory', icon:'📋', text:'库存' } 改为 { name:'product', icon:'📦', text:'商品' }，手机主页面底部最终为「首页 / 商品 / 我的」三项，点击直达商品档案页（用户明确选择「真正换页」并沿用商品档案原图标 📦）；库存管理页未被删除，仍可从首页入口与「我的 → 常用入口」进入，仅移除其底部固定入口（用户确认「首页已有库存入口，无需在底部继续保留」）；手机底部 navItems() 与电脑端侧栏 desktopNavOrder() 解耦，侧栏 12 项顺序不受影响；非底部页打开时高亮仍兜底到「我的」；测试新增 tests/nav-tabbar-v357.test.js 6 项 + 更新 1 项；Chromium 真机实测已开发完成（V3.57，SW v86）；全量测试 **717/717** 通过；回滚基线 tag：**v3.56-baseline**），**阶段 66 全局加载 / 切换 / 搜索深度性能优化 + 全系统列表统一每页 100 条**（用户反馈「页面切换很慢、加载也很慢」并要求所有列表改为 100 条/页以降低内存。① **核心根因**：ctx.getProduct/getPartner 是 Array.find 线性查找，而 profit.topProducts 为每条售出商品都调用它 → 3000 商品时 O(n²) 约 450 万次比较，报表页 106ms / 首页 90ms；修复为 repo.createContext 内建主键索引（失效=数组引用变化∨长度变化∨命中 key 校验，不依赖 touch 以免批量导入退化），条码查询因可原地改而仍走线性扫描保正确；② 利润聚合去重：报表畅销/滞销由两次 topProducts 改为 productAgg 单次聚合 + rankProducts 两次排序；③ 首屏懒加载：新增 js/core/lazy.js，xlsx.full.min.js(861KB) 与 zxing-decode.min.js(407KB) 改为按需注入（并发去重 + 失败可重试），首屏传输 ~1.99MB → 719KB（−64%），Excel 端到端导入与 ZXing 扫码兜底已实测可用；④ **通用区块级局部刷新**：把 V3.56 库存页专属的 update() 模式下沉为框架能力，同路由重渲染逐块比对只写变化区块（未变区块零 DOM 操作→不闪屏，且不 scrollTo→滚动位置天然保持），结构不可调和时回落整页替换，页面自带 update() 仍优先；⑤ 全系统列表统一 100 条/页：销售单 300→100、进货单 300→100、商品档案 200→100、库存查询 200→100、预警 200→100（盘点 100 与选货区 15 保持）。实测（Chromium 390px，3000 商品/2000 销售单/800 进货单）：报表 116.6→33.0ms、首页 101.1→35.3ms、商品档案 66.6→49.9ms、销售 49.8→33.2ms，各页 render() CPU 合计 104.5→10.8ms（约 9.7 倍），底部导航真实点击切换稳定 ~32ms，翻页与重复渲染滚动位置全程保持；测试新增 tests/perf-v358.test.js 22 项、tests/page-size-200.test.js 迁移为 tests/page-size-100.test.js（断言 200→100 并扩展到全部列表页+行为验证）；Chromium 全站路由与关键交互回归已开发完成（V3.58，SW v87）；全量测试 **745/745** 通过；回滚基线 tag：**v3.57-baseline**），**阶段 67 多员工账号 / 权限分级（全部手动分配，无角色模板）**：16 项权限按 4 组（零售/批发/档案库存/资金管理）由管理总控逐项手动勾选（无自动匹配）；管理总控（hawsystem）固定全权限不可改；其余账号新建默认全关、由管理总控开通（账户权限管理页新增权限分配面板：账号权限摘要 + 四组 16 项勾选 + 全选/清空 + 保存；admin 自身不可分配）；员工账号共用本店同一本数据（ownerId 归属，权限控制可见与操作、不隔离数据；新建账号可选「共用本店数据/独立数据空间」）；员工登录后按权限自动隐藏侧栏菜单与快捷入口、路由守卫拦截无权限页面、动作级拦截（开单/收款/改价/退货/盘点/建档/记账/同步等 20 余项规则）、无「报表与利润」权限隐藏商品档案成本列与首页毛利、无「建档」权限隐藏新建/编辑/删除/合并按钮、无「数据管理」权限隐藏导入/导出/清空/云同步；旧账号迁移补全权限避免锁死；审批流放二期。测试新增 tests/perm-system.test.js 21 项（含动作级拦截 7 项）、tests/ui-admin-perm.test.js 10 项；更新 ui-mine/ui-product(+2)/ui-customer/button-colors/perf-v358 适配；全量 **778/778** 通过（V3.59，SW v88）），**阶段 68 账号云同步（跨端共用账号表）**：解决「电脑网页版 / 电脑本地版 / 手机浏览器各自独立存账号（localStorage 按端隔离），A 端建的号在 B 端登录提示『账号不存在』」的跨端账号问题。设计定案（方案 A）：① **管理总控上传**——账户权限管理页新增「☁️ 账号表上传到云端」按钮（仅管理总控可见可用）：把全部账号（登录名/店铺名/角色/权限 perms/数据空间 ownerId/密码哈希，**不含明文密码**）用同步口令 AES 加密后上传到云端固定路径 `data/admin/accounts-sync.json`（GitHub Contents API 覆盖写，与现有账本云同步同一套配置/口令）；② **登录页自动降级拉取**——本地校验失败且提示「账号不存在」时，自动从云端拉取账号表 → 解密 → 合并到本地 → 重新校验登录（手机 / 本地版 / 其他浏览器首次登录无感完成）；拉取失败（本地无同步配置 / 云端无账号表 / 网络或口令错误）时静默保持原提示，不打断登录流程；③ **合并规则**——管理总控 admin（hawsystem）永远保留本地版本（系统级账号不受云端影响）；云端同名账号以云端内容为准更新（权限/数据空间以管理总控上传为准），但保留本地 id（数据空间引用稳定，避免切库丢数据）；本地独有账号保留（并集，不丢账号）；④ **配置查找**——拉取时优先用管理总控的同步配置，其次遍历本机全部按账号隔离的同步配置，最后回退全局配置（手机端只要同步过数据即可自动拉取，无需重复配置）；⑤ 上传后修改账号 / 权限需重新上传一次。**测试**：新增 `tests/accounts-sync.test.js` 16 项（exportForSync 脱敏导出含哈希不含明文、mergeCloud 五条规则、findSyncConfig 配置查找优先级、pushAccounts 加密上传到固定路径（mock fetch 验证明文不出现在信封）、pullAccounts 下载解密/云端无表 NO_SNAPSHOT/无配置 NO_CFG、tryCloudLogin 本地无账号云端拉取登录成功 / 云端合并后密码错误返回真实校验错误 / 云端无表静默降级 / 本地已有账号直接成功）；更新 `tests/ui-mine.test.js` 与 `tests/perf-v358.test.js` 版本号断言；全量 **795/795** 通过（V3.60，SW v89）；**阶段 69 账号云同步 V2：零配置公开拉取**（用户实测 V3.60 后手机仍无法登录测试号 pifadianqi：① 管理总控上传的「2 个账号」实测为 hawsystem + diaguser——pifadianqi 建在本地版/其他浏览器，从未出现在管理总控所在浏览器的账号表里，云端自然没有；② 拉取依赖本机同步配置，管理总控浏览器自身无配置 → NO_CFG 且静默降级成「账号不存在」，用户看不到真实原因）。方案 A 确认后重构：① **内置固定密钥**：账号表改用 `sync.ACCOUNTS_PASSPHRASE`（代码内嵌，仅防明文裸奔；账号表仅含密码哈希不含明文，属传输混淆层）加密——手机/本地版/其他浏览器无需任何配置即可解密；② **零配置公开拉取**：登录页先经「公开通道」从 GitHub Pages 静态地址 `https://<owner>.github.io/<repo>/data/admin/accounts-sync.json` 拉取（无需 Token/口令、无 API 限流）；公开地址来源优先级 = 本机任意同步配置的 owner/repo（file:// 本地版可借）→ 在线 GitHub Pages URL 自动推断（`<owner>.github.io/<repo>` 正则）；公开通道失败（file:// 无配置）再回退原「本地同步配置」通道（pullAccountsAny，source 标记 public/config）；③ **上传前账号清单确认**：管理总控点上传时弹 confirm 列出将上传的账号（「即将上传 N 个账号：hawsystem、…」），避免「以为传了其实没有」；④ **拉取失败显示真实原因**（不再一律「账号不存在」）：NO_CFG → 提示先配置云同步或使用在线版；NO_SNAPSHOT → 提示先用管理总控上传账号表；网络/解密错误 → 原样透出（不含任何敏感配置）；⑤ **使用说明**：账号必须建在管理总控登录的那个浏览器（账号表来自该浏览器 localStorage），建号/改权限后重新点「上传到云端」；旧版（用户口令加密）云端文件需重新上传一次。**测试**：`tests/accounts-sync.test.js` 扩至 25 项（pushAccounts 内置密钥加密+非内置密钥解不开、publicAccountsUrl 在线推断/file://借配置推断/配置优先、pullAccountsPublic 静态文件解密/404 NO_SNAPSHOT/无配置 NO_CFG_PUBLIC/网络透出、pullAccountsAny 公共优先/回退配置/错误分类、tryCloudLogin 零配置登录成功/云端无表提示上传/本地无配置提示环境/网络透出）；更新 `tests/ui-mine.test.js`、`tests/perf-v358.test.js` 版本断言；全量 **804/804** 通过（V3.61，SW v90）），**阶段 70 新用户登录后没有任何数据（共用本店数据的云同步走错数据空间）**（用户反馈「新用户已分配权限，但是登录后没有任何数据」。**排查**：把线上 `data/admin/accounts-sync.json` 用内置密钥离线解密核对，确认新账号 pifa（acct1）ownerId=admin、数据空间指向 `applianceErp_admin` 正确、权限 2 项（product_read + data_manage）也已分配——**账号与权限都没问题**，问题在业务数据链路。**根因**：① 账号表云同步（V3.60/61）只同步账号，不同步业务数据；商品/单据/账本存在各设备本地 IndexedDB，换设备登录必然为空；② 代码缺陷——`js/ui/page-mine.js` 的同步配置 key 与快照路径按 `currentAccount.id` 定位（`sync.loadConfig/saveConfig(..., currentAcctId())`），员工账号（id=acct1）会去找 `data/acct1/erp-snapshot.json`（实测 404），而老板的数据在 `data/admin/erp-snapshot.json`（实测 200）→ 员工点「从云端恢复」永远报「云端还没有快照」；③ 快照随行账户档案写员工自己的，会把老板店名/头像覆盖。**改动**：`page-mine.js` 新增 `syncAcctId()`（`accounts.dataOwnerId(currentAccount)`，共用本店数据→admin，独立→自身 id）替换全部配置读写维度；新增 `sharesBossData()` / `isEmptyLedger(ctx)`；`sync-up` 在「共用本店数据 + 本机空账本」时阻断上传并提示先恢复，非空时弹出覆盖全店共享快照的二次确认；新增 `syncAccountPublic()`（快照档案取归属账号）与 `dataSpaceTip()`（云同步卡片显示数据空间与库名）；`page-admin.js` 新增 `page.dataSpaceOf(a)` 纯函数 + 账号卡片「数据空间：共用本店数据（applianceErp_admin）/ 独立（applianceErp_xxx）」徽标，新建账号表单补充说明独立数据空间即空账。**临时解法（不改代码即可生效）**：在新用户那台设备先用 hawsystem 登录 → 我的 → 云同步 → 从云端恢复（老板数据落进该设备的 `applianceErp_admin`）→ 退出 → 用新账号登录即看到同一本账。**测试**：新增 `tests/shared-data-sync-v362.test.js` 10 项；更新 `tests/ui-mine.test.js`（同步接线断言改 `syncAccountPublic()` + 版本）、`tests/perf-v358.test.js`（版本）；全量 **814/814** 通过（V3.62，SW v91），**阶段 71 本机空账本引导**（V3.62 让员工「能」拉到本店数据，但新员工在新设备登录仍是一片空白、不知该做什么——这是用户最初反馈「登录后没有任何数据」的真实体感）。**改动**：`js/ui/page-home.js` 新增 `emptyLedgerGuide(ctx)`，在 `render()` 的提醒区顶部插入：已登录 + 本机空账本时显示「📭 本机还没有数据」卡片，**按数据空间分流**——共用本店数据：说明与老板共用同一本账、但这台设备还没拉到数据，给出两步操作（退出→管理总控登录→我的→云同步→从云端恢复→退出再用本账号登录）+「去云同步」按钮（`data-act="go" data-page="mine"`）；独立数据空间：说明是全新空账本 +「去建档 / 导入」按钮（`data-page="product"`）。**未登录（无账号上下文）不显示**（避免单测等无账号场景凭空多卡片）；已有商品/进货/销售/账目任一即不显示。判空抽为公共函数 `util.isEmptyLedger(ctxOrData)`，`page-mine.js` 空库上传保护改为复用。**测试**：新增 `tests/empty-ledger-guide-v363.test.js` 6 项；更新 3 个测试文件的版本号断言；全量 **820/820** 通过（V3.63，SW v92），**阶段 72 首页横幅「已备份」文案真实性**（V3.63 真机截图复核时发现：首页横幅副标题一直是**硬编码**的「已备份 · 今天 09:12」（`mobileHome`/`desktopHome` 各一处），而 `ctx.data.lastBackupAt` 有真实数据、下方 `backupReminder` 也正确提示「你已从未备份」——同一屏出现「横幅说已备份 / 提醒条说从未备份」的自相矛盾，会让用户误以为数据已有备份，属会引发真实数据风险的显示 Bug）。**改动**：`js/core/util.js` 新增纯函数 `util.backupLabel(lastISO, todayStr)`（无记录→`尚未备份 · 建议尽快导出一份账本`、当天→`已备份 · 今天 HH:MM`、30 天内→`已备份 · N 天前`、更早→`上次备份 · YYYY-MM-DD`）；`js/ui/page-home.js` 新增 `bannerBackupText(ctx)` 由 `ctx.data.lastBackupAt` 派生，两处 `banner-sub` 统一改用。**测试**：新增 `tests/backup-banner-v364.test.js` 8 项；更新 4 个测试文件版本断言；全量 **828/828** 通过（V3.64，SW v93）
- 开发依据：`PRD_家电版.md`（**V3.76**）
- 开发对象：全新项目 `/Users/ybf/Desktop/Home Appliance Wholesale/erp/`（复制母版《Shoes and clothing ERP》的 `erp/` 目录改造）

---

## 〇、总体说明

### 0.1 技术栈（沿用母版，零新增依赖）

| 项 | 方案 |
| --- | --- |
| 语言/框架 | 原生 JS，无框架、无构建、无 npm 依赖；`window.ERP` 命名空间，经典 `<script>` 按序加载 |
| 存储 | IndexedDB（每账号独立库 `erp_<账号id>`）+ localStorage（账号列表/设置/密码哈希） |
| 部署 | 静态网页，本地 `file://` 双击可用；扫码实时预览需 https/localhost；GitHub Pages 在线部署 |
| 测试 | `node --test tests/`（Node 22 自带），沿用母版测试基建（含 jsdom UI 测试） |
| 版本控制 | Git 每完成一个功能提交一次，消息遵循「功能/修复 + 涉及模块 + 新增测试数」 |

### 0.2 关键改造原则

1. **单据是唯一事实来源**：库存、流水、欠款全部由单据派生，不做手改余额入口。
2. **金额一律以「分」为整数存储**，显示时除 100，避免浮点误差（沿用）。
3. **成本用进价快照**：单据保存时固化成本，历史报表不随商品档案成本改动而变化。
4. **无编码**：商品内部自增 ID 作主键，界面不出现任何编码；品牌+型号做重复校验。
5. **每阶段闭环**：阶段内每个功能 = 实现 → 单测通过 → 提交；阶段末跑全量测试 + 验收。
6. **账号 role 分级（V2.3）**：管理员（`admin`）可管理全部账号经营范围；普通店铺账号无权限管理入口。

### 0.3 涉及目录（从母版复制后改造）

```
erp/
├── index.html            # SPA 外壳：调整 script 引入清单（移除 coding/渲染打印层）
├── manifest.webmanifest  # 名称/图标 → 电器店
├── sw.js                 # 缓存版本号 +1
├── css/                  # base/mobile/desktop/print（文案、品牌色保留）
├── js/
│   ├── core/             # schema/inventory/engine/cart/docNo/ledger/debt/profit/backup/sync/accounts/excel/util
│   ├── store/            # db.js / repo.js（库名、表结构）
│   ├── ui/               # router/components + 10 个 page-*.js
│   ├── barcode/          # 仅保留并改造 scan.js（render/label/print-bt 移除）
│   └── app.js            # 启动、落库、锁屏（文案/库名）
├── vendor/xlsx.full.min.js  # 保留（CSV/Excel 导入）
└── tests/                # 母版 56 个测试文件改造 + 新增
```

### 0.4 数据表结构（改造后）

| store | 说明 | 主键 | 关键字段 |
| --- | --- | --- | --- |
| products | 商品（单层，无 SKU） | `id`（自增） | brand, model, category, unit, cost, priceWholesale, priceRetail, stock, note, barcodes[]（原厂条码）, status |
| purchases | 进货单 | `no` | supplierId, items[{productId, qty, unitCost}], totalPaid, debt |
| sales | 销售单 | `no` | customerId, items[{productId, qty, unitPrice, priceType(w/r), costSnapshot, type, giftReason}], payMethod, paid, debt |
| stocktakes | 盘点调整单 | `no` | items[{productId, bookQty, actualQty, diff}] |
| stockLogs | 库存流水 | `id` | productId, docNo, dir(+/-), qty, balance |
| ledgers | 收支流水 | `id` | type, amount, refNo, note |
| partners | 往来单位 | `id` | type(supplier/customer), balance, debts[] |
| logs | 操作日志 | `id` | time, action, refNo |
| meta | 元信息 | `key` | schemaVersion 等 |

> **移除**：`skus`（无属性）、`printJobs`（无标签打印）。全项目清理相关引用。

---

## 一、阶段 0：项目初始化与基线

**目标**：建立电器版项目骨架，确认母版测试基线可跑通，作为改造起点。

| # | 任务 | 涉及文件/动作 | 验收 |
| --- | --- | --- | --- |
| 0.1 | 复制母版 `erp/` → 本项目 `erp/`，排除 `.git/node_modules` | `cp -R` | 目录完整 |
| 0.2 | 初始化 Git 仓库 + `.gitignore` | `git init` | 仓库就绪 |
| 0.3 | 运行母版全量测试，记录基线数 | `node --test tests/` | 基线 = 母版 441 项通过 |
| 0.4 | 写 README（项目目标/运行方式/备份纪律） | `README.md` | 文档骨架就绪 |

- **测试**：基线测试通过（改造前的对照）。
- **提交点**：`chore: 初始化电器版项目骨架（复制母版erp，测试基线441通过）`

---

## 二、阶段 1：数据模型与核心逻辑改造 ★核心

**目标**：把「款 + SKU」两级模型改造成「单层商品」，让引擎、库存、记账、利润在无编码、无属性的新模型上跑通。

> 本阶段**不做 UI**，全部为 `js/core` + `js/store` 纯逻辑层，保证可 Node 单测。

### 任务清单

| # | 任务 | 涉及文件 | 验收点 |
| --- | --- | --- | --- |
| 1.1 | **schema 改造**：库名 `shoeErp`→`erp`；移除 `skus/printJobs` store；商品字段改 9+1 字段；类型字典替换（冰箱/洗衣机/空调/电视/厨房电器/生活小家电/数码影音/配件耗材/其他）；移除编码相关（`DEFAULT_CATEGORY_PREFIX`、`oneCodePerSku`、`label/print` 设置）；默认店名「我的电器店」 | `core/schema.js` | schema 测试通过 |
| 1.2 | **db 改造**：IndexedDB 建表去掉 `skus/printJobs`；`dbNameFor` → `erp_<账号id>`；`KEY_PATH` products 用 `id` | `store/db.js` | 建库/读写测试通过 |
| 1.3 | **商品仓库**：新增/编辑/停售/查重（品牌+型号）、原厂条码数组、成本/批发/零售价存取；CSV 导入字段对齐 | `core/repo.js`（或 product 仓库函数） | 商品 CRUD + 查重单测 |
| 1.4 | **库存改造**：库存键从「款号+颜色+尺码」→ `productId` 单一数量；变动流水按商品记录 | `core/inventory.js` | 进货+/销售−/退货±/盘点 单测 |
| 1.5 | **单据引擎改造**：purchase/sale/gift/refund/stocktake 明细引用 `productId`（替代 SKU id）；销售明细增加 `priceType(wholesale/retail)` 与 `costSnapshot`；作废回滚逻辑对齐 | `core/engine.js` | 全业务闭环单测（进货→库存→销售→流水→欠款→作废回滚） |
| 1.6 | **开单购物车**：移除颜色/尺码选择；行含 批发价/零售价 两价，默认零售可切批发；赠品标记保留 | `core/cart.js` | 购物车单测 |
| 1.7 | **记账/欠款/利润**：沿用逻辑，仅确认成本口径走 `costSnapshot`；三层利润 + 库存资金占用计算对齐新模型 | `core/ledger.js` `core/debt.js` `core/profit.js` | 利润口径单测（进价2000/零售2699/赠1/费500 用例） |
| 1.8 | **备份/恢复/同步/账号**：备份 JSON 结构与新表一致（去 skus/printJobs）；同步快照路径 → `data/<acctId>/erp-snapshot.json`；多账号预置 大家电店/小家电店/厨电店，经营范围=类型字典 | `core/backup.js` `core/sync.js` `core/accounts.js` | 备份恢复一致、账号隔离单测 |
| 1.9 | **单据号/工具/编码层清理**：`docNo` 沿用；**移除 `coding.js`** 引用并删除；`util.js` 清理尺码/颜色清洗，保留金额/校验工具 | `core/docNo.js` `core/util.js` | util 测试通过 |
| 1.10 | **excel 导入**：字段映射改为 品牌/型号/类型/单位/成本/批发价/零售价/备注/期初库存 | `core/excel.js` | 导入单测 |
| 1.11 | **index.html script 清单**：移除 coding/label/render/print-bt 引入 | `index.html` | 页面无报错（jsdom 冒烟） |

- **阶段测试**：全部 core/store 单测通过 + 5000 单压测（沿用 perf 用例改键名）。
- **提交点**：`feat(核心): 单层商品模型改造——移除编码/SKU，商品9字段+成本+批发/零售双价，库存/引擎/记账/利润/备份/同步对齐，新增N条测试`

---

## 三、阶段 2：扫码模块改造（D3 保留）

**目标**：把「扫款号/内部条码」改成「扫**机身原厂条码/二维码**定位商品」，并支持 QR 识别。

| # | 任务 | 涉及文件 | 验收点 |
| --- | --- | --- | --- |
| 2.1 | **扫码解析改造**：`resolve` 改为按 `product.barcodes[]`（归一化：去空白/转大写）匹配商品；无内部编码匹配逻辑 | `js/barcode/scan.js` | resolve 单测：扫原厂条码/二维码内容 → 定位商品 |
| 2.2 | **识别格式扩展**：实时预览 `BarcodeDetector` formats 增加 `qr_code`；拍照识别 ZXing 兜底支持 QR | `js/barcode/scan.js` | 用例覆盖 formats 含 qr_code |
| 2.3 | **扫码入口对齐**：建档页（扫原厂码自动填入）、库存页（扫码查库存）、开单页（扫码加行）；未建档提示先建档 | `js/ui/page-product.js` `page-inventory.js` `page-sale.js` | UI 冒烟 + 逻辑单测 |
| 2.4 | **移除标签打印**：`barcode/render.js` `label.js` `print-bt.js` 删除；相关设置项清理 | 删除文件 + schema/设置 | 无残留引用 |
| 2.5 | **商品卡改造**：扫码结果弹出「品牌/型号/单位/成本/批发价/零售价/库存」卡片（无矩阵） | `js/barcode/scan.js` + components | 商品卡渲染单测 |

- **阶段测试**：scan resolve/card 单测 + UI 冒烟。
- **提交点**：`feat(扫码): 原厂条码/二维码定位商品——resolve改造、BarcodeDetector增加qr_code、入口对齐、移除标签打印层，新增N条测试`

---

## 四、阶段 3：UI 页面改造

**目标**：全部页面在新数据模型上可用，手机+电脑双端一致，文案电器化。

| # | 任务 | 涉及文件 | 验收点 |
| --- | --- | --- | --- |
| 3.1 | **商品档案页**：9 字段表单；列表**品牌、型号分两列**展示；搜索按品牌/型号/类型；CSV 导入入口；期初库存 | `js/ui/page-product.js` | UI 测试：建档/查重/列表两列/导入 |
| 3.2 | **开单页**：搜索选品 + 行内**批发/零售价切换**（默认零售）+ 改价 + 整单折扣 + 组合收款 + 赠送标记 + 扫码加行 | `js/ui/page-sale.js` | UI 测试：加行/切价/收款/赠送/扫码加行 |
| 3.3 | **进货页**：选商品 + 数量 + 进货单价（**带出档案成本可改**），保存后同步档案成本 | `js/ui/page-purchase.js` | UI 测试：带出成本/保存同步 |
| 3.4 | **库存页**：商品级列表（品牌/型号/库存/预警/单位）、扫码查库存、盘点（无矩阵） | `js/ui/page-inventory.js` | UI 测试 |
| 3.5 | **报表/首页**：文案电器化、指标口径不变（今日销售额/销量/毛利/预警/TOP5/趋势） | `js/ui/page-report.js` `page-home.js` | UI 测试 |
| 3.6 | **登录/我的/账号/供应商/退换货/设置**：预置账号改 大家电店/小家电店/厨电店；经营范围=类型；移除标签打印设置项；其余文案替换 | `page-login.js` `page-mine.js` `page-account.js` `page-supplier.js` `page-exchange.js` `page-setting.js` | UI 测试 |
| 3.7 | **导航/公共组件/CSS**：侧栏/Tab 文案（我的电器店）、按钮/表格/弹窗组件对齐新字段 | `js/ui/router.js` `components.js` `css/*` | UI 冒烟 |
| 3.8 | **app.js**：启动流程、锁屏、库名（`erp_<账号id>`）、版本号 | `js/app.js` | 启动冒烟 |

- **阶段测试**：全部 UI 测试（jsdom）+ 冒烟。
- **提交点**：`feat(UI): 全页面电器化——商品两列品牌/型号、开单批发零售切换、进货成本带出、扫码入口、预置账号，新增N条测试`

---

## 五、阶段 4：测试改造与回归

**目标**：原母版 441 项测试全部迁移到新模型，补齐成本/双价/扫码/新功能用例，全量通过。

| # | 任务 | 涉及文件 | 验收点 |
| --- | --- | --- | --- |
| 4.1 | **删除/改造编码类用例**：`coding.test.js` 移除；`barcode.test.js` 改为原厂条码匹配用例 | `tests/*` | 无编码用例残留 |
| 4.2 | **改造引擎/库存/利润/记账/备份用例**：SKU 键 → productId；成本口径 → costSnapshot | `tests/inventory.test.js` `engine相关` `profit.test.js` `ledger.test.js` `debt.test.js` `backup.test.js` | 全过 |
| 4.3 | **改造 UI 用例**：商品页/开单页/库存页/首页/登录/账号 断言对齐新字段与新文案 | `tests/ui-*.test.js` `scope*.test.js` `issue*` | 全过 |
| 4.4 | **新增用例**：成本同步（进货后档案成本更新且历史快照不变）、批发/零售双价成交、原厂条码扫码定位（含 QR 内容）、品牌/型号查重、CSV 导入含成本列 | `tests/新增` | 新增用例全过 |
| 4.5 | **全量回归 + 压测**：全部 `node --test tests/` + 5000 单压测 + 双端/断网冒烟 | 全量 | 全绿 |

- **验收**：全量测试通过（预计 400+ 项，含新增），压测达标。
- **提交点**：`test(回归): 测试迁移至单层商品模型，新增成本/双价/扫码用例，全量N项通过`

---

## 六、阶段 5：收尾与交付

**目标**：对外可交付、可部署、可验收。

| # | 任务 | 涉及文件/动作 | 验收点 |
| --- | --- | --- | --- |
| 5.1 | **文案/标识清理**：全局搜 `鞋服/鞋店/服装/新款建档/双/码` 等残留；店名「我的电器店」、备份文件名 `电器店备份_*.json` | 全局 grep + 替换 | 无鞋服残留 |
| 5.2 | **PWA 收尾**：manifest 名称/图标、sw.js 缓存版本 +1 | `manifest.webmanifest` `sw.js` | 添加到主屏幕正常 |
| 5.3 | **敏感信息审查**：检查无 AK/SK、数据库口令、隐私数据、硬编码密钥（沿用母版审查清单） | 全局 | 无敏感信息 |
| 5.4 | **README 完善**：运行方式、备份纪律、部署说明、测试命令 | `README.md` | 文档完整 |
| 5.5 | **最终全量测试**：`node --test tests/` 全绿 | 全量 | 全绿 |
| 5.6 | **Git 发布**：提交 + 打 tag（v1.0.0） | Git | 干净工作区 |

- **提交点**：`chore(发布): 电器版 v1.0.0——文案清理、PWA收尾、敏感信息审查通过、全量测试N项全绿`

---

## 六·五、已完成阶段记录（2026-09-02 起，持续更新）

主流程 6 阶段（0–5）与后续功能已全部开发完成并验收，全量测试逐轮增长至 **270/270** 通过：

| 提交 | 内容 |
| --- | --- |
| `e9e0d69`→`a653d14` 等 | 阶段 0–5：初始化、单层商品模型、扫码、UI 电器化、测试清理、收尾 |
| `0591de7` / `4bda1ba` | 问题1 类型可自定义输入 / 问题2 新建后列表即时显示 |
| `ecefe2a` | 问题3 页面 icon/logo/网页名可自定义（默认电器版图标） |
| `936c4dd` | 问题4 创建账号带头像 + 多店铺数据隔离 |
| `987f048` | 登录账户管理：编辑/删除账号（accounts.update/remove，ensurePreset 仅首次初始化） |
| `f277c6f`→`7a7e6d3` | 阶段 7：账户管理收归管理员（登录页取消编辑/删除/新建按钮；权限页升级为账户管理总控：新建/修改/删除账号+经营范围，admin 不可改删；删除清理数据空间） |
| `51721e5` | 阶段 8-1 商品档案备注列过长优化（省略号+title 悬停查看全文） |
| `5747528` | 阶段 8-2 云同步 403 诊断细分 +「测试连接」预检（sync.checkAuth） |
| `519f314` | 阶段 8-3 上传大小控制（gzip 压缩+加密信封 v2；100MB 超限拦截） |
| `7a9e08b` | 阶段 8-4 我的页常用入口图标与首页统一（开单手推车🛒） |
| `6b08419` | 阶段 8-5 智能同步（内容指纹 SHA-256，一致跳过上传/恢复） |
| `8317fc8` / `c6d4e4d` | SW 缓存 v5 / v6 强制线上刷新 |
| `b765110` | 阶段 8-6 取消手机版/本地版右上角铃铛与头像图标按钮 |
| `8666abf` / `9758455` | 阶段 8-7 商品档案斑马纹交替行底色（手机版/电脑版）+ SW v7 |
| `8551e01`（gh-pages） | 部署：斑马纹上线（SW v7），**保留 data/ 云端快照**（重建部署提交解决与云同步自动提交的分叉） |
| `fc793ec` | 阶段 9：客户功能页面（左侧导航+我的页入口，增删改查/按名称去重/收款跳记账中心，新增13条测试 → 全量 283/283） |
| `9935db1` | 版本号统一 V3.1（PRD / 开发计划 / 软件「关于」） |
| `df8d17a` | 阶段 10-1 开单三栏撑满布局（列等高 + min-height 消除空白） |
| `35db560` | 阶段 10-2 智能同步指纹键名排序加固（字段顺序不同但内容相同判一致，sync 增至 23 例） |
| `71d81b5` / `8350f58` | 阶段 10-3 收款模块移到页面底部横排（选货+订单两列在上、收款整行在下）+ 重新排布为一行紧凑式（宽度与两列等宽，降低高度） |
| `147df0b` | 阶段 10-4 左侧菜单更名「销售管理」（默认列表视图、右上角「销售开单」、所有开单按键统一直达开单视图） |
| `66bad16` | 阶段 10-5 销售/进货单打印（半张 A4 A5 模板、多页自动分页每页重复表头、详情弹层打印按钮，SW 清单加 print-doc.js） |
| `ade0bc8` | SW 缓存 v10→v11（收款一行紧凑排布 CSS/JS 更新）强制线上刷新 |

---

## 六·六、阶段 6：管理员账号与账户权限管理（V2.3，D6）

**目标**：新增管理员账号 `admin`（初始密码 `admina1b22c333`），登录后可进入「权限管理」管理**全部账号的经营范围**（9 类勾选 / 全部分类）；普通账号无此入口。

> 方案 A（用户已确认）：权限 = 经营范围（`scopeCategories`）。管理员不能删除自己，自身经营范围默认全部分类。

### 任务清单

| # | 任务 | 涉及文件 | 验收点 |
| --- | --- | --- | --- |
| 6.1 | **账号模型加 role**：`api.strip`/`publicList`/`create`/`update` 支持 `role`（`admin`/`user`，默认 `user`）；预置管理员账号 `admin`（shopName「管理总控」、初始密码 `admina1b22c333`、scopeCategories=全分类、role=`admin`，id=`admin`，库 `erp_admin`） | `core/accounts.js` `core/schema.js` | accounts 单测：admin 存在/初始密码可登录/role 透出 |
| 6.2 | **ensurePreset 兼容**：首次初始化写入 admin+3 店；后续调用**仅确保 admin 存在**（用户删除的店铺账号不恢复，admin 为系统级必保） | `core/accounts.js` | ensurePreset 单测：删店铺不恢复、删 admin 会补回 |
| 6.3 | **权限管理页**：管理员登录后可进入；列出全部账号（头像/店名/登录名/当前经营范围）；每账号 9 类分类勾选 +「全部分类」；保存写入账号 `scopeCategories` 并提示 | `js/ui/page-admin.js`（新增）`js/ui/router.js` `app.js` | ui-admin 单测：渲染/勾选/保存/普通账号不可见 |
| 6.4 | **入口与权限判断**：「我的」页管理员显示「权限管理」入口（普通账号不显示）；非管理员访问权限页被拒 | `js/ui/page-mine.js` `app.js` | UI 测试：入口显隐 |
| 6.5 | **登录页展示管理员**：管理员账号出现在登录列表（带头像/店名），点击可输密码进入 | `js/ui/page-login.js` | ui-login 单测：admin 出现在列表 |
| 6.6 | **CSS/文案**：权限页样式、按钮/表格复用；无鞋服残留 | `css/*` | 冒烟 |
| 6.7 | **全量回归**：`node --test tests/*.test.js` 全绿（新增 accounts/admin/ui 用例） | 全量 | 全绿 |

- **阶段测试**：accounts.test.js + ui-admin.test.js（新增）+ ui-login/ui-mine 追加，全量通过。
- **提交点**：`feat(管理员): 管理员账号admin+账户权限管理——role字段、权限页按账号勾选经营范围、仅admin可见入口，新增N条测试`

---

## 六·七、阶段 7：账户管理收归管理员（V2.4）

**目标**：登录页取消账号编辑/删除/新建按钮，只做账号选择登录；账户的新建/修改/删除统一收归管理员「账户权限管理」页操作，admin 自身不可改删。

> 用户明确要求：取消账号卡片后的编辑/删除按钮与「＋ 新建店铺账号」按钮，所有账户信息都由管理总控统一新建/修改/删除。

### 任务清单

| # | 任务 | 涉及文件 | 验收点 |
| --- | --- | --- | --- |
| 7.1 | **登录页精简**：移除账号卡片编辑/删除按钮与「＋ 新建店铺账号」按钮；未选中账号提示「账户的新建/修改/删除由管理员统一管理」 | `js/ui/page-login.js` | ui-login 单测：无 edit/del/add 按钮 |
| 7.2 | **权限页升级为账户管理总控**：新增「新建店铺账号」表单（登录名/店名/初始密码/头像）；每账号卡片加「修改」（改登录名/店名/重置密码/头像）与「删除」（二次确认 + 清理数据空间 `erp_<账号id>`）按钮 | `js/ui/page-admin.js` | ui-admin 单测：新建/修改/删除/清理库 |
| 7.3 | **admin 自身保护**：管理员账号卡片无修改/删除按钮，不可被改删 | `js/ui/page-admin.js` | ui-admin 单测：admin 无改删按钮 |
| 7.4 | **全量回归**：`node --test tests/*.test.js` 全绿 | 全量 | 全绿 |

- **阶段测试**：ui-login / ui-admin 更新，全量通过。
- **提交点**：`feat(账户管理): 账户收归管理员——登录页纯登录、权限页新建/修改/删除账号、admin不可改删，新增N条测试`

---

## 六·八、阶段 8：体验优化与云同步增强（V2.4）

**目标**：完成 7 项体验与同步优化，每项独立实现 → 测试 → 提交。

### 任务清单

| # | 任务 | 涉及文件 | 验收点 |
| --- | --- | --- | --- |
| 8.1 | **商品档案备注列过长优化**：备注列 `cell-note` 类（max-width 200px + 省略号）+ `title` 悬停完整备注；空备注显示「-」 | `css/base.css` `js/ui/page-product.js` | ui-product 单测：cell-note / title |
| 8.2 | **云同步 403 诊断细分 + 测试连接**：`githubError` 细分限流（rate limit / X-RateLimit-Remaining=0）与权限不足（指引 Contents: Read and write）；新增 `sync.checkAuth` 预检；「我的」同步设置加「测试连接」按钮 | `js/core/sync.js` `js/ui/page-mine.js` | sync 单测：githubError 分支 / checkAuth |
| 8.3 | **上传大小控制（gzip + 超限拦截）**：上传前 gzip 压缩（`CompressionStream` 或 pako 兜底）→ AES-GCM 加密（信封 v2，`comp:gzip`，v1 兼容解密）；`MAX_UPLOAD_BYTES=100MB` 超限拒绝；提示显示压缩后大小 + 已压缩标记 | `js/core/sync.js` | sync 单测：gzip 往返 / 超限拦截 / v1 兼容 |
| 8.4 | **我的页常用入口图标与首页统一**：进货🚚/商品📦/库存🗄️/记账📒/报表📈/退换货🔁 与首页一致；开单🛒手推车；供应商👤/设置⚙保留 | `js/ui/page-mine.js` | ui-mine 单测：图标映射断言 |
| 8.5 | **智能同步（内容指纹比对）**：`fingerprintOfText`（SHA-256，解析 JSON 后删除动态 `exportedAt` 再取指纹）；`syncUp`/`syncDown` 一致时返回 `skipped:true` + 提示「本地与云端一致，无需重新上传/恢复」 | `js/core/sync.js` `js/ui/page-mine.js` | sync 单测：指纹稳定性 / 一致跳过 / 有差异上传/恢复 |
| 8.6 | **取消手机版/本地版右上角图标按钮**：首页 banner 铃铛按钮（mobileHome/desktopHome）、顶栏 `.bell`（含 top-bell-dot）与 `#top-avatar` 移除；app.js 红点逻辑保留（元素不存在安全跳过） | `js/ui/page-home.js` `index.html` | ui-home 单测：无 banner-action/🔔/bell/top-avatar |
| 8.7 | **商品档案斑马纹交替行底色**：`tbl-striped` 规则（nth-child(even) #f6f7f9，含 cell-note 行对齐）；商品列表 `class="tbl tbl-striped"` | `css/base.css` `js/ui/page-product.js` | ui-product 单测：tbl-striped / nth-child(even) |

- **阶段测试**：逐项新增用例（sync.test.js / ui-mine / ui-home / ui-product 追加），全量 **270/270** 通过。
- **提交点**：每项独立提交（见六·五 已完成阶段记录）；部署时 SW 版本递增（v5→v6→v7）。

---

## 六·九、阶段 9：客户管理页面（V3.1）

**目标**：把「客户」从「开单时的即时新建」升级为可独立管理的功能页，电脑端左侧导航 + 手机端「我的 → 常用入口」双入口。

> 用户明确要求：软件左侧列表页面需要添加「客户」功能页面。

### 任务清单

| # | 任务 | 涉及文件 | 验收点 |
| --- | --- | --- | --- |
| 9.1 | **新增客户页**：仿供应商管理——列表（名称/电话/备注/应收余额标签）、搜索、新增/编辑（名称必填 + 按「名称+type=customer」去重）、删除（有余额/有销售记录禁止删除） | `js/ui/page-customer.js`（新增） | ui-customer 单测：渲染/增删改查/去重/删除保护 |
| 9.2 | **入口**：电脑端左侧导航（router 注册自动出现）；手机端「我的 → 常用入口」加「客户」图标 | `index.html` `js/ui/page-mine.js` | 侧栏含 data-page="customer"；我的页含客户入口 |
| 9.3 | **收款跳转**：客户页「收款」→ 跳记账中心 deep-link `#/account?collect=<id>` 预选该客户收款 | `js/ui/page-customer.js` | collect action 跳转断言 |
| 9.4 | **全量回归**：`node --test tests/*.test.js` 全绿（新增 ui-customer.test.js 13 例） | 全量 | 全绿（283/283） |

- **阶段测试**：ui-customer.test.js（新增 13 例），全量通过。
- **提交点**：`feat(客户管理): 新增客户功能页面——左侧导航+我的页入口，客户增删改查/按名称去重/收款跳记账中心，新增13条测试`

### 六·九·一、版本号统一（V3.1 → V3.2）

自 V3.1 起，PRD 文档、开发计划文档与软件「我的 → 关于」显示的版本号统一；V3.4 随新功能上线同步递增：

| 位置 | V3.1 | V3.2 | V3.3 | V3.4 |
| --- | --- | --- | --- | --- |
| PRD_家电版.md | V3.1 | V3.2 | V3.3 | **V3.4** |
| 开发计划_电器版.md | V3.1 | V3.2 | V3.3 | **V3.4** |
| 软件「我的 → 关于」（`page-mine.js` renderAbout） | V3.1 | V3.2 | V3.3 | **V3.4** |

- 软件「关于」版本号文案：`版本：V3.4（schema v2）`。
- 历史版本（V2.2/V2.3/V2.4/V3.1/V3.2/V3.3 等）作为变更记录保留，文档顶部主版本号统一为 V3.4。

---

## 六·十、阶段 10：销售开单体验与单据打印（V3.2）

**目标**：优化销售开单页布局（收款模块底部整行、一行紧凑排布降低高度）、左侧菜单更名「销售管理」并统一开单入口、新增销售/进货单打印（半张 A4、多页分页）。

> 用户要求：① 收款模块宽度与选货区+当前订单宽度相等、元素重新排布降低高度；② 左侧菜单「销售开单」更名「销售管理」，与进货管理同等级同布局，右上角「销售开单」进入开单页，所有开单按键统一直达开单视图；③ 进货单/销售单打印，半张 A4，列表多分多页。

### 任务清单

| # | 任务 | 涉及文件 | 验收点 |
| --- | --- | --- | --- |
| 10.1 | **收款模块底部横排 + 一行紧凑式**：开单页三列改「上部两列（选货+订单）+ 底部收款整行」；收款内部一行排布（收款方式三输入 / 实收·余款·欠款 / 备注·按钮），挂账客户欠款时另起一行；宽度与两列等宽、降低高度 | `js/ui/page-sale.js` `css/desktop.css` | desktop-sale-layout / ui-sale 单测：两列+底部收款、一行排布、pm-input 定宽 |
| 10.2 | **左侧菜单更名「销售管理」**：`page.title` 改「销售管理」，`init` 默认 `tab:'list'`（列表管理视图，与进货管理同布局）；列表页头「销售管理」+ 右上角「＋ 销售开单」 | `js/ui/page-sale.js` | ui-sale 单测：title / 默认 list / 列表标题 / 开单按钮 |
| 10.3 | **开单入口统一直达开单视图**：`#/sale?tab=new` 解析并应用后清除 query；`components.go` 支持 `data-query`；首页开单大按钮、我的页「开单」入口、扫码加单均直达开单页 | `js/ui/page-sale.js` `js/ui/components.js` `js/ui/page-home.js` `js/ui/page-mine.js` `js/barcode/scan.js` | ui-sale 单测：query 直达并清除；入口 data-query 断言 |
| 10.4 | **销售/进货单打印**：新增 `js/ui/print-doc.js`（`buildDocHtml(ctx,doc,kind)` + `openPrint`）；A5 148×210mm 模板、`thead` 分页重复表头、`tr` 不跨页；销售/进货详情弹层加「打印」按钮与 `print-doc` action | `js/ui/print-doc.js`（新增）`js/ui/page-sale.js` `js/ui/page-purchase.js` `index.html` `sw.js` | print-doc 单测：A5 尺寸/分页 CSS/店铺/单号/明细/合计/赠送零金额；sale/purchase 详情打印按钮断言 |
| 10.5 | **全量回归 + 部署**：`node --test tests/*.test.js` 全绿（298/298）；SW 版本递增（v9→v10→v11）；gh-pages 部署 | 全量 | 全绿（298/298） |

- **阶段测试**：desktop-sale-layout / ui-sale / ui-purchase / print-doc / sw-cache 追加，全量 **298/298** 通过。
- **提交点**：每项独立提交（见六·五 已完成阶段记录）；部署时 SW 版本递增（v9→v10→v11）。

---

## 六·十一、阶段 11：界面优化与适配（V3.3）

**目标**：记账中心头部单行重排、库存统计卡压缩与手机端自适应、三页搜索模块重排（电脑单行 / 手机多行）、电脑端侧栏折叠 + 导航顺序重排、手机端销售开单收款模块修复、常用入口改名。

> 用户要求：① 记账中心头部标题+应付/应收合计+流水/应付/应收按钮同一行、按钮高亮；② 库存统计卡面积再减、数字图标放大，手机端自适应屏幕宽度防溢出；③ 商品档案/进货/销售三页搜索模块按记账·库存风格重排（电脑），手机端改回多行；④ 电脑端侧栏可折叠（收起仅图标）+ 导航顺序重排（首页→进货→销售→档案→库存→记账→报表→退换→供应商→客户→我的→账户权限）；⑤ 手机端销售开单收款模块乱套修复；⑥ 我的→常用入口「开单」改「销售」直接进入销售管理列表页；⑦ 最终统一版本号 V3.3。

### 任务清单

| # | 任务 | 涉及文件 | 验收点 |
| --- | --- | --- | --- |
| 11.1 | **记账中心头部单行重排**：标题 + 应付/应收合计（紧凑横排胶囊）+ 流水/应付/应收 按钮合并同一行；tab 按钮改 btn 样式、激活项 btn-primary 高亮；桌面 nowrap、窄屏换行 | `js/ui/page-account.js` `css/desktop.css` `css/mobile.css` | ui-account 单测：page-head account-head / account-sum / account-tabs / tab 按钮高亮 |
| 11.2 | **库存统计卡压缩**：内边距/高度减半、桌面 4 列 gap 3px、数字 13→24px、图标 17→30px | `css/base.css` `css/desktop.css` | ui-inventory 单测：compact 参数（padding 2px 5px / min-height 20px / value 24px / icon 30px） |
| 11.3 | **手机库存统计卡自适应**：2 列 `minmax(0,1fr)` 防溢出、卡片竖排（标签上数字下）、长金额 ellipsis 兜底 | `css/mobile.css` | ui-inventory 单测：repeat(2, minmax(0,1fr)) / flex-direction: column |
| 11.4 | **三页搜索模块重排（电脑）**：商品档案搜索+状态下拉一行；进货/销售搜索+供应商/类型一行、日期一行；手机端 search-bar flex-wrap、select 换行第二行 | `js/ui/page-product.js` `js/ui/page-purchase.js` `js/ui/page-sale.js` `js/ui/components.js` `css/mobile.css` | ui-product/purchase/sale 单测：filters 内联同一行、日期第二行；ui-nav 单测：手机多行 CSS |
| 11.5 | **电脑端侧栏折叠 + 导航重排**：侧栏折叠按钮（收起 60px 仅图标、localStorage 记忆）；`desktopNavOrder` 重排导航；商品档案 navTitle「档案管理」 | `index.html` `js/app.js` `js/ui/page-product.js` `css/desktop.css` | ui-nav 单测：navOrder / navTitle / toggle-side / collapsed CSS / 折叠按钮 HTML |
| 11.6 | **手机端销售开单收款模块修复 + 常用入口**：覆盖 base 4 列网格为纵向堆叠（收款三输入并排/统计/按钮/备注）；常用入口「开单」改「销售」去掉 tab=new 直接进列表 | `css/mobile.css` `js/ui/page-mine.js` | ui-sale / ui-mine 单测：pay-grid flex column / 按钮等宽；常用入口「销售」无 query |
| 11.7 | **版本号统一 V3.3 + 全量回归 + 最终部署**：PRD / 开发计划 / page-mine.js 关于页统一 V3.3；`node --test` 全绿（325/325）；SW 版本递增（v19→v24→v25）；gh-pages 最终部署 | 全量 | ui-mine 单测：关于页 V3.3；全绿（325/325） |

- **阶段测试**：ui-account / ui-inventory / ui-nav / ui-product / ui-purchase / ui-sale / ui-mine 追加，全量 **325/325** 通过。
- **提交点**：每项独立提交（见下「已完成阶段记录」）；最终 SW 缓存版本 **v25**。

### 已完成阶段记录（V3.3 追加）

| 提交点 | 说明 |
| --- | --- |
| `8ff318a` / `0ff3627` | 记账筛选两行 + 库存卡再缩 50% + 单行筛选（SW v18） |
| `82267e4` | 记账中心头部单行重排（SW v19） |
| `26acf3a` | 库存统计卡再缩 + 数字图标放大（SW v20） |
| `1d09b0d` | 三页搜索模块重排 + 手机库存卡修复（SW v21） |
| `edeb1d0` | 侧栏折叠 + 导航顺序重排（SW v22） |
| `47c7032` | 手机库存卡自适应 + 手机搜索多行（SW v23） |
| `933f4fd` | 手机收款模块修复 + 常用入口改名（SW v24） |
| （最终） | 版本号统一 V3.3 + 最终部署（SW v25） |

---

## 六·十二、阶段 12：手机库存卡再缩 + 打印增强 + 蓝色配色 + 跨端库存合并（V3.4）

**目标**：① 手机版库存管理顶部四统计卡高度缩小一半；② 单据打印支持版本选择（版本1 不带价 / 版本2 带价）；③ 打印页增加吸顶操作栏（打印/关闭）解决手机端打印页无法退出；④ 整体 UI 配色改为蓝色系；⑤ 定位并修复电脑版与手机版库存数量不一致（跨端记录级合并同步）。

> 用户要求：问题1 手机版库存管理页顶部四个标签高度缩小一半；问题2 打印功能增加版本1不带价格、版本2带价格可自行选择；问题3 手机端打印页添加关闭/返回按键；问题4 整体 UI 配色更改为蓝色；问题5 电脑版库存数量与手机版不同，需定位并修改。完成后更新 PRD / 开发计划并更新版本号（V3.4）。

### 任务清单

| # | 任务 | 涉及文件 | 验收点 |
| --- | --- | --- | --- |
| 12.1 | **手机库存统计卡高度减半**：`.stat-grid-compact .stat-card` min-height 34→17px、padding `4px 8px`→`1px 6px`、label 10px / value 15px、gap 4px | `css/mobile.css` | ui-inventory 单测：17px / 1px 6px / 字号压缩（9/9 过） |
| 12.2 | **打印版本选择**：`buildDocHtml(ctx, doc, kind, opts.withPrice)` 新增版本1 不带价（无价格类型/单价/金额/应收/成本列，保留件数合计与备注）；销售/进货单据详情提供「打印带价 data-price=1 / 打印无价 data-price=0 / 关闭」按钮 | `js/ui/print-doc.js` `js/ui/page-sale.js` `js/ui/page-purchase.js` | print-doc 单测：版本断言 +2；ui-sale/ui-purchase 按钮断言（30/30 过） |
| 12.3 | **打印页操作栏**：`.print-toolbar` 吸顶「打印」+「关闭」按钮，`window.close()` 返回上一页；`@media print` 隐藏不印到单据 | `js/ui/print-doc.js` | print-doc 单测：toolbar/关闭按钮/print 隐藏（7/7 过） |
| 12.4 | **整体蓝色配色**：`--c-mint-*` 系列换蓝（mint-400 `#60a5fa` / mint-500 `#3b82f6` / primary `#2563eb` / primary-dark `#1d4ed8` / primary-soft `#eff6ff`）、全站旧绿色 rgba 阴影清换、首页图表 SVG 渐变/描边改蓝、theme-color `#2563eb`；CSS 变量名沿用 `--c-mint-*` 避免改动引用 | `css/base.css` `css/desktop.css` `index.html` `js/ui/page-home.js` | ui-theme 单测 +3（primary #2563eb / banner #60a5fa / 无旧绿残留） |
| 12.5 | **跨端库存合并（电脑/手机库存不一致根因修复）**：① `changeStock` 写入商品 `updatedAt` 时间戳；② `backup.restore(ctx, raw, {merge})` 新增记录级合并——同主键取较新（updatedAt\|\|createdAt\|\|date）、云端/本地独立新增都保留、settings 本地为底云端字段优先；③ `syncDown` 恢复默认 `merge:true`（内容一致仍跳过） | `js/core/inventory.js` `js/core/backup.js` `js/core/sync.js` | sync-consistency 单测 +4：基础 push→pull 两端一致 / 幂等跳过 / 同基线双端改库存按较新合并两端一致 / 独立新增不丢失（4/4 过） |
| 12.6 | **版本号统一 V3.4 + 全量回归 + 最终部署**：PRD / 开发计划 / page-mine.js 关于页统一 V3.4（SW v27）；`node --test` 全绿（338/338）；gh-pages 最终部署 | 全量 | ui-mine 单测：关于页 V3.4；全绿（338/338） |

- **阶段测试**：ui-inventory / print-doc / ui-sale / ui-purchase / ui-theme / sync-consistency / ui-mine 追加，全量 **338/338** 通过。
- **提交点**：每项独立提交（见下「已完成阶段记录」）；最终 SW 缓存版本 **v27**。

### 已完成阶段记录（V3.4 追加）

| 提交点 | 说明 |
| --- | --- |
| `d80d97e` | 手机库存统计卡高度减半（问题1，SW v27 途中） |
| `6f81e82` | 打印版本选择：带价/无价双按钮（问题2） |
| `36102f8` | 打印页吸顶操作栏：打印/关闭（问题3） |
| `36a661e` | 整体 UI 配色改蓝色系（问题4） |
| `2fef8a4` | 跨端库存合并：记录级合并同步（问题5） |
| （最终） | 版本号统一 V3.4 + 全量回归 338/338 + SW v27 + 最终部署 gh-pages |

---

## 六·十三、阶段 13：库存卡高度贴合 + 云同步账户/系统参数 + 价格体系 + 搜索下拉宽度（V3.5）

**目标**：① 手机版库存管理顶部统计卡高度与内容贴合（文字/数字字号不变，只压缩卡片高度）；② 云同步增强——系统整体参数（settings）与账户设置（店铺名/头像/经营范围，脱敏不含密码）随快照同步，各账户按账户隔离不与其它项目公用数据库；③ 价格体系——系统整体利润率（批发/零售）自动定价取整到元 + 一键应用系统价格；④ 电脑端搜索模块下拉宽度微调（方案B）。

> 用户要求：同步时不仅数据，系统整体参数与账户设置也要同步（账户隔离）；设定系统价格体系——只提供成本、系统按整体利润率自动生成批发/零售价（利润率可在系统设置中整体设置）、用户可自行覆盖、一键使用系统价格统一重算（取整到元不含小数点）；电脑端搜索框/下拉宽度被 V3.3 重排压窄——选方案B（仅给下拉固定 min-width，手机端多行样式不受影响）。完成后更新 PRD / 开发计划并更新版本号（V3.5）。

### 任务清单

| # | 任务 | 涉及文件 | 验收点 |
| --- | --- | --- | --- |
| 13.1 | **库存统计卡高度与内容贴合**：`.stat-grid-compact` 的 label/value 锁定内容尺寸（`flex:0 0 auto; height:auto; min-height:0; padding:0`），卡片 140→~38px 贴合数字，**字号不变**（label 10px / value 24px） | `css/mobile.css` `css/base.css` | ui-inventory 单测：卡片/标签/数值固定内容高度（9/9 过） |
| 13.2 | **云同步账户档案与系统参数**：快照新增脱敏账户档案（店铺名/头像/经营范围，不含 hash/password）；`buildSnapshotText` / `syncUp` / `syncDown` 透传并写回账户设置；确认 settings 随快照同步（merge 保留两端、口令脱敏）；按账户隔离 key/路径（`applianceErp.sync.config.<acctId>` / `data/<acctId>/erp-snapshot.json`） | `js/core/backup.js` `js/core/sync.js` `js/core/schema.js` `js/ui/page-mine.js` | sync-account 单测 +6 / ui-mine +1（账户档案脱敏、同步携带/写回、隔离 key） |
| 13.3 | **价格体系**：① `schema.defaultSettings` 新增整体利润率 `wholesaleMargin:20` / `retailMargin:35`；② `product.autoPrices(ctx, costFen)` 按利润率生成批发/零售并取整到元；③ `product.save` 批发/零售留空时自动生成（自定义保留、部分留空补生成）；④ 商品档案新增「价格体系」页（改利润率 + 一键更新全部商品价格） | `js/core/schema.js` `js/core/product.js` `js/ui/page-product.js` | price-system 单测 +9（默认/自定义利润率、自动生成、保留自定义、页面渲染、一键应用） |
| 13.4 | **电脑端搜索下拉宽度微调（方案B 加强）**：`.search-bar .select` 增加 `flex:0 0 auto; width:auto; min-width:130px`——`width:auto` 覆盖 base `.select` 的 `width:100%`（防止下拉撑满容器挤窄搜索框，实测宽度不协调的根因），搜索框 `flex:1` 占满剩余；手机端 `flex:1 1 100%` 独占一行不受影响 | `css/base.css` | ui-searchbar 单测 +2（桌面 width:auto+min-width / 手机端独占一行保持不变） |
| 13.5 | **版本号统一 V3.5 + 全量回归 + 最终部署**：PRD / 开发计划 / page-mine.js 关于页统一 V3.5（SW v28）；`node --test` 全绿（357/357）；gh-pages 最终部署 | 全量 | ui-mine 单测：关于页 V3.5；全绿（357/357） |

- **阶段测试**：ui-inventory / sync-account / ui-mine / price-system / ui-searchbar 追加，全量 **357/357** 通过。
- **提交点**：每项独立提交（见下「已完成阶段记录」）；最终 SW 缓存版本 **v28**。

### 已完成阶段记录（V3.5 追加）

| 提交点 | 说明 |
| --- | --- |
| `43daf11` | 库存统计卡高度与内容贴合（字号不变，140→38px） |
| `a48be13` | 云同步账户档案/系统参数同步 + 账户隔离 |
| `33c7c33` | 价格体系：整体利润率 + 自动定价取整到元 + 一键应用 |
| `bfe8538` | 电脑端搜索下拉宽度微调（方案B）：min-width 保底 |
| `b5fecc5` | 搜索框与下拉宽度不协调修复：.search-bar .select 加 width:auto 防撑满容器（方案B 加强，SW v29） |
| （最终） | 版本号统一 V3.5 + 全量回归 357/357 + SW v28 + 最终部署 gh-pages |

---

## 六·十四、阶段 14：价格体系移入设置 + 只填成本自动定价 + 打印按钮放大（V3.6）

**目标**：① 价格体系页面从商品档案移入「我的 → 设置」；② 新建/导入商品只填成本，批发/零售自动按整体利润率填充（取整到元）；③ 手机端打印页「打印/关闭」按钮放大醒目。

> 用户要求：问题1 将价格体系页面移动到我的→设置页面中，并取消商品档案页面中的价格体系按钮；问题2 新建商品只填成本，批发/零售自动按价格体系填充，导入批量产品也只体现成本、导入后自动生成；问题3 手机端打印页的关闭、打印按钮放大醒目点，手机上更好操作。完成后更新 PRD / 开发计划并更新版本号（V3.6）。

### 任务清单

| # | 任务 | 涉及文件 | 验收点 |
| --- | --- | --- | --- |
| 14.1 | **价格体系移入设置页**：`page-setting` 新增「价格体系（整体利润率）」卡片（利润率输入 + 保存利润率 + 一键更新全部商品价格），require product；`page-product` 移除价格体系 tab/按钮/renderPriceSys/相关 actions | `js/ui/page-setting.js` `js/ui/page-product.js` | price-system 单测更新：设置页渲染/保存利润率/一键应用/商品档案已移除（16/16 过） |
| 14.2 | **只填成本自动定价（新建+导入）**：新建表单成本走 `cost-field` 联动自动填充批发/零售（用户改过则不再覆盖），批发/零售标「（自动）」；导入模板去掉批发/零售列只体现成本，引导文案说明导入后自动生成 | `js/ui/page-product.js` | price-system 单测 +5（表单联动/覆盖保护/模板无价格列/导入自动生成），ui-product 模板断言更新 |
| 14.3 | **手机端打印按钮放大**：`.print-toolbar button` 字号 17px 加粗、min-height 48px、padding 13px 32px；「关闭」加 `pb-close` 红色醒目类 | `js/ui/print-doc.js` | print-doc 单测 +1（字号/触控高度/pb-close 醒目） |
| 14.4 | **版本号统一 V3.6 + 全量回归 + 最终部署**：PRD / 开发计划 / page-mine.js 关于页统一 V3.6（SW v30）；`node --test` 全绿（365/365）；gh-pages 最终部署 | 全量 | ui-mine 单测：关于页 V3.6；全绿（365/365） |

- **阶段测试**：price-system / ui-product / print-doc / ui-mine 追加，全量 **365/365** 通过。
- **提交点**：每项独立提交（见下「已完成阶段记录」）；最终 SW 缓存版本 **v30**。

### 已完成阶段记录（V3.6 追加）

| 提交点 | 说明 |
| --- | --- |
| `fd6e2a5` | 价格体系移入「我的→设置」，商品档案移除价格体系 |
| `0defa15` | 新建/导入只填成本自动定价（cost-field 联动 + 模板去价格列） |
| `179a5bb` | 手机端打印「打印/关闭」按钮放大醒目（pb-close 红色） |
| （最终） | 版本号统一 V3.6 + 全量回归 365/365 + SW v30 + 最终部署 gh-pages |
| `68dd659` | 家电组合插画设为系统 icon 与链接分享默认图（og:image），SW v31 |
| `62d8332` | 手机端「我的」头部经营信息缩略显示（单行省略，限定手机端） |
| `9918868` | 电脑端左侧导航折叠按钮红色醒目（限定电脑端），SW v32 |
| （V3.6 补充） | 全量回归 370/370 + SW v32 + 部署上线 |

---

## 六·十五、阶段 15：管理总控账户统一 + 登录界面改造 + 库存回滚修复（V3.7）

**目标**：① 仅保留管理总控账户（登录名 `hawsystem`/密码 `admina1b22c333`），账户统一由管理总控新建分配，「账户权限管理」菜单仅管理总控可见；② 登录界面只显示登录人头像 + 账号 + 密码 + 登录按键（去掉用户列表选择式登录）；③ 修复手机版每次登录后库存回滚到旧值（恢复未落库 + 库存时间戳污染两处根因）。

> 用户要求：问题1 保留管理总控账户统一管理各类型账户，管理总控登录名改为 `hawsystem`（密码 `admina1b22c333`），只保留管理总控删除其他默认账户，只有管理总控可见账户权限管理菜单；问题2 登录界面只显示登录人头像、登录名输入框、密码输入框、登录按键，不显示全部用户列表选择式登录；问题3 排查手机版每次登录后库存数据回滚到旧值（即便已同步云资料）。完成后更新 PRD / 开发计划并更新版本号（V3.7）。

### 任务清单

| # | 任务 | 涉及文件 | 验收点 |
| --- | --- | --- | --- |
| 15.1 | **管理总控账户统一**：`accounts.PRESET` 仅保留管理总控（登录名 `hawsystem`），历史默认店铺账户 acct1-3 按 id+登录名+店名三重匹配迁移清理（避免误删同 id 自建账户，修复 ensurePreset 误删新建账户 bug）；admin 登录名强制 `hawsystem`；`app.js` 新增 `isAdmin()`，侧栏「账户权限管理」菜单仅管理总控可见 | `js/core/accounts.js` `js/app.js` | accounts/ui-admin/ui-nav/ui-login 单测更新，全量 371/371 通过 |
| 15.2 | **登录界面改造**：`page-login` 移除账号列表 pick-account，改为 登录人头像 + 登录账号输入框 + 密码输入框 + 登录按键；新增 `loginWithUsername`（登录名+密码校验） | `js/ui/page-login.js` `css/base.css` | ui-login 单测重写（渲染四要素/无账户列表/loginWithUsername/管理总控 hawsystem），全量 372/372 通过 |
| 15.3 | **库存回滚修复**：① 从云端恢复只改内存未落库 → `page-mine` 恢复后显式落库（同步上传前也先落库）；② 库存与档案编辑混用同一 `updatedAt` → `inventory.changeStock` 新增独立时间戳 `stockAt`，`backup.mergeStore` 对 products 拆分为「库存按 stockAt 取较新、档案字段按 updatedAt 取较新」 | `js/ui/page-mine.js` `js/core/inventory.js` `js/core/backup.js` | sync-consistency +3、ui-mine +1 回归测试，全量 377/377 通过 |
| 15.4 | **版本号统一 V3.7 + 全量回归 + 最终部署**：PRD / 开发计划 / page-mine.js 关于页统一 V3.7（SW v34）；`node --test` 全绿（377/377）；gh-pages 最终部署 | 全量 | ui-mine 单测：关于页 V3.7；全绿（377/377） |

- **阶段测试**：accounts / ui-admin / ui-nav / ui-login / sync-consistency / ui-mine 追加，全量 **377/377** 通过。
- **提交点**：每项独立提交（见下「已完成阶段记录」）；最终 SW 缓存版本 **v34**。

### 已完成阶段记录（V3.7 追加）

| 提交点 | 说明 |
| --- | --- |
| `6c7ee11` | 问题1 管理总控账户统一（hawsystem/仅保留管理总控/菜单权限/修复 ensurePreset 误删自建账户） |
| `08af346` | 问题2 登录界面改造（头像+账号+密码+登录键，去掉用户列表选择式登录） |
| `62a96ac` | 问题3 库存回滚修复（恢复后落库 + stockAt 库存独立时间戳 + merge 拆分合并） |
| （最终） | 版本号统一 V3.7 + 全量回归 377/377 + SW v34 + 最终部署 gh-pages |

---

## 六·十六、阶段 16：登录页双头像修复 + 密码框输入修复 + 账户名修正为 hawsystem + 侧边栏折叠按钮长条样式（V3.8）

**目标**：① 修复登录页双头像（login-brand 内 logo img 与 login-head-avatar 同时渲染）；② 修复密码框无法输入（data-live 重渲染后清空，缺 value 绑定）；③ 管理总控登录名修正为 hawsystem（原 hawystem 少一个 s）；④ 侧边栏折叠按钮展开态为红色长条覆盖侧栏宽度、折叠态回弹为小方形。

> 用户要求：问题1 登录框有两个头像，输入密码无反应需要优化；问题2 账户名为 hawsystem 需要修改；问题3 弹开菜单栏时红色要能覆盖整个红框，缩回时变成现有的按键。完成后更新 PRD / 开发计划并统一版本号（V3.8）。

### 任务清单

| # | 任务 | 涉及文件 | 验收点 |
| --- | --- | --- | --- |
| 16.1 | **登录页双头像修复**：移除 page-login.js render 中 login-brand 内的 `<img src="assets/favicon.png" alt="logo">`，仅保留文字标题，全局只保留 login-head-avatar 一处头像 | `js/ui/page-login.js` | ui-login 单测：头像 img 数量=1，login-brand 不含 img |
| 16.2 | **密码框无法输入修复**：给密码 input 补上 `value="' + esc(state.pwd) + '"` 绑定，data-live 重渲染后保留输入内容 | `js/ui/page-login.js` | ui-login 单测：密码框含 value 绑定 state.pwd；pwd dispatch 更新 state |
| 16.3 | **管理总控登录名修正为 hawsystem**：accounts.js PRESET/ensurePreset/注释/测试/PRD/开发计划全部 hawystem→hawsystem（密码仍为 admina1b22c333，id 仍为 admin） | `js/core/accounts.js` `tests/*` `PRD_家电版.md` `开发计划_电器版.md` | accounts/ui-login 单测：登录名 hawsystem 校验通过 |
| 16.4 | **侧边栏折叠按钮长条样式**：desktop.css 展开态 width:auto + margin:0 8px 4px + text-align:center 形成红色长条；折叠态 width:30px + margin:0 17px 4px 回弹小方形 | `css/desktop.css` | ui-theme 单测：展开态 width:auto 长条、折叠态 width:30px 小方形 |
| 16.5 | **版本号统一 V3.8 + 全量回归 + 最终部署**：PRD / 开发计划 / page-mine.js 关于页统一 V3.8（SW v35）；`node --test` 全绿（378/378）；gh-pages 最终部署 | 全量 | ui-mine 单测：关于页 V3.8；全绿（378/378） |

- **阶段测试**：ui-login / accounts / ui-theme / ui-mine 追加，全量 **378/378** 通过。
- **提交点**：每项独立提交（见下「已完成阶段记录」）；最终 SW 缓存版本 **v35**。

### 已完成阶段记录（V3.8 追加）

| 提交点 | 说明 |
| --- | --- |
| `0da1a60` | 登录页双头像修复 + 密码框 value 绑定 + 管理总控登录名修正为 hawsystem + SW v35 |
| `753410a` | 侧边栏折叠按钮展开态红色长条 + 折叠态回弹小方形 |
| （最终） | 版本号统一 V3.8 + 全量回归 378/378 + SW v35 + 最终部署 gh-pages |

---

## 六·十七、阶段 17：记一笔模块上移 + 全列表斑马纹 + 侧边栏按钮基础样式 + 打印布局优化 + 清空数据bug修复（V3.9）

**目标**：① 记账中心记一笔编辑模块移到列表上方，点击展开/保存取消后自动隐藏（弹性出现）；② 系统所有列表模块默认启用斑马纹区分行；③ 侧边栏折叠按钮基础样式移到 base.css（不依赖媒体查询），确保展开态红色长条、折叠态小方形在任何视口下正确显示；④ 打印模板表格文字居左、型号列放宽一倍、取消价格列、单价数量金额列缩窄；⑤ 修复手机版清空数据后刷新页面数据恢复的 bug。

> 用户要求：5个问题逐个解决，每问题加有效测试、测试通过后 Git 提交，全部完成后全量验收，最后更新 PRD/开发计划并统一版本号 V3.9。

### 任务清单

| # | 任务 | 涉及文件 | 验收点 |
| --- | --- | --- | --- |
| 17.1 | **记账中心记一笔模块上移**：将 renderManual 从列表下方移到筛选区下方、列表上方；点击「记一笔」按钮 open-manual 展开（manualOpen=true），保存 save-manual/取消 close-manual 后自动隐藏（manualOpen=false） | `js/ui/page-account.js` | ui-account 单测：默认隐藏、展开后位置在筛选区与列表之间、取消/保存后隐藏 |
| 17.2 | **所有列表模块默认斑马纹**：将斑马纹规则从 table.tbl.tbl-striped 扩展到 table.tbl，所有 .tbl 表格偶数行交替底色 #f6f7f9 | `css/base.css` | ui-theme 单测：base.css 含 table.tbl tbody tr:nth-child(even) 规则 |
| 17.3 | **侧边栏折叠按钮基础样式移到 base.css**：根因是样式只在 desktop.css @media(min-width:768px) 内，base.css 无基础样式致临界点显示默认小方形；将展开态（红色长条 width:auto height:36px text-align:right bg:#dc2626）和折叠态（30x30 text-align:center）移到 base.css | `css/base.css` | ui-theme 单测：base.css 含展开态/折叠态规则；浏览器验证展开态 bg=#dc2626 height=36px、折叠态 30x30 |
| 17.4 | **打印模板表格布局优化**：所有文字居左（去掉 class=num）、型号列放宽至 32%、取消销售单价格列（批发/零售类型）、单价 14%/数量 10%/金额 16% 缩窄、添加 colgroup 控列宽 | `js/ui/print-doc.js` | print-doc 单测：无价格列、型号列 32%、无 num 右对齐类、表头列顺序正确 |
| 17.5 | **修复清空数据后刷新恢复 bug**：根因是 clear-data 先设空数组再 touchAll，touchAll 遍历空数组不标记脏记录，flush 遇空列表 continue 跳过致 IndexedDB 旧数据未清空；改为 async 直接调用 db.clear(name) 清空每张表 + saveSettings 持久化 + await commit | `js/ui/page-setting.js` | ui-setting 单测：db.clear 被调用、内存数据清空、设置重置、源码不再用 touchAll |
| 17.6 | **版本号统一 V3.9 + 全量回归 + 最终部署**：PRD / 开发计划 / page-mine.js 关于页统一 V3.9（SW v36）；`node --test` 全绿；gh-pages 最终部署 | 全量 | ui-mine 单测：关于页 V3.9；全绿 |

- **阶段测试**：ui-account / ui-theme / print-doc / ui-setting / ui-mine 追加，全量 **388/388** 通过。
- **提交点**：每项独立提交（见下「已完成阶段记录」）；最终 SW 缓存版本 **v36**。

### 已完成阶段记录（V3.9 追加）

| 提交点 | 说明 |
| --- | --- |
| `a9716a2` | 记账中心记一笔模块移到列表上方，点击展开/保存取消后自动隐藏 |
| `f7a8d59` | 系统所有列表模块默认启用斑马纹（交替行底色） |
| `d99f1aa` | 侧边栏折叠按钮样式移到 base.css 不依赖媒体查询，展开态红色长条+折叠态小方形 |
| `89d2b18` | 打印模板表格布局优化：文字居左、型号列放宽、取消价格列、单价数量金额列缩窄 |
| `923a0f0` | 修复清空数据后刷新页面数据恢复的 bug——直接调用 db.clear 而非依赖 flush |

---

## 六·十八、阶段 18：侧边栏宽度样式移到 base.css + 表格行内输入框焦点修复（V3.10）

**目标**：① 深度修复侧边栏折叠按钮展开/折叠样式不生效（根因是窗口750px时 desktop.css 媒体查询不匹配）；② 修复表格行内输入框重渲染后焦点跳到错误输入框（根因是 inputSelector 不支持 data-id）。

> 用户要求：问题1 侧边栏折叠按钮展开/折叠样式不生效，深度排查逻辑和实现；问题2 桌面版网络版搜索输入框不能输入文字会自动清除，很严重。完成后更新 PRD / 开发计划并统一版本号（V3.10）。

### 任务清单

| # | 任务 | 涉及文件 | 验收点 |
| --- | --- | --- | --- |
| 18.1 | **侧边栏宽度样式移到 base.css**：将 .app-sidebar 宽度(var(--sidebar-w)=200px)、flex固定宽度、.app-sidebar.collapsed 宽度(60px)、折叠态brand/nav-item隐藏文字等样式从 desktop.css 移到 base.css，不依赖媒体查询；desktop.css 仅保留 position:sticky/height:100vh/overflow-y:auto | `css/base.css` `css/desktop.css` | 浏览器验证：折叠态 sideWidth=60px btn=30x30居中；展开态 sideWidth=200px btn=width:auto height=36px红色长条 |
| 18.2 | **表格行内输入框焦点修复**：inputSelector 增加 data-id 属性读取，有 data-id 时选择器末尾拼接 [data-id="xxx"] 精确定位行内输入框，覆盖 relive 和 scheduleSearchRender 两条路径 | `js/app.js` | input-live 单测：inputSelector 含 data-id 处理；所有 data-change+data-live 输入框必须有 data-name 或 data-id |
| 18.3 | **版本号统一 V3.10 + 全量回归 + 最终部署**：PRD / 开发计划 / page-mine.js 关于页统一 V3.10（SW v37）；`node --test` 全绿；gh-pages 最终部署 | 全量 | ui-mine 单测：关于页 V3.10；全绿 |

- **阶段测试**：ui-theme / ui-nav / input-live / ui-mine 追加，全量 **392/392** 通过。
- **提交点**：每项独立提交（见下「已完成阶段记录」）；最终 SW 缓存版本 **v37**。

### 已完成阶段记录（V3.10 追加）

| 提交点 | 说明 |
| --- | --- |
| `a677897` | 深度修复侧边栏折叠按钮——宽度样式移到 base.css，修复窗口750px时媒体查询不匹配 |
| `84417dc` | 修复表格行内输入框焦点跳到错误输入框——inputSelector 增加 data-id 精确定位 |

---

## 六·十九、阶段 19：变动明细上移 + 商品条数一致 + 扫码按钮修复 + 蓝牙标签打印（V3.11）

**目标**：① 库存变动明细模块移到明细表上方，关闭后自动隐藏；② 修复商品档案与库存管理商品条数不一致（移除scope过滤）；③ 修复搜索框后扫码按钮无反应；④ 新增手机版蓝牙标签打印（凝优P50等CPCL标签机）。

> 用户要求：每次只解决一个问题，每完成一个问题添加并运行对应有效测试，测试通过后创建一次 Git 提交；全部完成后运行所有测试验收；完成后更改 PRD 和开发计划并更新版本号。

### 任务清单

| # | 任务 | 涉及文件 | 验收点 |
| --- | --- | --- | --- |
| 19.1 | **库存变动明细模块上移**：将变动明细模块从库存明细表下方移到上方，点击关闭按钮后 logsProduct='' 自动隐藏 | `js/ui/page-inventory.js` | 变动明细在表格上方渲染；关闭后不渲染 |
| 19.2 | **商品条数一致**：移除库存管理页面3处 inScope 过滤（filterList/desktopFilters/renderTake）和销售开单选货区 scope 过滤，所有页面商品列表一致 | `js/ui/page-inventory.js` `js/ui/page-sale.js` | 不在经营范围的商品也显示在库存管理和销售开单 |
| 19.3 | **扫码按钮修复**：商品档案页面添加 scan action（调用 ERP.scan.start 三级降级），记账中心设置 scan:false 隐藏扫码按钮 | `js/ui/page-product.js` `js/ui/page-account.js` | 商品档案扫码按钮点击后调起扫码；记账中心无扫码按钮 |
| 19.4 | **蓝牙标签打印**：新增 js/print/bluetooth-label.js 模块（Web Bluetooth 连接、CPCL指令生成、商品标签构建）；设置页面新增蓝牙连接管理；商品档案列表新增打印标签按钮 | `js/print/bluetooth-label.js` `js/ui/page-setting.js` `js/ui/page-product.js` `index.html` | 蓝牙模块接口完整；CPCL指令正确；设置页有连接管理；商品列表有打印按钮 |
| 19.5 | **版本号统一 V3.11 + 全量回归 + 最终部署**：PRD / 开发计划 / page-mine.js 关于页统一 V3.11（SW v38）；全量测试通过；gh-pages 部署 | 全量 | 关于页 V3.11；全绿；线上验证 |

- **阶段测试**：ui-inventory 追加2个（变动明细位置+scope移除）、scan-button 新增4个、bluetooth-label 新增9个，全量 **408/408** 通过。
- **提交点**：每项独立提交（见下「已完成阶段记录」）；最终 SW 缓存版本 **v38**。

### 已完成阶段记录（V3.11 追加）

| 提交点 | 说明 |
| --- | --- |
| `c9b5ee3` | 库存变动明细模块移到明细表上方，点击关闭后自动隐藏 |
| `6e329dd` | 修复商品档案与库存管理商品条数不一致——移除scope经营范围过滤 |
| `93cbb53` | 修复搜索框后扫码按钮无反应——商品档案添加scan action，记账中心隐藏扫码按钮 |
| `23d85da` | 新增手机版蓝牙标签打印——支持凝优P50等CPCL热敏标签机 |

---

## 六·二十、阶段 20：侧边栏折叠按钮深度修复（断点600px + 按钮撑满宽度）（V3.12）

**目标**：彻底解决侧边栏折叠按钮展开/折叠样式不生效问题——根因是断点768px导致750px窗口显示移动端布局（侧边栏被隐藏），且展开态按钮 width:auto 对 button 元素不撑满父容器。

> 用户要求：死磕这个问题直到解决，解决后再更新版本号。

### 任务清单

| # | 任务 | 涉及文件 | 验收点 |
| --- | --- | --- | --- |
| 20.1 | **断点降低到600px**：desktop.css 从 min-width:768px 改为 min-width:600px，mobile.css 从 max-width:767px 改为 max-width:599px，确保750px窗口显示桌面端侧边栏 | `css/desktop.css` `css/mobile.css` | 750px宽度下 media600 匹配，侧边栏 display:block |
| 20.2 | **展开态按钮撑满宽度**：.app-sidebar .side-toggle 的 width 从 auto 改为 calc(100% - 16px)，配合 margin:4px 8px 形成红色长条覆盖侧栏宽度 | `css/base.css` | 展开态按钮宽度≈183px（200px-16px margin），红色长条 |
| 20.3 | **版本号统一 V3.12 + 全量回归 + 最终部署**：PRD / 开发计划 / page-mine.js 关于页统一 V3.12（SW v39）；全量测试通过；gh-pages 部署 | 全量 | 关于页 V3.12；全绿；线上验证 |

- **阶段测试**：ui-nav 追加1个（断点600px）、ui-theme 更新4个断言（断点+width:calc），全量 **409/409** 通过。
- **提交点**：`d2d0a57`（侧边栏折叠按钮深度修复）；最终 SW 缓存版本 **v39**。

### 已完成阶段记录（V3.12 追加）

| 提交点 | 说明 |
| --- | --- |
| `d2d0a57` | 侧边栏折叠按钮深度修复——断点降低到600px+展开态按钮width:calc(100%-16px)撑满侧栏红色长条 |

---

## 六·二十一、阶段 21：蓝牙打印兼容性引导（平台检测 + 浏览器建议 + 复制链接）（V3.13）

**目标**：解决手机微信内置浏览器/自带浏览器不支持 Web Bluetooth API 时，仅显示"当前浏览器不支持蓝牙"无引导的问题。新增平台检测、针对性浏览器建议、复制链接功能和详细使用步骤。

> 用户反馈：手机蓝牙连接标签打印机时显示"当前浏览器不支持蓝牙，请使用 Chrome/Edge"，需要引导用户如何在手机上使用蓝牙打印。

### 任务清单

| # | 任务 | 涉及文件 | 验收点 |
| --- | --- | --- | --- |
| 21.1 | **平台与浏览器检测**：蓝牙模块新增 getPlatform（Android/iOS/Mac/Windows）、getBrowser（微信/Chrome/Edge/Safari等）检测函数 | `js/print/bluetooth-label.js` | 接口存在，返回正确平台/浏览器 |
| 21.2 | **结构化帮助信息**：新增 getHelpInfo 函数，根据平台返回针对性引导（Android 用 Chrome/Edge，iPhone 用 Bluefy），包含分步骤操作指南 | `js/print/bluetooth-label.js` | helpInfo 包含 platform/browser/title/steps/note |
| 21.3 | **复制链接功能**：新增 copyLink 函数，支持 navigator.clipboard 和 execCommand 降级 | `js/print/bluetooth-label.js` | 接口存在，支持两种复制方式 |
| 21.4 | **设置页引导 UI**：不支持蓝牙时显示详细引导（检测到的平台+浏览器+编号步骤+复制链接按钮） | `js/ui/page-setting.js` | 设置页有 bt-copy-link 按钮和详细引导 |
| 21.5 | **商品页打印引导**：点击打印标签按钮不支持蓝牙时自动复制链接并提示 | `js/ui/page-product.js` | 商品页调用 getHelpInfo 和 copyLink |
| 21.6 | **版本号统一 V3.13 + 全量回归 + 最终部署**：PRD / 开发计划 / page-mine.js 关于页统一 V3.13（SW v40）；全量测试通过；gh-pages 部署 | 全量 | 关于页 V3.13；全绿；线上验证 |

- **阶段测试**：bluetooth-label 追加5个（平台检测接口+helpInfo结构+getState字段+设置页引导+商品页引导），全量 **414/414** 通过。
- **提交点**：`74f38dd`（蓝牙打印兼容性引导）；最终 SW 缓存版本 **v40**。

### 已完成阶段记录（V3.13 追加）

| 提交点 | 说明 |
| --- | --- |
| `74f38dd` | 蓝牙打印兼容性引导——平台检测+浏览器建议(Chrome/Edge/Bluefy)+复制链接按钮+详细使用步骤 |

---

## 六·二十二、阶段 22：取消蓝牙打印 + 登录头像位置调整 + 分页跳转输入框（V3.14）

**目标**：①取消蓝牙标签打印机功能（手机端UI乱套）；②登录窗口头像移到标题文字上方；③所有列表分页添加输入框可直接跳转页码。

> 用户反馈：手机端蓝牙标签打印机模块乱套，直接取消该功能；登录窗口头像位置调整；分页需要输入框跳转。

### 任务清单

| # | 任务 | 涉及文件 | 验收点 |
| --- | --- | --- | --- |
| 22.1 | **取消蓝牙打印功能**：从 index.html 移除 bluetooth-label.js 引入，设置页面移除蓝牙打印机模块，商品档案移除打印标签按钮和 action | `index.html` `js/ui/page-setting.js` `js/ui/page-product.js` | 系统中无蓝牙打印相关UI和action |
| 22.2 | **登录头像位置调整**：将 login-head-avatar 从 login-brand 下方移到上方 | `js/ui/page-login.js` | 头像在"我的电器店/电器批发进销存·请登录"标题上方 |
| 22.3 | **分页跳转输入框**：pager 组件添加页码输入框和跳转按钮，app.js 添加全局 page-jump action，支持页码范围验证 | `js/ui/components.js` `js/app.js` | 所有分页处有输入框+跳转按钮，输入页码可跳转 |
| 22.4 | **版本号统一 V3.14 + 全量回归 + 最终部署**：PRD / 开发计划 / page-mine.js 关于页统一 V3.14（SW v41）；全量测试通过；gh-pages 部署 | 全量 | 关于页 V3.14；全绿；线上验证 |

- **阶段测试**：bluetooth-label 测试改为验证移除（6个）、ui-login 追加1个（头像位置）、pager-jump 新增4个，input-binding 排除 pager-jump-input，全量 **419/419** 通过。
- **提交点**：`61b27da`（取消蓝牙打印）、`016f207`（登录头像位置）、`ed1c81c`（分页跳转输入框）；最终 SW 缓存版本 **v41**。

### 已完成阶段记录（V3.14 追加）

| 提交点 | 说明 |
| --- | --- |
| `61b27da` | 取消蓝牙标签打印机功能——移除引入、设置页模块、商品页按钮 |
| `016f207` | 登录窗口头像移到标题文字上方 |
| `ed1c81c` | 分页添加跳转输入框——输入页码可直接跳转 |

---

## 六·二十三、阶段 23：库存管理标签间距 + 打印模板列宽调整与类型列（V3.15）

**目标**：①库存管理页标签（库存查询/预警/盘点）上下留空隙、按钮不再拥挤；②打印模板品牌列缩短至 90%、单位列缩短至 50%、单位列前新增类型列（宽度与单位列相同）。

> 用户反馈：库存管理页标签高度挤得很需要上下留空隙；打印模板品牌列缩短至90%宽度、单位列缩短至50%宽度、单位列前增加类型列（宽度与单位列相同）。

### 任务清单

| # | 任务 | 涉及文件 | 验收点 |
| --- | --- | --- | --- |
| 23.1 | **库存管理标签上下留空隙**：desktopTabs 改用专用类 `inv-tabs`（原 `row wrap mb8` 的 mb8 未定义导致无间距），base.css 新增 `.inv-tabs`（margin-top:10px / margin-bottom:14px / gap:10px）与 `.inv-tabs .btn`（min-height:44px / padding:9px 18px / 字号15px） | `js/ui/page-inventory.js` `css/base.css` | 标签行与统计卡、搜索卡之间有明显空隙；按钮高度 44px；追加测试 V3.15-问题1 |
| 23.2 | **打印模板列宽调整+新增类型列**：colgroup 品牌列 14%→12.6%（90%）、单位列 8%→4%（50%），单位列前新增类型列 4%（与单位列同宽）；表头与明细行同步输出类型；engine 进货单/销售单明细新增 `category` 字段（历史单据留空） | `js/ui/print-doc.js` `js/core/engine.js` | 列顺序 #→品牌→型号→类型→单位→…；更新既有品牌列/表头顺序断言，追加 2 个测试（V3.15-问题2） |
| 23.3 | **版本号统一 V3.15 + 全量回归 + 最终部署**：PRD / 开发计划 / page-mine.js 关于页统一 V3.15（SW v42）；全量测试通过；gh-pages 部署 | 全量 | 关于页 V3.15；全绿；线上验证 |

- **阶段测试**：ui-inventory 追加1个（标签间距+CSS断言）、print-doc 更新2处断言并追加2个（列宽/类型列/engine明细category）、ui-mine 更新1个（关于页 V3.15），全量 **422/422** 通过。
- **提交点**：`9423ffe`（库存管理标签间距）、`2294787`（打印模板列宽+类型列）；最终 SW 缓存版本 **v42**。

### 已完成阶段记录（V3.15 追加）

| 提交点 | 说明 |
| --- | --- |
| `9423ffe` | 库存管理标签上下留空隙——inv-tabs 专用类，按钮加高 44px |
| `2294787` | 打印模板列宽调整——品牌 12.6%、单位 4%、新增类型列 4%，engine 明细带 category |

---

## 六·二十四、阶段 24：打印模板类型列空白修复+列宽二轮调整 + 进货单补付款分期结清（V3.16）

**目标**：①打印模板类型列历史单据空白修复，列宽再调一轮（品牌收缩10%/类型增加20%/单位增加10%/成本收缩10%）；②进货单未结清款后续处理——按单据补付款分期结清，可继续推进欠款单据。

> 用户反馈：打印模板类型列未显示任何内容，列宽再调整；进货单未结清款后期应如何处理欠款单据，需要合理方案继续推进此单据。

### 方案说明（问题2）

- **现状缺口**：进货欠款只累加到供应商应付总余额（`partner.balance`），记账中心只能按供应商整笔付款，无法针对某张进货单推进结清，单据上的「欠款」永远是下单时的初始值。
- **落地方案（按单据补付款）**：进货管理列表未结清单行内新增「付款」按钮 → 补付款弹窗（默认填入剩余未结金额，支持分次付）→ 单据新增 `paidExtra`（补付累计）/`payLog`（日期/金额/方式/备注）/`settledAt`（结清日期）。
- **口径统一**：付款同步冲减供应商应付余额（与记账中心按供应商付款同口径，夹紧到 0）；付款流水 `refNo` 指向单号，报表可追溯。
- **作废安全**：补付款后作废进货单只回滚剩余未结部分，不与已补付款重复冲减。
- **旧数据兼容**：旧单据无新字段自动按 0/无处理，无需迁移。

### 任务清单

| # | 任务 | 涉及文件 | 验收点 |
| --- | --- | --- | --- |
| 24.1 | **类型列空白修复**：打印模板 itemCategory 兜底（明细 category 优先 → productId 回查商品档案 → 留空）；退货/换货明细补齐 category | `js/ui/print-doc.js` `js/core/engine.js` | 历史单据打印类型列显示商品类型 |
| 24.2 | **列宽二轮调整**：品牌 12.6%→11.34%、类型 4%→4.8%、单位 4%→4.4%、成本/单价 14%→12.6% | `js/ui/print-doc.js` | colgroup 与新列宽一致；更新列宽断言 |
| 24.3 | **进货单补付款**：engine.payPurchase（超额/已结清/作废拦截）+ 供应商应付冲减 + 付款流水（refNo=单号）；作废只回滚剩余未结（debt.reverseDoc 支持指定金额） | `js/core/engine.js` `js/core/debt.js` | 分次付款→结清→settledAt；超额拦截提示剩余未结 |
| 24.4 | **进货管理 UI**：列表已付含补付/欠款列剩余未结/状态三态/付款按钮+弹窗/详情补付记录/未结合计 | `js/ui/page-purchase.js` | 部分付款显示「部分付款」徽标，结清后按钮消失 |
| 24.5 | **版本号统一 V3.16 + 全量回归 + 最终部署**：PRD / 开发计划 / page-mine.js 关于页统一 V3.16（SW v43）；全量测试通过；gh-pages 部署 | 全量 | 关于页 V3.16；全绿；线上验证 |

- **阶段测试**：print-doc 更新2处并追加2个（列宽数组断言+历史单据回查/脏数据兜底、退货换货category）、purchase-pay 新增5个（补付款全流程+作废回滚+UI全流程），ui-mine 更新1个（V3.16），全量 **429/429** 通过。
- **提交点**：`85eca92`（类型列空白修复+列宽二轮调整）、`12341b4`（进货单补付款分期结清）；最终 SW 缓存版本 **v43**。

### 已完成阶段记录（V3.16 追加）

| 提交点 | 说明 |
| --- | --- |
| `85eca92` | 打印模板类型列空白修复（productId 回查兜底）+ 列宽二轮调整 |
| `12341b4` | 进货单补付款（按单据分期结清）——engine/debt/进货管理UI/测试 |

---

## 六·二十五、阶段 25：手机端库存搜索下拉同行 + 打印模板按汉字分配列宽 + 进货单品字布局 + 销售单补回款（V3.17）

**背景**：4 项用户反馈
- 手机端库存管理搜索模块的分类下拉与重置按钮被 flex 拆到三行，操作不便
- 打印模板列宽改用「按汉字宽度」分配方案更贴近用户直觉（中文表格常用 4-5 汉字为标准列宽）
- 新建进货单中间模块（按商品加行 + 进货明细）改为同行两列布局，与上方的供应商模块形成「品」字
- 销售单未结款与进货单未结款应具有同样的处理能力

| # | 任务 | 文件 | 验收要点 |
| --- | --- | --- | --- |
| 25.1 | **手机端库存搜索下拉与重置按钮同行**：将分类下拉 + 重置按钮包进 `.search-bar-filters` 容器；mobile.css 让容器整行换行、内部下拉 `flex:1 1 auto` 与重置按钮并排 | `js/ui/page-inventory.js` `css/base.css` `css/mobile.css` | 手机端：下拉与重置同一行；电脑端：下拉按内容宽、重置靠右；新增测试 V3.17-问题1 |
| 25.2 | **打印模板列宽按汉字分配**：# 2汉字(6%)、品牌 4(12%)、类型 5(15%)、单位 2(6%)、单价/成本 4(12%)、数量 2(6%)、金额 5(15%)，型号列占剩余空间（带价格版 28%、不带价格版 55%）；按列配置数组动态生成 colgroup/表头 | `js/ui/print-doc.js` | 销售单/进货单/价格版/清单版 4 种组合列宽正确；更新既有断言 + 新增测试 V3.17-问题2 |
| 25.3 | **新建进货单品字布局**：供应商选择模块在顶（不动）；按商品加行 + 进货明细包进 `.purchase-form-grid`，桌面端 `1fr 1fr` 两列并排；已付款模块在底（不动）；手机端单列堆叠 | `js/ui/page-purchase.js` `css/base.css` `css/mobile.css` | 桌面端「品」字视觉验证（截图）；手机端可正常堆叠；新增测试 V3.17-问题3 |
| 25.4 | **销售单补回款**：`engine.paySale` + 销售管理列表「回款」按钮 + 补回款弹窗 + 作废只回滚剩余未结 + 客户应收余额同步 + 收款流水；与进货单补付款完全对等 | `js/core/engine.js` `js/ui/page-sale.js` | 销售单可分期回款、状态区分/超额拦截/补回款记录/旧单据兼容；新增 tests/sale-pay.test.js 5 个测试 |
| 25.5 | **版本号统一 V3.17 + 全量回归**：PRD / 开发计划 / page-mine.js 关于页统一 V3.17（SW v44）；全量测试通过 | 全量 | 关于页 V3.17；全绿 |

- **阶段测试**：ui-inventory 追加1个（搜索下拉同行）、print-doc 更新既有并追加2个（按汉字分配列宽）、ui-purchase 追加1个（品字布局+CSS断言）、sale-pay 新增5个（paySale 全流程/UI 列表与弹窗）、ui-nav/ui-searchbar 旧断言改为新行为、ui-mine 追加1个（V3.17），全量 **437/437** 通过。
- **提交点**：`cb0578b`（问题1）、`8e4eab9`（问题2）、`8ed19f4`（问题3）、`c071f87`（问题4）；最终 SW 缓存版本 **v44**。

### 已完成阶段记录（V3.17 追加）

| 提交点 | 说明 |
| --- | --- |
| `cb0578b` | 手机端库存管理搜索模块——分类下拉与重置按钮同行（search-bar-filters 容器） |
| `8e4eab9` | 打印模板列宽按汉字分配，型号列占剩余空间 |
| `8ed19f4` | 新建进货单表单品字布局（purchase-form-grid 桌面端两列、手机端堆叠） |
| `c071f87` | 销售单补回款（与进货单补付款同功能） |

---

## 六·二十六、阶段 26：分页200条 + 打印列宽三轮调整与表头样式优化（V3.18）

**目标**：①商品档案和库存管理分页每页200条，提升显示速度和内存优化；②打印模版列宽第三轮调整（品牌-10%、类型-20%、型号+约10%），表头加粗加大居中、底行双线。

> 用户反馈：数据量大时分页显示条数需增加到200条以提升速度；打印模版品牌列和类型列需再缩窄，型号列放宽，表头需加粗加大居中且底行用双线。

### 任务清单

| # | 任务 | 涉及文件 | 验收点 |
| --- | --- | --- | --- |
| 26.1 | **分页每页200条**：商品档案和库存管理均配置 `util.paginate(list, page, 200)`，新增测试固化 | `js/ui/page-product.js` `js/ui/page-inventory.js` `tests/page-size-200.test.js` | 两页面分页大小均为200条 |
| 26.2 | **打印列宽三轮调整**：品牌 chars 4→3.6（-10%，12%→10.8%），类型 chars 5→4（-20%，15%→12%），型号(rest)自然增加（带价格版28%→32.2%，不带价格版55%→59.2%） | `js/ui/print-doc.js` | 列宽符合新比例 |
| 26.3 | **表头样式优化**：表头文字加粗（font-weight:700）加大一个字号（11px→12px），左右居中（text-align:center），底行双线（border-bottom:3px double #000） | `js/ui/print-doc.js` | 表头样式符合要求 |
| 26.4 | **打印内容居中+型号列折行**：所有表格内容左右居中（th,td text-align:center，移除num右对齐），表格 table-layout:fixed 保障列宽弹性，型号列 td 添加 model-col class（word-break:break-all + word-wrap:break-word + white-space:normal），内容过长自动折行不截取，单元格 vertical-align:middle 垂直居中 | `js/ui/print-doc.js` | 所有内容居中，型号列过长折行 |
| 26.5 | **版本号统一 V3.18 + 全量回归 + 最终部署**：PRD / 开发计划 / page-mine.js 关于页统一 V3.18（SW v45）；全量测试通过；gh-pages 部署 | 全量 | 关于页 V3.18；全绿；线上验证 |

- **阶段测试**：page-size-200 新增3个，print-template-v315 新增10个，print-doc 旧测试更新列宽断言，全量 **450/450** 通过。
- **提交点**：`29f402b`（分页200条测试固化）、`e3893f9`（打印模版列宽+表头样式优化）、`d5ff5ee`（打印内容居中+型号列折行）；最终 SW 缓存版本 **v45**。

### 已完成阶段记录（V3.18 追加）

| 提交点 | 说明 |
| --- | --- |
| `29f402b` | 验证商品档案和库存管理分页每页200条（已配置，新增测试固化） |
| `e3893f9` | 打印模版优化——品牌-10%类型-20%型号+约10%，表头加粗加大12px居中，底行双线 |
| `d5ff5ee` | 打印模版所有内容居中显示，型号列过长自动折行不截取 |
| `93fb2ab` | 打印带价按钮橘色、打印无价按钮黄色 |
| `b5ba41f` | 系统所有13个取消按钮统一红色 |

---

## 六·二十七、阶段 27：打印按钮颜色区分 + 所有取消按钮统一红色（V3.19）

**目标**：①打印带价/打印无价按钮颜色区分（橘色/黄色）；②系统所有取消按钮统一红色。

> 用户反馈：打印按钮需要颜色区分便于识别；取消按钮统一红色更醒目。

### 任务清单

| # | 任务 | 涉及文件 | 验收点 |
| --- | --- | --- | --- |
| 27.1 | **打印按钮颜色区分**：CSS新增 btn-orange（#f97316橘色）和 btn-yellow（#eab308黄色）样式；销售单和进货单的"打印带价"按钮改为 btn-orange，"打印无价"按钮改为 btn-yellow | `css/base.css` `js/ui/page-sale.js` `js/ui/page-purchase.js` | 4个打印按钮颜色正确 |
| 27.2 | **所有取消按钮红色**：全系统13个取消按钮（销售单3、进货单2、商品档案1、管理员3、供应商1、客户1、账户2）统一改为 btn-danger 红色样式（白底红字） | 7个页面文件 | 13个取消按钮全部红色 |
| 27.3 | **版本号统一 V3.19 + 全量回归 + 最终部署**：PRD / 开发计划 / page-mine.js 关于页统一 V3.19（SW v46）；全量测试通过；gh-pages 部署 | 全量 | 关于页 V3.19；全绿；线上验证 |

- **阶段测试**：button-colors 新增8个测试（橘色/黄色样式、4个打印按钮、13个取消按钮全部红色），全量 **458/458** 通过。
- **提交点**：`93fb2ab`（打印按钮颜色区分）、`b5ba41f`（所有取消按钮红色）；最终 SW 缓存版本 **v46**。

---

## 六·二十八、阶段 28：所有关闭/返回按钮红色 + 商品档案搜索模块重排（V3.20）

**目标**：①全系统关闭/返回按钮统一红色，与取消按钮样式一致；②商品档案搜索模块重排——下拉收缩40%、搜索框扩宽、桌面端一行/手机端两行。

> 用户反馈：所有关闭、返回按键需改为红色与取消按键样式相同；商品档案搜索模块下拉太宽、搜索框不够宽，手机端布局需要调整。

### 任务清单

| # | 任务 | 涉及文件 | 验收点 |
| --- | --- | --- | --- |
| 28.1 | **所有关闭/返回按钮红色**：销售单/进货单单据详情关闭、库存变动明细关闭、账户流水弹窗关闭（2处）、商品档案返回、modal组件默认关闭按钮统一 btn-danger；打印页面 pb-close 已是白底红字（#dc2626）保持不变 | `page-sale.js` `page-purchase.js` `page-inventory.js` `page-account.js` `page-product.js` `components.js` | 所有关闭/返回按钮红色 |
| 28.2 | **商品档案搜索模块重排**：searchBar 组件支持 cls 参数；商品档案搜索栏加 search-bar-product class；下拉 min-width 130px→78px（收缩40%）搜索框 flex:1 扩宽；手机端搜索框独占一行（flex:1 1 100%），下拉+扫描按钮并排一行 | `components.js` `page-product.js` `base.css` `mobile.css` | 桌面端一行扩宽搜索框，手机端两行排列 |
| 28.3 | **版本号统一 V3.20 + 全量回归**：PRD / 开发计划 / page-mine.js 关于页统一 V3.20（SW v47）；全量测试通过；**部署待用户确认** | 全量 | 关于页 V3.20；全绿；等待部署指令 |

- **阶段测试**：button-colors 追加4个（关闭/返回/modal/打印关闭），product-search-layout 新增5个，全量 **467/467** 通过。
- **提交点**：`8b1cb28`（所有关闭/返回按钮红色）、`f815273`（商品档案搜索模块重排）、`2fa4d14`（版本号统一 V3.20）；最终 SW 缓存版本 **v47**，已部署上线。

---

## 六·二十九、阶段 29：批量导入按型号比对更新（V3.21）

**目标**：批量导入商品信息时，与系统已有商品按「型号」比对——型号相同则以新导入数据更新商品档案，型号未匹配才新建。

> 用户反馈：批量导入商品信息，将导入信息与系统存在的信息进行比对，如果产品型号相同，品牌和价格有差异，则以新导入的产品为准更新商品档案。

### 任务清单

| # | 任务 | 涉及文件 | 验收点 |
| --- | --- | --- | --- |
| 29.1 | **按型号匹配更新**：product.js 新增 findByModel（仅型号匹配不分品牌）；importFromRows 逐行按型号查已有商品，匹配到则携带 id 走 save 更新分支（品牌/类型/单位/成本/价格全部以新导入为准，批发/零售未填时按整体利润率自动重算），未匹配才新建 | `js/core/product.js` | 型号相同→更新（品牌价格按新导入）；型号不同→新建 |
| 29.2 | **安全细节**：备注/条码仅当导入单元格非空时更新（save 更新分支 note 加 undefined 保护），避免空单元格误清空已有信息；更新已有商品不重复累加期初库存 | `js/core/product.js` | 空备注/条码保留原值；库存不重复累加 |
| 29.3 | **导入页提示**：批量导入页面说明新增更新规则提示 | `js/ui/page-product.js` | 页面显示更新规则 |
| 29.4 | **版本号统一 V3.21 + 全量回归**：PRD / 开发计划 / page-mine.js 关于页统一 V3.21（SW v48） | 全量 | 关于页 V3.21；全绿；等待部署指令 |

- **阶段测试**：price-system 追加5个（型号相同更新品牌价格、自定义价格更新、型号未匹配新建、更新不重复累加期初库存、空备注/条码保留原值），全量 **472/472** 通过。
- **提交点**：`ef58414`（批量导入按型号比对更新）；最终 SW 缓存版本 **v48**（尚未部署，等待部署指令）。

---

## 六·三十、阶段 30：批量导入多工作表支持与行数去向统计（V3.22）

**目标**：批量导入读取 Excel 全部工作表的数据；导入结果面板完整展示每一行的去向（读取/新增/更新/跳过/未导入），杜绝静默丢行。型号匹配更新规则保持 V3.21 不变。

> 用户反馈：批量导入商品只能导入 39 条信息（导入结果「新增 0 款，更新 39 款」）。排查结论：① 导入逻辑无条数上限，39 行均为型号匹配已有商品走更新；② Excel 导入此前只解析第一个工作表，后续 sheet 数据被静默丢弃；③ 空行被静默跳过且无提示。

### 任务清单

| # | 任务 | 涉及文件 | 验收点 |
| --- | --- | --- | --- |
| 30.1 | **读取全部工作表**：excel.js 新增 parseAll(buf) 返回 `[{name, rows}]` 全部工作表；parse 保持旧签名兼容（取第一个 sheet） | `js/core/excel.js` | 多 sheet 工作簿全部可读取；旧调用不受影响 |
| 30.2 | **多表行合并**：product.js 新增 mergeSheetRows(sheets)——第一个工作表整表保留（含表头），后续工作表首行若能识别为表头（≥2 个已知列名）则剥离后追加 | `js/core/product.js` | 各表数据按序合并，重复表头不混入数据 |
| 30.3 | **行数去向统计**：importFromRows 返回增加 total（数据行总数，不含表头）与 skipped（跳过空行数）；导入结果面板显示「共读取 X 行数据：新增 A 款，更新 B 款，跳过空行 C 行，未导入 D 行」；导入页说明提示「Excel 会读取所有工作表的数据」 | `js/core/product.js`、`js/ui/page-product.js` | 每一行去向可见，不再静默消失 |
| 30.4 | **版本号统一 V3.22 + 全量回归**：PRD / 开发计划 / page-mine.js 关于页统一 V3.22（SW v49） | 全量 | 关于页 V3.22；全绿；等待部署指令 |

- **阶段测试**：新增 tests/product-import-multisheet.test.js 共 10 个用例（parseAll 多 sheet、mergeSheetRows 剥离重复表头/保留非表头首行/空入参安全、total/skipped 统计、多表 xlsx 完整导入链路、结果面板行数去向展示、导入页多表提示），全量 **482/482** 通过。
- **提交点**：`abfbef2`（批量导入读取全部工作表 + 行数去向统计）；最终 SW 缓存版本 **v49**（尚未部署，等待部署指令）。

---

## 六·三十一、阶段 31：批量导入同型号多品牌匹配优先级修正（V3.23）

**目标**：系统同一型号存在多个品牌的商品时，导入行精确命中「品牌+型号」的那一个进行更新，不再报「该品牌型号已存在」。

> 用户反馈：导入《电器批发管理总控商品导入模版.csv》（表头+45 行数据），结果「共读取 45 行数据：新增 0 款，更新 39 款，未导入 6 行」，6 行报「该品牌型号已存在（创维 86Q8E / 86Q7FPro …）」。排查确认：与型号特殊符号无关，文件解析无问题（45 = 39+6 账目平，第 47 行仅为文件末尾空行）；根因是系统里这些型号同时存在不同品牌的商品（其他品牌排在档案前面），型号匹配取错目标后撞上品牌+型号查重。

### 任务清单

| # | 任务 | 涉及文件 | 验收点 |
| --- | --- | --- | --- |
| 31.1 | **匹配优先级修正**：importFromRows 中 `existing = findDuplicate(brand, model) \|\| findByModel(model)`——先精确匹配品牌+型号，找不到再按型号兜底 | `js/core/product.js` | 同型号多品牌时精确命中该品牌商品并更新，其他品牌商品不受波及；无精确匹配时保持 V3.21 型号更新规则 |
| 31.2 | **版本号统一 V3.23 + 全量回归**：PRD / 开发计划 / page-mine.js 关于页统一 V3.23（SW v50） | 全量 | 关于页 V3.23；全绿；等待部署指令 |

- **阶段测试**：price-system.test.js 追加 2 个用例（同型号多品牌优先精确匹配不报错且其他品牌不受波及、无精确匹配时型号更新规则保持），全量 **484/484** 通过。
- **提交点**：`e5194a5`（同型号多品牌匹配优先级修正）；最终 SW 缓存版本 **v50**（尚未部署，等待部署指令）。

---

## 六·三十二、阶段 32：批量导入规则重构（V3.24）

**目标**：按用户确认的导入规则重写批量导入——仅按「型号」匹配、文件内去重、系统同型号合并保留（不删除任何数据）。

> 用户确认规则：①所有导入以「型号」为匹配模式，不使用 品牌+型号；②文件内同型号重复行去重，保留最后一行；③系统已存在该型号则以导入信息为准更新 品牌/类型/单位/成本/备注（备注与条码留空时保留原值，不改动现有库存）；④系统无该型号则新建全量信息；⑤同一型号在系统中存在多个不同品牌/价格的商品时合并保留——只修改商品档案信息，**其余的关联文件（商品、单据、库存流水）一概不能删除**。

### 任务清单

| # | 任务 | 涉及文件 | 验收点 |
| --- | --- | --- | --- |
| 32.1 | **纯型号匹配**：新增 normModelKey（去空白+大写）与 findAllByModel；importFromRows 改为仅按型号匹配，撤掉 V3.23 的品牌+型号优先 | `js/core/product.js` | 系统里是别的品牌也视为同一产品，品牌以导入为准；`86Q8E` / `86q8 e` 识别为同一型号 |
| 32.2 | **规则1 文件内去重**：按型号规范化去重，保留最后一行，统计 `deduplicated` | `js/core/product.js` | 同型号 3 行 → 去重 2 行、只建 1 款、取最后一行数据 |
| 32.3 | **规则2/3 更新与新建**：已有型号全量更新档案字段（空备注/条码保留原值，不改库存）；无此型号新建并写入期初库存 | `js/core/product.js` | 品牌/类型/单位/成本/备注以导入为准；库存 7 不被期初 99 覆盖 |
| 32.4 | **规则4 合并保留**：同型号多商品时保留一个全量更新并恢复在售，其余仅置停售；**绝不删除商品/单据/库存流水，也不动库存**，统计 `merged` | `js/core/product.js` | 商品总数不变（未删除）；保留者成本更新为导入值且在售；其余停售且成本/库存原样保留 |
| 32.5 | **结果面板与说明同步**：展示 去重 M 行 / 合并停售 N 个，并提示「未删除，历史单据与库存流水完整保留」 | `js/ui/page-product.js` | 面板可见去重与合并数量 |
| 32.6 | **版本号统一 V3.24 + 全量回归**：PRD / 开发计划 / page-mine.js 关于页统一 V3.24（SW v51） | 全量 | 关于页 V3.24；全绿；等待部署指令 |

- **阶段测试**：新增 tests/product-import-rules.test.js 共 10 个用例（文件去重保留最后一行、全量更新各字段、空备注条码保留原值、更新不改库存、新建含期初库存、合并保留且不删除、停售商品库存原样保留、型号忽略空格与大小写、纯型号匹配、结果面板展示去重与合并）；同步更新 V3.23 旧用例断言以匹配规则1。全量 **494/494** 通过。
- **提交点**：`66476bb`（批量导入规则重构）；最终 SW 缓存版本 **v51**（尚未部署，等待部署指令）。

---

## 六·三十三、阶段 33：批量导入预演体检与纠错覆盖（V3.25）

**目标**：解决「商品档案品牌/价格大面积标错，需用新导入信息批量覆盖修正」的场景——导入前先看清楚会改什么，导入后能处理型号标错的遗留商品。

> 用户场景与确认：现有档案多数品牌标错、价格标错，需要及时用新导入信息覆盖。确认三点：①型号标错的旧商品（导入管不到的）提供**一键停售**；②新文件**只填成本、价格留空**，由系统按整体利润率自动重算；③已有云端快照，无需额外备份。

### 任务清单

| # | 任务 | 涉及文件 | 验收点 |
| --- | --- | --- | --- |
| 33.1 | **预演体检**：新增 `previewImport` 纯计算不写库，返回 creates/updates/merges/uncovered/errors；updates 含逐字段新旧值对比（品牌/类型/单位/成本/批发价/零售价/备注/条码） | `js/core/product.js` | 导入前可逐字段核对，确认无误再执行 |
| 33.2 | **两步导入**：导入改为「预演体检 → 确认执行」，预演阶段不写入任何数据 | `js/ui/page-product.js` | 先看到影响面再决定是否写入 |
| 33.3 | **未覆盖清单**：导入结果列出系统中存在、但文件里没有的商品（型号对不上/已淘汰），含品牌/型号/类型/库存 | `js/core/product.js`、`js/ui/page-product.js` | 型号标错的旧商品不再无声遗漏 |
| 33.4 | **一键停售**：新增 `retireProducts(ctx, ids)` 批量置停售，返回 retired/withStock/missing；清单下方提供「全部停售」按钮 | `js/core/product.js`、`js/ui/page-product.js` | 只停售，不删除商品、不动库存、不碰单据，可恢复；有库存的另行提示盘点 |
| 33.5 | **合并时库存转入**：规则4 合并保留时，其余同型号商品名下的库存经盘点调整单转入保留商品，总数守恒且留痕 | `js/core/product.js` | 停售商品库存归零、保留商品累加，生成盘点调整单 |
| 33.6 | **版本号统一 V3.25 + 全量回归**：PRD / 开发计划 / page-mine.js 关于页统一 V3.25（SW v52） | 全量 | 关于页 V3.25；全绿；等待部署指令 |

- **阶段测试**：新增 `tests/product-import-preview.test.js`（11 个用例，预演字段对比/不写库/取消）、`tests/product-import-uncovered.test.js`（6 个用例，未覆盖清单）、`tests/product-import-retire.test.js`（8 个用例，一键停售安全语义）、`tests/product-import-stock-transfer.test.js`（库存转入守恒与留痕）；同步更新 `product-import-rules.test.js` 中「库存不转移」的旧断言（更新而非删除）。全量 **526/526** 通过。
- **提交点**：`2cdf22a`（预演体检）、`40786d6`（未覆盖清单）、`5a9a134`（库存转入 + 一键停售）；最终 SW 缓存版本 **v52**（尚未部署，等待部署指令）。
- **使用口径**：品牌/成本/价格标错但**型号正确** → 直接导入即可批量修正（预演可逐字段核对）；**型号本身标错** → 导入会新建正确商品，旧商品进入未覆盖清单，用一键停售处理。

---

## 六·三十四、阶段 34：单据明细模块移到列表上方（V3.26）

**目标**：进货管理 / 销售管理页面，点列表中的「查看」后，单据明细直接显示在列表上方，无需下滚。

### 任务清单

| # | 任务 | 涉及文件 | 验收点 |
| --- | --- | --- | --- |
| 34.1 | **进货明细上移**：renderList 中 `docModal` 由列表末尾移到列表之前（空列表早退之前渲染，保证任何情况下都能显示） | `js/ui/page-purchase.js` | 点「查看」后明细在列表上方；未点查看时不渲染 |
| 34.2 | **销售明细上移**：同上；退货弹窗 `refundModal` 位置保持不变 | `js/ui/page-sale.js` | 销售明细在列表上方；退货弹窗仍在列表下方 |
| 34.3 | **记账备注截断**：流水列表备注最多 30 字，超出以 `...` 截断并渲染为可点击链接 | `js/ui/page-account.js` | 30 字内原样显示；超长显示前 30 字 + `...` |
| 34.4 | **完整信息弹窗**：新增 `state.viewNoteId` 与 `view-note` / `close-note`，点击截断备注弹出该流水完整信息（日期/类型/金额/往来单位/单据号/完整备注，保留换行） | `js/ui/page-account.js` | 点击可见完整备注；关闭后列表恢复截断显示 |
| 34.5 | **版本号统一 V3.26 + 全量回归**：PRD / 开发计划 / page-mine.js 关于页统一 V3.26（SW v53） | 全量 | 关于页 V3.26；全绿；等待部署指令 |

- **阶段测试**：新增 `tests/doc-detail-position.test.js` 共 5 个用例（进货明细在列表上方、未点查看不渲染、销售明细在列表上方、退货弹窗不受影响、明细卡按钮完整）、`tests/account-note-clip.test.js` 共 7 个用例（30 字内不截断、超长截断、截断可点击、弹窗含全部字段、关闭恢复、恰好 30 字不截断、空备注无 undefined）。全量 **538/538** 通过。
- **提交点**：`29333b5`（单据明细模块移到列表上方）、`045f12c`（备注截断与完整查看）、`2489007`（文档）；最终 SW 缓存版本 **v53**（已部署上线）。

---

## 六·三十五、阶段 35：批量导入操作按钮同排靠右（V3.27）

**目标**：批量导入的体检报告很长（新增/更新/合并/未覆盖/错误多张表），原「取消」「确认执行导入」在报告末尾需一路下滚；改为与「预演体检（不写入）」同排、靠最右端。

### 任务清单

| # | 任务 | 涉及文件 | 验收点 |
| --- | --- | --- | --- |
| 35.1 | **按钮同排**：`取消` / `确认执行导入` 移入 `预演体检（不写入）` 所在的 `import-actions` 行 | `js/ui/page-product.js` | 三者同排，顺序 预演体检 → 取消 → 确认执行导入 |
| 35.2 | **最右端**：按钮组位于该行最右端（spacer 在左侧撑开），确认执行导入为最后一个 | `js/ui/page-product.js` | 确认执行导入在该行最后 |
| 35.3 | **条件显示**：仅在已生成体检报告（`state.csvPlan`）时出现 取消 / 确认执行导入，未预演时不显示 | `js/ui/page-product.js` | 未预演无这两个按钮，避免误触 |
| 35.4 | **移除重复**：报告末尾不再重复渲染按钮行，全局只渲染一次 | `js/ui/page-product.js` | `do-import` 全局仅出现一次 |
| 35.5 | **窄屏适配**：新增 `.import-actions`（`flex-wrap: wrap` + `justify-content: flex-end`），窄屏自动换行且仍靠右 | `css/base.css` | 手机端不挤爆，换行后仍右对齐 |
| 35.6 | **版本号统一 V3.27 + 全量回归**：PRD / 开发计划 / page-mine.js 关于页统一 V3.27（SW v54） | 全量 | 关于页 V3.27；全绿；等待部署指令 |

- **阶段测试**：新增 `tests/import-actions-layout.test.js` 共 9 个用例（三按钮同排相邻、同一 `import-actions` 行容器内、确认执行导入在最右端、`do-import` 仅渲染一次、未预演不显示、取消按钮 btn-danger、spacer 在按钮组左侧、报告内容不受影响、`.import-actions` 样式已定义）。全量 **547/547** 通过。
- **提交点**：`6b4367a`（操作按钮同排靠右）；最终 SW 缓存版本 **v54**（尚未部署，等待部署指令）。

---

## 六·三十六、阶段 36：扫码识别深度修复（V3.28）

**目标**：解决手机端（华为鸿蒙 / Android）扫码"取景框识别不了条码/二维码"问题——拍照识别成功率低、第二次打开取景纯黑、识别空转无反馈。

### 问题根因（诊断结论）

1. **拍照识别绕过原生解码**：`decodeImage` 直接走 ZXing 纯 JS 解码，未使用设备原生 BarcodeDetector（Android/鸿蒙识别率远高于 ZXing），反光条码必失败 → 报"识别失败，请重试或手输"。
2. **摄像头流未统一释放**：实时取景仅"识别成功 / 手输"两条路径停流；点「取消」或弹窗关闭时直接 closeModal，摄像头流被占用 → 第二次打开取景纯黑。
3. **识别空转无兜底**：detect 返回空或抛异常均被静默吞掉，无超时降级、无用户反馈。

### 任务清单

| # | 任务 | 涉及文件 | 验收点 |
| --- | --- | --- | --- |
| 36.1 | **拍照识别原生解码优先**：`decodeImage` 按 `pickDecoders` 顺序尝试 ① 原生 BarcodeDetector 解图片 → ② ZXing 兜底 → ③ 清晰报错 | `js/barcode/scan.js` | 有 BarcodeDetector 时拍照优先原生解码 |
| 36.2 | **摄像头流统一释放**：modal 增加 `onClose` 钩子（`currentModalOpts` 记录被关闭 modal），任何关闭路径（取消/X/外部 closeModal）触发 `scan.closeCamera` stop 全部轨道 | `js/ui/components.js` `js/barcode/scan.js` | 所有关闭路径均释放流；`closeCamera` 幂等 |
| 36.3 | **识别超时自动降级**：`needDowngrade` 判定——空转 60 帧或自首次空转 12 秒 → 自动切「拍照识别 / 手输」并提示 | `js/barcode/scan.js` | 12 秒无结果自动降级 |
| 36.4 | **detect 异常自动降级**：连续异常 5 次 → 判定设备实时识别不可用，自动降级 | `js/barcode/scan.js` | 异常 5 次降级 |
| 36.5 | **黑屏检测**：摄像头启动后 3 秒无实际画面帧（videoWidth=0）→ 提示并降级 | `js/barcode/scan.js` | 黑屏自动提示降级 |
| 36.6 | **取景引导 + 拍照入口**：实时画面提示「对准取景框、保持平稳、避免反光」；新增常驻「📷 拍照识别」按钮 | `js/barcode/scan.js` | 引导文案与拍照按钮可见 |
| 36.7 | **版本号统一 V3.28 + 全量回归**：PRD / 开发计划 / page-mine.js 关于页统一 V3.28（SW v55） | 全量 | 关于页 V3.28；全绿；等待部署指令 |

- **阶段测试**：新增 `tests/scan-realtime.test.js` 共 9 个用例（buildFormats 码制列表、pickDecoders 解码优先级、needDowngrade 空转/异常/12秒超时判定、closeCamera 流释放与幂等）。全量 **556/556** 通过。
- **提交点**：`198c083`（扫码识别修复）；最终 SW 缓存版本 **v55**（已部署上线 V3.28）。

---

## 六·三十七、阶段 37：扫码黑屏回归修复（V3.29）

**目标**：修复 V3.28 引入的手机端扫码回归——点击扫描后取景框完全黑、弹出「当前设备无法实时识别」错误；拍照识别报「未识别到条码/二维码」。

### 问题根因

V3.28 给 `getUserMedia` 新增 `width/height: { ideal: 1280×720 }` 分辨率约束，部分鸿蒙/Android WebView 对带约束的流无法渲染到 video 元素（无帧，videoWidth=0）：
- `detect(video)` 对无帧视频持续抛异常 → 异常计数 5 次（不足 1 秒）即触发「当前设备无法实时识别」降级
- 异常降级早于 3 秒黑屏检测，黑屏检测被抢跑 → 取景框黑屏 + 误导性报错

### 任务清单

| # | 任务 | 涉及文件 | 验收点 |
| --- | --- | --- | --- |
| 37.1 | **移除分辨率约束**：getUserMedia 回到 `{ video: { facingMode: 'environment' } }`（V3.27 同款最兼容配置） | `js/barcode/scan.js` | 无 width/height 约束 |
| 37.2 | **黑屏检测优先**：`isBlackOut(videoWidth, startedAt, now)`——2.5 秒无帧判定黑屏，优先级高于异常降级 | `js/barcode/scan.js` | 黑屏 2.5 秒触发降级 |
| 37.3 | **异常仅画面正常时计数**：`shouldCountError(videoWidth)`——黑屏期间 detect 异常不计数，交给黑屏检测，不再抢跑 | `js/barcode/scan.js` | 黑屏异常不触发「无法实时识别」 |
| 37.4 | **play() 失败处理**：`video.play()` Promise 失败 → 降级「摄像头启动失败」 | `js/barcode/scan.js` | play 失败有提示降级 |
| 37.5 | **拍照识别放大重试**：原生解码失败后画布放大 2 倍重试（反光/小条码识别率提升）；报错文案优化 | `js/barcode/scan.js` | 有放大重试路径 |
| 37.6 | **版本号统一 V3.29 + 全量回归**：PRD / 开发计划 / page-mine.js 关于页统一 V3.29（SW v56） | 全量 | 关于页 V3.29；全绿；等待部署指令 |

- **阶段测试**：`tests/scan-realtime.test.js` 新增 `isBlackOut`（无帧满 2.5 秒降级 / 有帧不降级 / 未到 2.5 秒不降级）与 `shouldCountError`（黑屏异常不计数 / 画面正常计数）2 项，共 11 项。全量 **558/558** 通过。
- **提交点**：`d300c0d`（扫码黑屏回归修复）；最终 SW 缓存版本 **v56**（已部署上线 V3.29）。

---

## 六·三十八、阶段 38：扫码多通道解码重构（V3.30）

**目标**：解决鸿蒙/部分国产 WebView 的 BarcodeDetector「半实现」问题——标准 EAN-13 条码在实时取景与拍照识别均失败、取景器一闪而过。

### 问题根因

鸿蒙/部分国产浏览器 WebView 的 BarcodeDetector 可能是「半实现」：`window.BarcodeDetector` 与 `getSupportedFormats()` 存在，但 `detect(video)` 不可用/检测不可靠（返回空或抛异常）。用户以白底清晰的标准 EAN-13 条码（5012345678900）实测：实时取景识别失败、拍照识别（原生+ZXing）也失败。「取景器一闪而过」= detect(video) 连续异常 5 次（不足 1 秒）触发降级关闭。

### 任务清单

| # | 任务 | 涉及文件 | 验收点 |
| --- | --- | --- | --- |
| 38.1 | **detect 异常自动切抓帧**：`shouldSwitchFrame(mode, errorFrames)`——实时模式 detect(video) 连续异常 3 次 → 切「抓帧识别」模式，画面保留不关闭 | `js/barcode/scan.js` | 3 次异常切帧，画面不闪退 |
| 38.2 | **抓帧节流**：`frameDue(lastFrameAt, now, interval)`——每 500ms 从 video 抓一帧到 canvas，静态帧多通道解码 | `js/barcode/scan.js` | 500ms 节流抓帧 |
| 38.3 | **统一多通道解码**：`decodeWith(source, done)`——① 原生 BarcodeDetector.detect(source) → ② ZXing（Image 元素 decodeFromImageElement / canvas dataURL decodeFromImageUrl），图片源支持 Image/canvas | `js/barcode/scan.js` | 双通道串行，任一成功即返回 |
| 38.4 | **拍照识别重构**：Image 加载 → decodeWith 多通道 → 画布放大 2 倍重试 → 清晰引导报错 | `js/barcode/scan.js` | 拍照多通道 + 放大重试 |
| 38.5 | **版本号统一 V3.30 + 全量回归**：PRD / 开发计划 / page-mine.js 关于页统一 V3.30（SW v57） | 全量 | 关于页 V3.30；全绿；等待部署指令 |

- **阶段测试**：`tests/scan-realtime.test.js` 新增 `shouldSwitchFrame`（3 次异常切帧 / 已在抓帧不重复切）与 `frameDue`（500ms 节流 / 首帧立即抓）2 项，共 13 项。全量 **560/560** 通过。
- **提交点**：`fc73f36`（多通道解码重构）；最终 SW 缓存版本 **v57**（已部署上线 V3.30）。

---

## 六·三十九、阶段 39：扫码解码链路加固（V3.31）

**目标**：解决华为/鸿蒙自带浏览器上标准 EAN-13 条码仍无法识别、拍照 5-10 秒才报错的问题。

### 问题根因（用户实测：华为 Mate 自带浏览器，条码为白底标准 EAN-13 5012345678900）

1. **native 通道饿死 ZXing**：华为自带浏览器 `BarcodeDetector.detect()` 可能挂起不返回或耗时 5-10 秒，`decodeWith` 卡死在 native 通道，ZXing 兜底永远轮不到——这是「拍照 5-10 秒才报错」的直接原因。
2. **ZXing 解码大图极慢**：手机拍照原图可达 4000 万像素级，ZXing 纯 JS 逐行扫描需要 5-10 秒以上。

### 任务清单

| # | 任务 | 涉及文件 | 验收点 |
| --- | --- | --- | --- |
| 39.1 | **native 超时保护**：`scan.decodeWith` 导出并可注入实现（可测试）；native 通道 2500ms 超时强制跳过，交给 ZXing 兜底 | `js/barcode/scan.js` | native 挂起/超慢不阻塞，ZXing 兜底执行 |
| 39.2 | **解码前压缩**：`toDecodeCanvas(source)` 统一转 canvas 且长边 ≤1280（只缩不放）；native 与 ZXing 均吃压缩小图 | `js/barcode/scan.js` | 大图解码从 5-10 秒降至 <1 秒 |
| 39.3 | **中心区域放大重试**：`zoomCenterCanvas(img, 2)` 替代全图放大（条码占画面比例小时有效） | `js/barcode/scan.js` | 拍照失败后中心放大 2 倍再试 |
| 39.4 | **版本号统一 V3.31 + 全量回归**：PRD / 开发计划 / page-mine.js 关于页统一 V3.31（SW v58） | 全量 | 关于页 V3.31；全绿；等待部署指令 |

- **阶段测试**：`tests/scan-realtime.test.js` 新增 `decodeWith` 注入实现测试 6 项（native 挂起超时→ZXing 兜底成功 / native 快速失败→ZXing 兜底成功 / native 快速成功不等 ZXing / 双通道失败 / native 不可用仅走 ZXing / 均不可用不抛错），共 19 项。全量 **566/566** 通过。
- **提交点**：`566550d`（解码链路加固）；最终 SW 缓存版本 **v58**（待部署）。

---

## 六·四十、阶段 40：扫码识别套件自研化（V3.32）

**目标**：官方 ZXing 一维解码链路打包损坏（`decodeDigit` 内 `this.recordPattern` undefined），标准 EAN-13 在华为/鸿蒙自带浏览器完全扫不出；改用零依赖自研解码器接管主通道。

### 任务清单

| # | 任务 | 涉及文件 | 验收点 |
| --- | --- | --- | --- |
| 40.1 | 自研轻量 EAN-13 解码器：4-run 比例匹配（不依赖绝对模块宽）+ 起止符/分隔符结构校验 + 首位数字奇偶枚举（10 种 PARITY）+ 校验位过滤 + 多行投票（最高票 ≥3 且 ≥30% 扫描行，防噪声误报） | `js/barcode/ean13.js` | 完美条码 / 模糊噪声 / 全白全黑 / 防误报用例全通过，Node 可测 |
| 40.2 | 解码顺序改为 **native → 自研 EAN-13 → ZXing 兜底**，并修复 `bestRes` 比较属性笔误 | `js/barcode/scan.js` | 偏差最小的组合才能胜出，不再被错误分支抢占 |
| 40.3 | 实时取景 `detect(video)` 挂起（鸿蒙/安卓半实现）新增 **2500ms 超时保护**，超时自动切抓帧多通道解码 | `js/barcode/scan.js` | 取景不再「无识别操作」干等，自动降级到抓帧 |

- **阶段测试**：新增 `tests/scan-ean13.test.js` 11 项（完美条码/另一合法条码/模糊+噪声模拟相机帧/防误报/全白全黑/空输入/单行/外部行/多行投票/码表结构），`scan-realtime` 增 ean13 通道 5 项；全量 **582/582** 通过。
- **提交点**：`eddb50966`（统一版本号 V3.32）；SW 缓存 **v59**。

---

## 六·四十一、阶段 41：扫码失效根治（V3.33）

**目标**：用户实测 V3.32「与以前无异」——两个根因：① 用户浏览器从没运行过新代码；② ZXing 兜底通道本身是坏的。

### 任务清单

| # | 任务 | 涉及文件 | 验收点 |
| --- | --- | --- | --- |
| 41.1 | 导航请求改为 **network-first**（在线一律取最新页面，离线才回退缓存），SHELL 缓存列表补 `ean13.js` | `sw.js` | 用户下次打开即新版，不再拿到旧 index.html |
| 41.2 | 用 esbuild 从官方 **ESM 源码重打包干净 ZXing**（`vendor/zxing-decode.min.js`）替代损坏 UMD；zxing 通道改用 `decodeCanvas` | `vendor/`、`js/barcode/scan.js` | 不再走 dataURL + BrowserCodeReader 旧 API |
| 41.3 | 真实相机帧模拟验证（旋转 5° + 高斯模糊 + 噪声 + 光照渐变）：自研 EAN-13 稳定解出（140/225 行），干净 ZXing 反而失败 → **自研保持主通道** | `js/barcode/ean13.js` | 0° 失败时自动 ±4° 旋转重试，手持倾斜可兜底 |

- **阶段测试**：`scan-ean13` 增不均匀光照渐变用例（专项 12 项）；全量 **583/583** 通过。
- **提交点**：`965fe0ef3`（统一版本号 V3.33）；SW 缓存 **v60**。

---

## 六·四十二、阶段 42：扫码场景打通（V3.34）

**目标**：解码能力修好后，补齐各业务入口的扫码调用。

### 任务清单

| # | 任务 | 涉及文件 | 验收点 |
| --- | --- | --- | --- |
| 42.1 | 销售开单「去开单」修复（扫码定位商品后正确进入开单流程） | `js/ui/page-sale.js` | 扫码后可直接加行进单 |
| 42.2 | 新建进货单「选货区」支持扫码加行 | `js/ui/page-purchase.js` | 选货区扫码命中商品即加行 |
| 42.3 | 新建商品时「条码」字段支持扫码填写 | `js/ui/page-product.js` | 扫机身码自动填入条码输入框 |

- **阶段测试**：全量 **590/590** 通过。
- **提交点**：`794b302c2`（统一版本号 V3.34）；SW 缓存 **v61**。

---

## 六·四十三、阶段 43：扫码功能增强（V3.35）

**目标**：提升实际扫码命中率——补码制规范化与变形容错。

### 任务清单

| # | 任务 | 涉及文件 | 验收点 |
| --- | --- | --- | --- |
| 43.1 | UPC-A 12 位补 0 规范化 | `js/barcode/scan.js` | 12 位条码按 EAN-13 正确解析 |
| 43.2 | 解码出口统一规范化（去空格/全半角/前后缀） | `js/barcode/scan.js` | 同一条码各通道输出一致 |
| 43.3 | `resolve` 双向变体匹配（带前导 0 / 去前导 0 互相兜底） | `js/core/product.js` | 档案里存的变体也能命中 |
| 43.4 | 竖排 90°/270° 转置重试 | `js/barcode/scan.js` | 竖着贴的条码可识别 |

- **阶段测试**：全量 **599/599** 通过。
- **提交点**：`4ff088fac`（统一版本号 V3.35）；SW 缓存 **v63**（v62 未落地，跳号）。

---

## 六·四十四、阶段 44：公司内部自定义条码扫码识别（V3.36）

**目标**：公司内部自贴条码不是标准码制、位数不定，原方案一律识别失败 → 自研 Code39 解码通道。

### 任务清单

| # | 任务 | 涉及文件 | 验收点 |
| --- | --- | --- | --- |
| 44.1 | 自研 Code39 解码器：44 符号码表 / 宽窄比自适应 / 无校验容错 / 多行投票 | `js/barcode/generic39.js` | 位数不定的内部码可解 |
| 44.2 | 解码顺序调整为 **native → ean13 → code39 → zxing** | `js/barcode/scan.js` | 标准码不被内部码通道误截 |

- **阶段测试**：全量 **611/611** 通过。
- **提交点**：`dffe03a1f`（统一版本号 V3.36）；SW 缓存 **v64**。

---

## 六·四十五、阶段 45：商品档案多选删除未使用档案（V3.37）

**目标**：批量清理建错的/不再使用的商品档案。

### 任务清单

| # | 任务 | 涉及文件 | 验收点 |
| --- | --- | --- | --- |
| 45.1 | 表头全选 + 行勾选 + 「删除选中」按钮 | `js/ui/page-product.js` | 勾选集合与计数实时一致 |
| 45.2 | `product.removeUnused` 引用保护：有单据/库存引用的档案拒绝删除 | `js/core/product.js` | 已发生业务的档案删不掉，不产生脏引用 |
| 45.3 | flush 支持 `__deleted` 物理删除 | `js/store/repo.js` | 删除真正落到 IndexedDB，刷新不复现 |

- **阶段测试**：全量 **624/624** 通过。
- **提交点**：`e7c9870e9`（统一版本号 V3.37）；SW 缓存 **v65**。

---

## 六·四十六、阶段 46：手机版取消商品档案删除入口（V3.38）

**目标**：手机端误删风险高，删除只在网页版保留。

### 任务清单

| # | 任务 | 涉及文件 | 验收点 |
| --- | --- | --- | --- |
| 46.1 | 删除按钮加 `desktop-only` 标记，≤599px 隐藏 | `js/ui/page-product.js`、`css/mobile.css` | 手机端无删除入口，网页版正常 |

- **阶段测试**：全量 **627/627** 通过。
- **提交点**：`61b8c036d`（删除功能仅保留网页版）；SW 缓存 **v66**。

---

## 六·四十七、阶段 47：「加入」「选为原单」按钮统一橘色（V3.39）

**目标**：选货区主操作按钮配色不统一，且 `.act` 链接样式会覆盖 `.btn`。

### 任务清单

| # | 任务 | 涉及文件 | 验收点 |
| --- | --- | --- | --- |
| 47.1 | 4 处按钮改 `btn-orange`（#f97316） | `js/ui/page-sale.js`、`page-purchase.js`、`page-exchange.js` | 三页选货按钮同色 |
| 47.2 | 修复 `.act` 链接样式覆盖 `.btn` 导致配色失效 | `css/base.css` | 按钮色不被链接规则吃掉 |

- **阶段测试**：全量 **632/632** 通过。
- **提交点**：`b790e90e2`（按钮统一橘色配色）；SW 缓存 **v67**。

---

## 六·四十八、阶段 48：商品多选删除勾选同步修复（V3.40）

**目标**：搜索/筛选/翻页/扫码后勾选状态与实际列表对不上，删除条数出错。

### 任务清单

| # | 任务 | 涉及文件 | 验收点 |
| --- | --- | --- | --- |
| 48.1 | 搜索 / 筛选 / 翻页 / 扫码时清空勾选 | `js/ui/page-product.js` | 勾选集合与当前列表一致 |
| 48.2 | 删除计数实时一致 | `js/ui/page-product.js` | 按钮计数 = 实际勾选项数 |

- **阶段测试**：全量 **638/638** 通过。
- **提交点**：`e8a739005`（勾选同步修复）；SW 缓存 **v68**。

---

## 六·四十九、阶段 49：全选与列表渲染统一分页管道（V3.41）

**目标**：大库存下「全选」会勾到不在当前页的商品——根因是全选集合与列表渲染各算一套过滤/排序/分页。

### 任务清单

| # | 任务 | 涉及文件 | 验收点 |
| --- | --- | --- | --- |
| 49.1 | 抽出 `computePage`：**过滤 → 排序 → 分页** 全选与渲染共用同一管道 | `js/ui/page-product.js` | 全选只影响当前筛选结果，不再勾错集合 |

- **阶段测试**：全量 **640/640** 通过。
- **提交点**：`bf3a8297a`（全选与列表渲染一致）；SW 缓存 **v69**。

---

## 六·五十、阶段 50：商品档案同型号信息合并（V3.42）

**目标**：同一型号被重复建档（库存分散、报表失真）→ 提供「按型号合并」。

### 任务清单

| # | 任务 | 涉及文件 | 验收点 |
| --- | --- | --- | --- |
| 50.1 | `product.mergeByModel`：仅型号相同可合并；主档取库存最大者；库存相加、备注拼接、条码合并；保留主档成本/品牌/价格/状态；副档 `__deleted` 物理删除；停售商品也可合并 | `js/core/product.js` | 合并后库存 = 各档之和，副档消失 |
| 50.2 | UI 合并按钮 `desktop-only`（手机版隐藏）+ `merge-selected` 二次确认流程 | `js/ui/page-product.js` | 仅网页版可见，误触有确认 |
| 50.3 | 修复超期测试日期漂移（断言依赖当前日期导致隔天失败） | `tests/` | 测试不再随时间失败 |

- **阶段测试**：新增 15 项；全量 **654/654** 通过。
- **提交点**：`6fcfeb66b`（同型号信息合并，仅网页版）；SW 缓存 **v70**。

---

## 六·五十一、阶段 51：多选勾选局部刷新（V3.43）

**目标**：长列表里勾选一次就整页重渲染 → 滚动位置跳顶。

### 任务清单

| # | 任务 | 涉及文件 | 验收点 |
| --- | --- | --- | --- |
| 51.1 | `refreshSelUI` 原地更新：按钮计数 / 禁用态 / 表头全选 / 当前页行勾选，**不再整页重渲染** | `js/ui/page-product.js` | 长列表多选滚动位置不跳顶 |

- **阶段测试**：新增 3 项；全量 **657/657** 通过。
- **提交点**：`457b5fb76`（多选勾选局部刷新）；SW 缓存 **v71**。

---

## 六·五十二、阶段 52：修复 V3.43 勾选失效回归（V3.44）

**目标**：V3.43 上线后勾选点不动——根因是 `change` 事件被第三方框架拦截，到不了 document 的 change 委托。

### 任务清单

| # | 任务 | 涉及文件 | 验收点 |
| --- | --- | --- | --- |
| 52.1 | 行勾选 / 表头全选改用 **click 委托** `data-act`，去掉 `data-change` | `js/ui/page-product.js` | 勾选恢复可用，且不再依赖 change 冒泡 |

- **阶段测试**：新增 3 项；全量 **660/660** 通过。
- **提交点**：`ff91a5e4e`（勾选改成 click 委托）；SW 缓存 **v72**。

---

## 六·五十三、阶段 53：商品档案「导出全部」CSV（V3.45）

**目标**：批量导入前先导出全部档案给外部核实，核实后按型号重新导入。

### 任务清单

| # | 任务 | 涉及文件 | 验收点 |
| --- | --- | --- | --- |
| 53.1 | 导出全部档案 CSV：品牌/型号/类型/单位/成本/批发/零售/库存/备注/条码/状态；金额按**元**导出；多条码用分号连接 | `js/ui/page-product.js` | 导出的文件可直接作为导入模板 |

- **阶段测试**：新增 5 项；全量 **666/666** 通过。
- **提交点**：`effb8ee5a`（新增导出全部 CSV）；SW 缓存 **v73**。

---

## 六·五十四、阶段 54：清空库存与档案（保留单据）（V3.46）

**目标**：换一批货/重开账时，只想清商品与库存，不想丢单据。

### 任务清单

| # | 任务 | 涉及文件 | 验收点 |
| --- | --- | --- | --- |
| 54.1 | 设置页数据管理新增按钮：仅清 `products` / `stockLogs` / `stocktakes` 三表，保留全部单据、记账、客户、日志、设置 | `js/ui/page-setting.js` | 清完后单据与账目完整 |
| 54.2 | 二次确认 + 写操作日志 | `js/ui/page-setting.js` | 误触有拦截，操作可追溯 |

- **阶段测试**：新增 4 项；全量 **669/669** 通过。
- **提交点**：`551e9f5c1`（清空库存与档案，保留单据）；SW 缓存 **v74**。

---

## 六·五十五、阶段 55：勾选不再跳回顶部（V3.47）

**目标**：框架 `afterAction` 无条件整页重渲染 + 滚动到顶，勾选一次就跳顶。

### 任务清单

| # | 任务 | 涉及文件 | 验收点 |
| --- | --- | --- | --- |
| 55.1 | `row-check` / `toggle-all-check` 显式 `return false` 跳过框架重渲染 | `js/ui/page-product.js` | 长列表勾选滚动位置保持 |

- **阶段测试**：新增 2 项；全量 **671/671** 通过。
- **提交点**：`99fd69228`（复选框跳顶修复与部署）；SW 缓存 **v75**。

---

## 六·五十六、阶段 56：根治全页面内操作跳顶 + 搜索框一键清除（V3.48）

**目标**：V3.47 只是单点修补；框架层 `render()` 无条件 `scrollTo(0,0)` 让**任何**同路由重渲染都跳顶。顺带解决搜索框要逐字删除。

### 任务清单

| # | 任务 | 涉及文件 | 验收点 |
| --- | --- | --- | --- |
| 56.1 | `app.js` 的 `render()` 新增路由变化检测：同路由重渲染保持滚动位置，仅换页才回顶 | `js/app.js` | 销售开单/进货单/商品档案等页内操作不跳顶 |
| 56.2 | 全系统搜索框改 `type="search"` 启用原生清除按钮；盘点、进货选货区等补齐 `data-live="1" data-debounce="1"` | `js/ui/components.js` 及各页面 | 点击清除后列表即时刷新 |
| 56.3 | `base.css` 显式定义 `input[type=search]` 与 `::-webkit-search-cancel-button` | `css/base.css` | 清除按钮各浏览器可见 |

- **阶段测试**：新增 8 项（searchBar type=search + CSS、进货选货区、盘点、换货搜索、render 滚动保持）；全量 **680/680** 通过。
- **提交点**：`9c069b0f4`（V3.48 部署记录）；SW 缓存 **v76**。

---

## 六·五十七、阶段 57：横向滚动容器保持 scrollLeft（V3.49）

**目标**：手机端选货区左右滑动后点「加入」会跳回最左——V3.48 只恢复了 window 级滚动。

### 任务清单

| # | 任务 | 涉及文件 | 验收点 |
| --- | --- | --- | --- |
| 57.1 | `render()` 同路由重渲染前记录各 `.table-wrap` 的 `scrollLeft`，渲染后按序写回（`app._savedHScroll`） | `js/app.js` | 手机端选货区、当前订单列表横向位置保持 |
| 57.2 | 仅同路由恢复（换页不带旧滚动） | `js/app.js` | 换页行为不变 |

- **阶段测试**：新增 3 项；全量 **683/683** 通过。
- **提交点**：`a79d7e35a`（V3.49 推送部署）；SW 缓存 **v77**。

---

## 六·五十八、阶段 58：选货区两行卡片式排版（V3.50）

**目标**：手机端选货区横向表格必须左右滑才能看全型号与价格。

### 任务清单

| # | 任务 | 涉及文件 | 验收点 |
| --- | --- | --- | --- |
| 58.1 | 销售开单 / 新建进货单按商品加行 / 退换货换货 三处选货区统一改两行卡片：第一行品牌+型号，第二行类型/单位 + `批:¥xx / 零:¥xx`（进货为 `成本:¥xx`）；右侧库存与加入按钮大小位置不变 | `js/ui/page-sale.js`、`page-purchase.js`、`page-exchange.js` | 手机普通宽度即可容纳，无需横向滚动 |
| 58.2 | `base.css` 新增 `.pick-list` / `.pick-item` / `.pick-sub` / `.pick-stock`（含 `.pick-stock.low` 红色） | `css/base.css` | 卡片结构与低库存样式统一 |

- **阶段测试**：新增 4 项；全量 **687/687** 通过。
- **提交点**：`06406dab3`（V3.50 部署记录）；SW 缓存 **v79**；回滚基线 tag：**v3.49-baseline**。

---

## 六·五十九、阶段 59：手机端选货区右侧排版优化（V3.51）

**目标**：手机端「加入」按钮横排文字太宽，挤压型号显示。

### 任务清单

| # | 任务 | 涉及文件 | 验收点 |
| --- | --- | --- | --- |
| 59.1 | `@media max-width:599px` 内 `.pick-side` 改 `flex-direction:row` 让库存数字前置到按钮左侧 | `css/mobile.css` | 库存先于按钮显示 |
| 59.2 | 「加入」改竖排文字：`writing-mode:vertical-rl` + `text-orientation:upright`；按钮 30×45px（宽收 30%、高增 50%），padding:0、line-height:1 | `css/mobile.css` | 按钮排布位置不变、不再撑宽；电脑端 ≥600px 不受影响 |

- **阶段测试**：新增 1 项；全量 **688/688** 通过。
- **提交点**：`7d0857d40`（V3.51 部署记录）；SW 缓存 **v80**。

---

## 六·六十、阶段 60：选货区双布局切换（V3.52）

**目标**：澄清需求——V3.50/51 的卡片式**只应作用于手机端**，电脑端要恢复 V3.49 的横向表格。

### 任务清单

| # | 任务 | 涉及文件 | 验收点 |
| --- | --- | --- | --- |
| 60.1 | JS 同时渲染两套：`pick-desktop` 包装 V3.49 五列 `tbl-striped` 表格 + `pick-mobile` 包装 V3.51 两行卡片 | `js/ui/page-sale.js`、`page-purchase.js`、`page-exchange.js` | 两种布局 DOM 都在，由 CSS 决定显示 |
| 60.2 | CSS-only 响应式切换：`base.css` 默认显示 desktop 隐藏 mobile；`mobile.css` ≤599px 反转 | `css/base.css`、`css/mobile.css` | 切换不触发 JS 重渲染，无闪烁 |

- **阶段测试**：新增 2 项 + 重写 3 项 V3.50 选货区断言为双布局断言（ui-sale / ui-purchase 行数 15→30）；全量 **690/690** 通过。
- **提交点**：`6eef5d91f`（V3.52 部署记录）；SW 缓存 **v81**；回滚基线 tag：**v3.51-baseline**。

---

## 六·六十一、阶段 61：取消「加入」变「＋再加」（V3.53）

**目标**：商品已在明细时按钮文案变成「＋ 再加」，用户认为毫无意义（数量应在明细列表里改），且更长文案撑宽按钮破坏页面。

### 任务清单

| # | 任务 | 涉及文件 | 验收点 |
| --- | --- | --- | --- |
| 61.1 | 删除 `hasD` / `has` 两个 `state.form.items.some(...)` 判断分支，桌面表格与手机卡片的选货按钮**一律固定显示「加入」** | `js/ui/page-purchase.js` | 按钮宽度稳定不抖动 |

- **阶段测试**：`tests/pick-list.test.js` 新增 1 项（先把商品加入 `state.form.items` 模拟已加入，断言渲染不含「＋再加」且按钮数保持 4 = 2 款商品 × 双布局）；全量 **691/691** 通过。
- **提交点**：`6292ff77f`（V3.53 部署记录）；SW 缓存 **v82**；回滚基线 tag：**v3.52-baseline**。

---

## 六·六十二、阶段 62：进货明细宽表格撑破整页 + 缓存新鲜度（V3.54）

**目标**：① 新建进货单页面被宽表格撑破（经典 Grid 溢出）；② 手机端仍显示已删除的「＋再加」（GitHub Pages 缓存）。

### 任务清单

| # | 任务 | 涉及文件 | 验收点 |
| --- | --- | --- | --- |
| 62.1 | `.purchase-form-grid` 子项 `min-width:auto` 被 `table.tbl td white-space:nowrap` 撑到 min-content → 整页被撑宽；改 `minmax(0,1fr)` + 子卡片 `min-width:0`，宽表格只在 `.table-wrap` 内横向滚动 | `css/desktop.css`、`css/mobile.css` | 页面不再横向溢出，表格内部滚动 |
| 62.2 | 防御性补 `.card{min-width:0}`、`.pick-name{overflow-wrap:anywhere}`、手机端 `.app-main{overflow-x:clip}` | `css/base.css`、`css/mobile.css` | 同类溢出不再发生 |
| 62.3 | GitHub Pages `Cache-Control:max-age=600` 让 network-first 的 `fetch(req)` 10 分钟内命中 HTTP 缓存返回旧 JS；改 `fetch(req,{cache:'no-cache'})` 强制重新校验 | `sw.js` | 新版本即时生效 |
| 62.4 | 「我的 → 关于」新增「🔄 检查更新」：清空 SW 缓存 + `registration.update()` + 重新加载，作为旧缓存兜底自救入口 | `js/ui/page-mine.js` | 手机端一键自救 |

- **阶段测试**：新增 3 项 + 更新 3 项；全量 **694/694** 通过（Chromium 390px 实测）。
- **提交点**：`cb5a9b549`（V3.54 部署记录）；SW 缓存 **v83**；回滚基线 tag：**v3.53-baseline**。

---

## 六·六十三、阶段 63：全系统商品列表斑马纹统一 + 「已加入」行高亮（V3.55）

**目标**：① 手机端选货卡片完全没有斑马纹；② 已加入的商品看不出来，容易重复加。

### 任务清单

| # | 任务 | 涉及文件 | 验收点 |
| --- | --- | --- | --- |
| 63.1 | 抽 `--c-stripe:#f6f7f9` 变量供表格与卡片共用；新增 `.pick-item:nth-child(even)` 交替底色；`.pick-item` padding 改 `10px 8px` 让色带铺满 | `css/base.css` | 手机端选货卡片逐行交替（表格侧 6 页本来已由全局规则覆盖） |
| 63.2 | 带「加入」按钮的 3 处选货区（销售开单 / 进货加行 / 退换货换货）× 双布局 = 6 个列表：已加入整行灰底 `--c-picked:#e2e5ea`（明显深于斑马纹）+ 按钮由橙转蓝新增 `.btn-added`（#2563eb 白字 + 三态） | `css/base.css` 及三个页面 JS | 状态派生：删明细后高亮自动消失；按钮文案恒为「加入」（延续 V3.53） |
| 63.3 | `table.tbl tbody tr.picked`（含 td）用 `!important` 稳定压过同特异性 `nth-child(even)` | `css/base.css` | 高亮不被斑马纹覆盖 |

- **阶段测试**：新增 6 项（含用相对明度公式断言 `--c-picked` 必须比 `--c-stripe` 更深）+ 更新 4 项；Chromium 1200px/390px 实测：已加入 #e2e5ea、斑马 #f6f7f9、按钮 #f97316↔#2563eb、390px 横向溢出 0；全量 **701/701** 通过。
- **提交点**：`fdfec0096`（V3.55 部署记录）；SW 缓存 **v84**；回滚基线 tag：**v3.54-baseline**。

---

## 六·六十四、阶段 64：库存「预警 / 盘点」深度性能优化（V3.56）

**目标**：用户反馈「库存 → 预警/盘点 切换卡顿、没有分页、闪屏并跳到页头」。

### 问题根因

预警页固定只渲染前 300 行且**无分页器**；盘点页**一次性渲染全部商品**——3000 商品 = 3000 行 + 3002 个实盘 input、单页 HTML 711 KB，且每次切标签/搜索/改实盘数都整页重建 DOM；预警页的搜索与分类筛选此前对该页完全无效。

### 任务清单

| # | 任务 | 涉及文件 | 验收点 |
| --- | --- | --- | --- |
| 64.1 | 三个标签统一分页且各自独立页码：库存查询 200/页（沿用）、预警 200/页（新增分页器）、盘点 100/页（每行含输入框故页容量减半）；`page` 动作按当前标签分流，搜索/筛选自动回第 1 页 | `js/ui/page-inventory.js` | 盘点 3000 行 + 3002 input → 100 行 + 100 input（HTML 711 KB → 24 KB） |
| 64.2 | 库存页 DOM 拆为 `inv-head / inv-tabs / inv-search / inv-body` 四区块，页面新增 `update(ctx, state)` 做区块级局部刷新：未变化区块零写入、替换后恢复焦点与光标；`update` 返回 true 时跳过整页重建且完全不调用 `scrollTo` | `js/ui/page-inventory.js`、`js/app.js` | 切标签 盘点 124ms→5ms、预警 24ms→8ms、翻页 2ms，滚动位置逐像素不变 |
| 64.3 | 单渲染周期缓存：`getAlerts`（原一帧最多算 3 次）与「过滤+排序」各只算一次 | `js/ui/page-inventory.js` | 重复计算消除 |
| 64.4 | 预警页搜索/分类筛选修复生效 + 空态文案区分；盘点卡片「已填 N 项」提示，实盘数跨页保留 | `js/ui/page-inventory.js` | 翻页往返已填数据不丢 |

- **阶段测试**：新增 `tests/inventory-perf-v356.test.js` 10 项 + 更新 1 项；Chromium 390px/1200px 真机实测；全量 **711/711** 通过。
- **提交点**：`e9531401d`（V3.56 部署记录）；SW 缓存 **v85**；回滚基线 tag：**v3.55-baseline**。

---

## 六·六十五、阶段 65：手机端底部导航「库存」更换为「商品」（V3.57）

**目标**：手机底部固定三格应为「首页 / 商品 / 我的」。

### 任务清单

| # | 任务 | 涉及文件 | 验收点 |
| --- | --- | --- | --- |
| 65.1 | `app.js` 的 `navItems()`（底部栏唯一数据源）第二项由 `{inventory, 📋, 库存}` 改为 `{product, 📦, 商品}`，点击**真正换页**到商品档案 | `js/app.js` | 底部为 首页 / 商品 / 我的 |
| 65.2 | 库存管理页保留（首页入口 +「我的 → 常用入口」仍可进），仅移除底部固定入口 | `js/app.js` | 功能不丢失 |
| 65.3 | 手机 `navItems()` 与电脑侧栏 `desktopNavOrder()` 解耦，侧栏 12 项顺序不变；非底部页高亮仍兜底到「我的」 | `js/app.js` | 两端互不影响 |

- **阶段测试**：新增 `tests/nav-tabbar-v357.test.js` 6 项 + 更新 1 项；全量 **717/717** 通过。
- **提交点**：`738c23c91`（V3.57 部署记录）；SW 缓存 **v86**；回滚基线 tag：**v3.56-baseline**。

---

## 六·六十六、阶段 66：全局加载 / 切换 / 搜索深度性能优化 + 列表统一 100/页（V3.58）

**目标**：用户反馈「页面切换很慢、加载也很慢」，并要求所有列表改 100 条/页降低内存。

### 问题根因

`ctx.getProduct/getPartner` 是 `Array.find` 线性查找，而 `profit.topProducts` 为**每条售出商品**都调用它 → 3000 商品时 O(n²) 约 450 万次比较（报表页 106ms / 首页 90ms）；首屏还强制加载 xlsx 861KB + zxing 407KB。

### 任务清单

| # | 任务 | 涉及文件 | 验收点 |
| --- | --- | --- | --- |
| 66.1 | `repo.createContext` 内建**主键索引**（失效 = 数组引用变化 ∨ 长度变化 ∨ 命中 key 校验，**不依赖 touch** 以免批量导入退化）；条码查询因可原地修改仍走线性扫描以保证正确 | `js/store/repo.js` | 主键查询 O(1) |
| 66.2 | 利润聚合去重：畅销/滞销由两次 `topProducts` 改为 `productAgg` 单次聚合 + `rankProducts` 两次排序 | `js/core/profit.js`、`js/ui/page-report.js` | 结果等价，耗时大幅下降 |
| 66.3 | 新增 `js/core/lazy.js`：xlsx 与 zxing 改为按需注入（并发去重 + 失败可重试） | `js/core/lazy.js`、`js/core/excel.js`、`js/barcode/scan.js`、`index.html` | 首屏传输 ~1.99MB → 719KB（−64%），Excel 导入与扫码兜底实测可用 |
| 66.4 | **通用区块级局部刷新**：把 V3.56 库存页专属 `update()` 下沉为框架能力 `patchMain/reconcileList`，同路由逐块比对只写变化区块，结构不可调和时回落整页 | `js/app.js` | 不闪屏、不 scrollTo，滚动位置天然保持 |
| 66.5 | 全系统列表统一 100 条/页：销售 300→100、进货 300→100、商品档案 200→100、库存查询 200→100、预警 200→100（盘点 100 与选货区 15 保持） | 各页面 JS | 页容量一致，内存下降 |

- **阶段测试**：新增 `tests/perf-v358.test.js` 22 项，`tests/page-size-200.test.js` 迁移为 `tests/page-size-100.test.js`；Chromium 全站路由与关键交互回归；全量 **745/745** 通过。
- **实测**（Chromium 390px，3000 商品 / 2000 销售单 / 800 进货单）：报表 116.6→33.0ms、首页 101.1→35.3ms、商品档案 66.6→49.9ms、销售 49.8→33.2ms；各页 `render()` CPU 合计 104.5→10.8ms（约 9.7 倍）；底部导航真实点击切换稳定 ~32ms。
- **提交点**：`fed5eee51`（V3.58 部署记录）；SW 缓存 **v87**；回滚基线 tag：**v3.57-baseline**。

---

## 六·六十七、阶段 67：多员工账号 / 权限分级（V3.59）

**目标**：用户核心业务 = 零售 / 批发 / 管理总控；**所有权限由管理总控手动逐项分配，不要角色模板**。

### 任务清单

| # | 任务 | 涉及文件 | 验收点 |
| --- | --- | --- | --- |
| 67.1 | 16 项权限分 4 组：零售（开单/收款/退货作废/改价折扣）、批发（开单挂账/收款核销/退货作废）、档案库存（档案只读/库存只读/进货入库/建档改价合并删除/盘点调整）、资金管理（记账中心/报表与利润含成本/客户管理/数据管理） | `js/core/accounts.js` | 清单与分组唯一、无重叠 |
| 67.2 | 管理总控（hawsystem）固定全权限不可改；其余账号新建**默认全关**，由管理总控逐项开通；admin 自身不可被分配 | `js/core/accounts.js`、`js/ui/page-admin.js` | 不会因误操作锁死总控 |
| 67.3 | 员工账号**共用本店同一本数据**（ownerId 归属，权限只控制可见与操作、不隔离数据）；新建账号可选「共用本店数据 / 独立数据空间」 | `js/core/accounts.js` | 老板实时可见店员单据 |
| 67.4 | 三级拦截：菜单与快捷入口按权限隐藏、路由守卫拦截无权限页面、动作级拦截（开单/收款/改价/退货/盘点/建档/记账/同步等 20 余项规则）；无「报表与利润」权限隐藏成本列与首页毛利；无「建档」权限隐藏新建/编辑/删除/合并；无「数据管理」权限隐藏导入/导出/清空/云同步 | `js/app.js`、`js/ui/page-admin.js` 及各页面 | 越权操作被拦且不暴露入口 |
| 67.5 | 账户权限管理页新增权限分配面板：账号权限摘要 + 四组 16 项勾选 + 全选/清空 + 保存 | `js/ui/page-admin.js` | 一目了然、一次保存 |
| 67.6 | 旧账号迁移自动补全权限，避免升级后锁死 | `js/core/accounts.js` | 老账号登录后功能不缩水 |

- **阶段测试**：新增 `tests/perm-system.test.js` 21 项（含动作级拦截 7 项）、`tests/ui-admin-perm.test.js` 10 项；更新 `ui-mine`、`ui-product`(+2)、`ui-customer`、`button-colors`、`perf-v358` 适配；全量 **778/778** 通过。
- **提交点**：`625955c03`（V3.59 最后提交：员工共用本店数据不得覆盖老板 settings）；SW 缓存 **v88**；回滚基线 tag：**v3.58-baseline**。
- **二期**：审批流（订单审批）暂不实现。

---

## 六·六十八、阶段 68：账号云同步（跨端共用账号表）（V3.60）

**目标**：电脑网页版 / 电脑本地版 / 手机浏览器各自独立存账号（localStorage 按端隔离），A 端建的号在 B 端登录提示「账号不存在」。

### 任务清单

| # | 任务 | 涉及文件 | 验收点 |
| --- | --- | --- | --- |
| 68.1 | **管理总控上传**：账户权限管理页新增「☁️ 账号表上传到云端」（仅总控可见）：全部账号（登录名/店铺名/角色/perms/ownerId/**密码哈希，不含明文**）用同步口令 AES 加密后写入 `data/admin/accounts-sync.json` | `js/ui/page-admin.js`、`js/core/sync.js` | 云端文件只含哈希，明文不出现在信封 |
| 68.2 | **登录页自动降级拉取**：本地校验失败提示「账号不存在」时，自动拉云端 → 解密 → 合并 → 重新校验 | `js/ui/page-login.js`、`js/core/sync.js` | 其他端首次登录无感完成 |
| 68.3 | 合并规则：hawsystem 永远保留本地版本；云端同名以云端为准更新但**保留本地 id**；本地独有账号保留（并集不丢号） | `js/core/sync.js` | 数据空间引用稳定，不切库丢数据 |
| 68.4 | 配置查找优先级：总控同步配置 → 本机各账号隔离配置 → 全局配置 | `js/core/sync.js` | 手机端同步过数据即可自动拉取 |

- **阶段测试**：新增 `tests/accounts-sync.test.js` 16 项（脱敏导出、mergeCloud 五规则、findSyncConfig 优先级、pushAccounts 加密上传、pullAccounts 解密 / NO_SNAPSHOT / NO_CFG、tryCloudLogin 四种路径）；全量 **795/795** 通过。
- **提交点**：`0b8fde861`（V3.60 文档同步）；SW 缓存 **v89**；回滚基线 tag：**v3.59-baseline**。

---

## 六·六十九、阶段 69：账号云同步 V2 · 零配置公开拉取（V3.61）

**目标**：V3.60 上线后手机仍登录不了测试号 `pifadianqi`——它建在本地版/其他浏览器，从未进入总控所在浏览器的账号表；且拉取失败时静默降级成「账号不存在」，看不到真实原因。

### 任务清单

| # | 任务 | 涉及文件 | 验收点 |
| --- | --- | --- | --- |
| 69.1 | **内置固定密钥**：账号表改用 `sync.ACCOUNTS_PASSPHRASE`（代码内嵌，仅防明文裸奔）加密 → 手机/本地版**无需任何配置**即可解密 | `js/core/sync.js` | 非内置密钥解不开，内置密钥零配置可解 |
| 69.2 | **零配置公开拉取**：登录页先走公开通道，从 GitHub Pages 静态地址 `https://<owner>.github.io/<repo>/data/admin/accounts-sync.json` 拉取（无需 Token/口令、无限流）；地址来源 = 本机任意同步配置 → 在线 URL 自动推断；失败再回退本地配置通道 | `js/core/sync.js`、`js/ui/page-login.js` | file:// 本地版也能借配置推断地址 |
| 69.3 | **上传前账号清单确认**：总控点上传时 confirm 列出「即将上传 N 个账号：hawsystem、…」 | `js/ui/page-admin.js` | 不再出现「以为传了其实没传」 |
| 69.4 | **拉取失败显示真实原因**：`NO_CFG` → 提示先配置云同步或用在线版；`NO_SNAPSHOT` → 提示先用总控上传账号表；网络/解密错误原样透出（不含敏感配置） | `js/ui/page-login.js` | 用户能自己判断下一步 |

- **使用说明**：账号必须建在**管理总控登录的那个浏览器**；建号/改权限后需重新点「上传到云端」；旧版（用户口令加密）的云端文件需重新上传一次。
- **阶段测试**：`tests/accounts-sync.test.js` 扩至 25 项（内置密钥加密、publicAccountsUrl 推断优先级、pullAccountsPublic 解密 / 404 / NO_CFG_PUBLIC、pullAccountsAny 公共优先与回退、tryCloudLogin 四条路径）；全量 **804/804** 通过。
- **提交点**：`7907e82cd`（V3.61 文档同步）；SW 缓存 **v90**；回滚基线 tag：**v3.60-baseline**。

---

## 六·七十、阶段 70：新用户登录后没有任何数据（V3.62）

- **目标**：让「共用本店数据」的员工账号在任何设备上都能取到本店账本，并杜绝员工误操作清空全店云端数据。
- **问题根因**：
  1. 账号表云同步（V3.60/61）只同步账号档案，不同步业务数据；业务数据在各设备本地 IndexedDB。
  2. `js/ui/page-mine.js` 云同步的配置 key 与快照路径按 `currentAccount.id` 定位 → 员工去找自己的空路径（404）。
  3. 快照随行账户档案写员工自己的 → 覆盖老板店名/头像。
- **任务清单**：

| # | 任务 | 文件 | 说明 |
|---|---|---|---|
| 1 | 同步维度改数据归属账号 | `js/ui/page-mine.js` | 新增 `syncAcctId()`，替换 `loadConfig/saveConfig` 的账号参数 |
| 2 | 空库阻断上传 | `js/ui/page-mine.js` | 共用本店数据 + `isEmptyLedger(ctx)` → 不发起 `sync.syncUp` |
| 3 | 覆盖共享快照二次确认 | `js/ui/page-mine.js` | 文案含「覆盖全店共享快照」 |
| 4 | 快照档案取归属账号 | `js/ui/page-mine.js` | 新增 `syncAccountPublic()` |
| 5 | 数据空间可观测 | `js/ui/page-mine.js`、`js/ui/page-admin.js` | 同步卡片 `dataSpaceTip()` + 账号卡片 `page.dataSpaceOf()` 徽标 |

- **阶段测试**：`tests/shared-data-sync-v362.test.js` 10 项；`ui-mine.test.js`、`perf-v358.test.js` 断言同步更新。
- **提交点**：`ee9e719`（V3.62 代码 + 文档）；SW 缓存 **v91**；回滚基线 tag：**v3.61-baseline**；gh-pages 部署 `259eab9`（`8cd6813..259eab9` 快进，`data/admin` 完整保留）。

## 六·七十一、阶段 71：本机空账本引导（V3.63）

- **目标**：新员工在新设备登录后，首页明确告诉他「为什么没数据、下一步做什么」，而不是一片空白。
- **问题根因**：V3.62 只解决了「能拉到数据」，没解决「不知道要去拉」。且不同数据空间的操作路径完全不同，必须分流提示。
- **任务清单**：

| # | 任务 | 文件 | 说明 |
|---|---|---|---|
| 1 | 公共判空函数 | `js/core/util.js` | 新增 `util.isEmptyLedger(ctxOrData)`（商品/进货/销售/账目全空） |
| 2 | 首页引导卡 | `js/ui/page-home.js` | `emptyLedgerGuide(ctx)` 插入 `render()` 提醒区顶部，按数据空间分流 |
| 3 | 复用判空 | `js/ui/page-mine.js` | 空库上传保护改用 `util.isEmptyLedger`，消除逻辑漂移 |

- **阶段测试**：`tests/empty-ledger-guide-v363.test.js` 6 项。
- **提交点**：`bca54bb`（V3.63 代码 + 文档）；SW 缓存 **v92**；回滚基线 tag：**v3.62-baseline**（→`ee9e719`，git push 502 改用 `gh api` 建远端 ref）；gh-pages 部署 `c448035`（`259eab9..c448035` 快进，`data/admin` 完整保留）。

## 六·七十二、阶段 72：首页横幅「已备份」文案真实性（V3.64）

- **目标**：横幅的备份状态必须与真实数据一致，不能出现「横幅说已备份、提醒条说从未备份」的自相矛盾。
- **问题根因**：`js/ui/page-home.js` 的 `mobileHome` / `desktopHome` 各硬编码了一处
  `<div class="banner-sub">已备份 · 今天 09:12</div>`；而真实备份时间在 `ctx.data.lastBackupAt`
  （导出备份写入、清空数据置空），`backupReminder()` 读的是真实值 → 二者长期不一致。
- **任务清单**：

| # | 任务 | 文件 | 说明 |
|---|---|---|---|
| 1 | 备份文案纯函数 | `js/core/util.js` | 新增 `util.backupLabel(lastISO, todayStr)` → `{backed, text}` |
| 2 | 横幅改派生 | `js/ui/page-home.js` | 新增 `bannerBackupText(ctx)`，两处 `banner-sub` 替换硬编码 |

- **阶段测试**：`tests/backup-banner-v364.test.js` 8 项（含「横幅与提醒条不得自相矛盾」的交叉断言）。
- **提交点**：`1d55799`（V3.64 代码 + 文档）；SW 缓存 **v93**；回滚基线 tag：**v3.63-baseline**（→`bca54bb`）；gh-pages 部署 `6e7d783`（`c448035..6e7d783` 快进，`data/admin` 完整保留）。

## 六·七十三、阶段 73：记录级同步 —— 员工自助拉取权限内数据（V3.65）

- **目标**：员工在新设备上**只用自己的账号**登录，就能拉到自己权限内的那部分数据，全程不需要老板到场、不需要登录管理账户。
- **问题根因**：V3.62 只统一了「云同步找哪个数据空间」（员工与老板共用 `data/admin/erp-snapshot.json`），但快照里是**全店全部单据**，员工会把别人的单据一并拉下来；且单据上没有「谁经手」的标记，无从按人拆分。
- **任务清单**：

| # | 任务 | 文件 | 说明 |
|---|---|---|---|
| 1 | 记录打归属标记 | `js/store/repo.js`、`js/core/schema.js` | 交易类集合（`sales`/`purchases`/`ledgers`/`stocktakes`）落库时补 `createdBy`；OWNED/SHARED 集合分类 |
| 2 | 云端快照按经手人过滤 | `js/core/sync.js` | 新增 `isDataOwner()` / `filterSnapshotForAccount()`：员工只取 `createdBy` 为自己或为空的记录；商品/客户等共享主数据不过滤 |
| 3 | 员工只读拉取 | `js/ui/page-mine.js` | 共用本店数据的员工禁止上传，只保留「从云端恢复」 |
| 4 | ~~经手人下拉~~ **V3.65 未实现**（只预留了 `ctx.operatorOverride` 读取口，无 UI、无任何赋值） | 4 个交易表单 | **顺延 V3.68** |
| 5 | ~~取数凭证~~ **V3.65 未实现**（`bossPhrase`/`makeCredential` 只定义未调用＝死代码） | `js/core/accounts.js`、`js/ui/page-admin.js` | **顺延 V3.69** |

- **阶段测试**：记录级过滤与归属判定由 `sync` 单测覆盖；员工类夹具统一补 `ownerId:'admin'`。
- **提交点**：与 V3.66 合并提交于 `bcb2635`（V3.65 开发完成后一直未单独入库，本次随 V3.66 一并发布）；SW 缓存 **v94**。

## 六·七十四、阶段 74：字段级可见性 —— 成本仅老板可见，零售/批发逐款开关（V3.66）

- **目标**：① 成本对员工**一律隐藏**（哪怕是财务型员工）；② 零售价、批发价**每款商品单独控制**是否对员工可见，两个开关互不影响。
- **问题根因**：V3.65 解决的是「能看到**哪些单据**」，但单据里的**成本**仍对员工可见；而 `canViewCost` 原判定是「管理总控**或**报表权限」，导致有报表权限的财务员工照样能看到进货成本。零售/批发原先只能整体给或不给，无法按商品区分敏感度。
- **任务清单**：

| # | 任务 | 文件 | 说明 |
|---|---|---|---|
| 1 | 成本收口「仅老板」 | `js/core/accounts.js` | `canViewCost` 改为严格 `acct.id === (acct.ownerId \|\| acct.id)`；权限文案改为「报表与利润（成本仅老板可见）」 |
| 2 | 统一可见性判定 | `js/core/product.js` | 新增 `isDataOwner()` / `visibleToStaff(p, acct, kind)`（`cost`/`retail`/`wholesale`）；无账号上下文按老板兜底 |
| 3 | 逐款开关字段 | `js/core/product.js` | 商品新增 `staffShowRetail` / `staffShowWholesale`，新建**与编辑**两条路径都持久化（编辑路径此前漏写已修复），缺省 `true` |
| 4 | 档案页表单与列表 | `js/ui/page-product.js` | 两个独立复选框「员工可见零售价/批发价」；非老板时成本输入框替换为无权提示；列表成本列整列隐藏、价格列按开关显 `—` |
| 5 | 销售/进货遮蔽 | `js/ui/page-sale.js`、`js/ui/page-purchase.js` | 选品与明细的单价/成本列对员工显 `—`，**仍正常参与合计**，不影响开单与进货录入 |
| 6 | 报表与打印遮蔽 | `js/ui/page-report.js`、`js/ui/print-doc.js` | 成本类指标全显 `—`；畅销滞销对非老板**强制按数量**并隐藏「按毛利」；进货单打印成本列仅老板可见 |

- **阶段测试**：`tests/field-visibility-v366.test.js` 8 项；更新 3 项旧断言（员工夹具补 `ownerId`、报表权限员工改为不可见成本、聚合次数断言改匹配动态 `by`）；全量 **836/836** 通过。
- **提交点**：`bcb2635`（V3.66 代码 + 文档）；SW 缓存 **v95**；回滚基线 tag：**v3.64-baseline**（→`cad3edb`）；gh-pages 部署 `20704b8`（`6e7d783..20704b8` 快进，`data/admin` 完整保留）。

## 六·七十五、阶段 75：账户权限管理页操作失败提示修复（V3.67）

- **目标**：任何操作失败都要**看得见**提示，杜绝「点了按钮但毫无反应」。
- **问题根因**（Playwright 真机复现）：管理员新建账号填入非法登录账号后，
  `state.error` 确实已写入，但**页面上零个提示元素** —— 两处缺陷叠加：
  ① 校验失败分支写 `return false`，而本框架中 `return false` = **跳过 `afterAction` 重渲染**
  （V3.47 为防「勾选跳顶」引入），错误只存在内存、页面从不重绘；
  ② 即便重绘也看不到 —— 错误渲染在整页最底部（保存按钮之后），而表单在页面上方。
- **任务清单**：

| # | 任务 | 文件 | 说明 |
|---|---|---|---|
| 1 | 错误分支改为返回 true | `js/ui/page-admin.js` | 7 处「设 `state.error` 后 `return false`」→ `true`（创建/修改保存/删除/账号表上传四类，此前全部静默） |
| 2 | 错误就地显示 | `js/ui/page-admin.js` | 新建表单、修改表单内部（按钮正上方）各加一处 `notice-warn` |
| 3 | 去掉重复提示 | `js/ui/page-admin.js` | 表单打开时页底不再重复渲染同一条错误（一屏只出现一次） |

- **阶段测试**：`tests/admin-error-feedback-v367.test.js` 8 项（校验失败返回 true、密码不一致返回 true、错误出现在 `create-box` 内、全页只出现一次、修改/删除 admin 同样返回 true、非法账号不入库、合法账号仍能创建、版本号）；改写 `tests/ui-admin.test.js`「admin 不可删」断言 `false`→`true`（拦截语义不变）。全量 **844/844** 通过。
- **提交点**：`38132c6`（V3.67 代码 + 文档）；SW 缓存 **v96**；回滚基线 tag：**v3.66-baseline**（→`ffc2f23`）；gh-pages 部署 `baaa94c`（`20704b8..baaa94c` 快进，`data/admin` 完整保留）。

## 六·七十六、阶段 76：经手人下拉 —— 记录归属可改派（V3.68）

- **目标**：老板替员工开的单能归到员工名下，员工在新设备才拉得到（补齐 V3.65 遗留）。
- **问题根因**：V3.65 打通了记录级同步（按 `createdBy` 过滤），但归属**只能「谁登录归谁」**；
  `repo.touch` 里预留的 `ctx.operatorOverride` **没有任何地方给它赋值**，四个表单也没有下拉
  —— 即「预留了口子但没接线」。
- **任务清单**：

| # | 任务 | 文件 | 说明 |
|---|---|---|---|
| 1 | 经手人候选 helper | `js/core/accounts.js` | 新增 `isDataOwner()` / `operatorChoices()`：老板→本人+共用员工（独立数据空间不算）；员工→`[]` |
| 2 | 销售表单 | `js/ui/page-sale.js` | `state.form.operatorId` + 下拉；保存前设 `ctx.operatorOverride`，保存后清除 |
| 3 | 进货表单 | `js/ui/page-purchase.js` | 同上（新建态 tab 名为 `form`，不是 `new`） |
| 4 | 记账（记一笔） | `js/ui/page-account.js` | 同上（`state.manual.operatorId`，动作 `manual-field`） |
| 5 | 盘点 | `js/ui/page-inventory.js` | `st.take.operatorId` + 新增动作 `take-operator` |

- **取值链**：下拉 → `ctx.operatorOverride` → `repo.touch` 写 `rec.createdBy`
  → `sync.filterSnapshotForAccount` 按人过滤 → 员工拉到自己名下的单。
- **阶段测试**：`tests/operator-v368.test.js` 13 项。
  ⚠️ **坑**：Node 无 `localStorage`，`operatorChoices` 返回空 → **下拉根本不渲染**，
  单测「没报错」不等于「能用」，已用 Playwright 真机验证（选员工后 `createdBy=acct1`）。
- **提交点**：SW 缓存 **v97**；回滚基线 tag：**v3.68-baseline**。

## 六·七十七、阶段 77：取数凭证接线 —— 员工零配置自助拉数据（V3.69）

- **目标**：员工换到一台新设备，**只用自己的账号密码登录**就能拉到本店数据，
  不需要老板到场、也不需要把同步口令告诉员工。
- **问题根因**：V3.65 只**定义**了凭证形态（`syncPhraseEnc` = 用员工密码加密的老板同步口令）
  并写了 `page.bossPhrase` / `page.makeCredential`，但**这两个函数从未被调用（死代码）**
  —— 发放端没人发、消费端没人领，链路是断的。
- **任务清单**：

| # | 任务 | 文件 | 说明 |
|---|---|---|---|
| 1 | 发放端 | `js/ui/page-admin.js` | `issueCredential(store, acctId, empPwd)`：新建账号 / 改员工密码时用**明文密码**加密老板口令写入 `syncPhraseEnc` |
| 2 | 消费端 | `js/ui/page-login.js` | `claimCredential(store, account, pwd)`：登录时解开凭证，写入本机 `passphrase`（按**数据归属账号**存） |
| 3 | 登录不中断 | `js/ui/page-login.js` | `_doLoginSuccess` 先登录、再后台解凭证；失败静默跳过 |
| 4 | 可观测 | `js/ui/page-admin.js` | 凭证状态徽章（已发放 / 未发放 / 不需要）+ 「清除凭证」按钮 |

- **为什么只能在建号 / 改密码时发放**：凭证必须用**员工明文密码**加密，
  而老板只在这两个时刻掌握明文密码（其余时刻库里只有哈希）。
- **取值链**：老板建号 → `wrapPhrase(员工密码, 老板口令)` → 存 `syncPhraseEnc`
  → 员工登录 → `unwrapPhrase(本次输入密码, 凭证)` → 写 `sync 配置的 passphrase`
  → 「从云端恢复」即可解密全店快照。
- **阶段测试**：`tests/credential-v369.test.js` 21 项。
  ⚠️ **坑 1**：老板机器与员工新设备是**两台设备、两份配置**，
  单测里必须分别造 sync 实例，否则退化成「本机已配口令」走 `already-configured`，测不到新设备场景。
  ⚠️ **坑 2**：Node 单测环境无 WebCrypto，**凭证加密根本不会执行**，
  单测「没报错」不等于「能用」——已用 Playwright 真机跑完整链路（含反向对照）。
- **提交点**：SW 缓存 **v98**；回滚基线 tag：**v3.69-baseline**。

## 六·七十八、阶段 78：凭证「未发放」补操作指引（V3.70）

- **目标**：老板看到「未发放」时知道下一步做什么 —— 存量员工（V3.69 之前建的）
  必须**重设一次密码**才能补发凭证。
- **问题根因**：凭证只能用员工**明文密码**加密，发放时机仅为「新建账号 / 改员工密码」，
  所以 V3.69 之前建的账号（如线上的 `pifa`）**永远不会自动补上**；
  而界面只写「未发放」，没说怎么办。
  （该路径本身可用，已真机验证：重设密码 → 凭证 `kind:'secret'` 落库 → 界面转「已发放」
  → 员工用新密码登录自动取回老板口令 → 旧密码失效。）
- **任务清单**：

| # | 任务 | 文件 | 说明 |
|---|---|---|---|
| 1 | 指引文案 | `js/ui/page-admin.js` | `credentialBadge(a, bossPhrase)`：未配口令→引导去云同步；已配→引导点「修改」重设密码 |
| 2 | 传参 | `js/ui/page-admin.js` | `page.render` 开头缓存一次 `page.bossPhrase(store)`，避免逐账号重复读 |

- **⚠️ 坑**：并行对同一文件发两个 `Edit` 会互相覆盖（本次把 `bossPh` 定义改丢了，
  render 报 `bossPh is not defined`）。**同一文件的多处改动要串行改，改完必须复核。**
- **阶段测试**：`credential-v369.test.js` 新增 D1–D3 共 3 项。
  真机：Playwright 桌面 1280 / 手机 390 双视口确认文案渲染正确且无横向溢出。
- **提交点**：SW 缓存 **v99**；回滚基线 tag：**v3.70-baseline**。

## 六·七十九、阶段 79：管理页「从云端拉取账号表」入口（V3.71）

- **目标**：换设备 / 换浏览器后，老板能一键把云端员工账号拉回本机，继续管理。
- **问题根因**：账号表存在**本机 localStorage**。新设备登录后本地只有预置 `admin`
  （登录时本地命中就不会再拉云端）→ 管理页看不到 `pifa` → 改不了密码、发不了凭证。
  V3.61 的公开拉取通道一直**没有界面入口**。
- **任务清单**：

| # | 任务 | 文件 | 说明 |
|---|---|---|---|
| 1 | 合并规则 | `js/ui/page-admin.js` | 纯函数 `mergeAccounts(local, cloud)`：id 优先→username；云端为准；本地独有保留 |
| 2 | 入口与动作 | `js/ui/page-admin.js` | 按钮 `admin-pull-accounts` + 说明文案；失败按 V3.61 错误分类给真实原因 |

- **关键边界**：本地已发放的 `syncPhraseEnc`、云端缺该键时**不得被抹掉**
  （否则刚发的凭证会被拉取冲掉）—— 已单测锁定。
- **阶段测试**：`tests/accounts-pull-v371.test.js` 10 项。
  ⚠️ **验证顺序坑**：真机验证必须**先部署再验**，否则线上还是上一版（本次就先跑脚本
  发现线上仍是 V3.70、按钮不存在）。
- **提交点**：SW 缓存 **v100**；回滚基线 tag：**v3.71-baseline**。

## 六·八十、阶段 80：凭证发放后提示「上传账号表」（V3.72）

- **目标**：别让「改密码补发凭证」变成只在本机看起来成功。
- **问题根因（关键链路）**：`page-login.js` 的 `tryCloudLogin` —— **本地已有该账号就直接本地校验**，
  本地没有才走云端。员工在**新设备**登录 → 本机无此账号 → 账号对象来自**云端账号表**；
  而老板改密码发的凭证写在**本机 localStorage** → **不上传 = 云端没凭证 = 仍然取不到**
  （`claimCredential` 返回 `no-credential`）。
- **任务清单**：

| # | 任务 | 文件 | 说明 |
|---|---|---|---|
| 1 | 徽章文案 | `js/ui/page-admin.js` | 「已发放」→ 追加「需再点『☁️ 账号表上传到云端』」 |
| 2 | 操作提示 | `js/ui/page-admin.js` | 新建账号 / 改密码两条发放成功提示同样追加 |

- **纯文案改动，零逻辑改动**。
- **操作口径（三步缺一不可）**：① 给员工改一次密码 → ② 点「☁️ 账号表上传到云端」→ ③ 把新密码告诉员工。
- **阶段测试**：`tests/credential-v369.test.js` 新增 E1–E2 共 2 项。
- **提交点**：SW 缓存 **v101**。

## 六·八十、阶段 80：员工「我的」页精简（V3.73）

- **背景**：用户反馈（附手机截图）——员工账号登录后「我的」页还是老板的样子：头部显示老板店铺名「电器批发管理总控」与经营范围；云同步卡片带大段备注、同步设置按钮、无用的「同步到云端（只读）」灰按钮、数据空间与上次同步信息。
- **要求**：员工页只显示 **权限模块（常用入口）** 与 **「从云端恢复」按钮**，其余注释/提醒信息全部隐藏；头部显示员工自己的账号名。
- **任务清单**：

| # | 任务 | 文件 | 说明 |
|---|---|---|---|
| 1 | 员工头部 | `js/ui/page-mine.js` | 显示 `account.shopName \|\| username` 与员工头像；去掉编辑入口与老板经营范围 |
| 2 | 员工云同步卡片 | `js/ui/page-mine.js` | 只留「从云端恢复」按钮；备注/同步设置/数据空间/上次同步/只读灰按钮全部不显示 |
| 3 | 员工隐藏「关于」 | `js/ui/page-mine.js` | 员工不渲染关于卡片（含检查更新） |
| 4 | 桌面端页头 | `js/ui/page-mine.js` | 员工显示自己账号名 |
| 5 | 老板视图回归 | — | 老板（数据归属账号）一切照旧，已有断言不改 |

- **改写 1 条旧断言**：V3.62「员工卡片显示数据空间归属」→ V3.73 按用户要求隐藏（归属由 `syncAcctId` 代码保证，无需界面说明）。
- **阶段测试**：新增 `tests/mine-staff-v373.test.js` S1–S8 共 8 项；真机 Playwright 全链路 16 项断言全过。
- **提交点**：SW 缓存 **v102**。

## 六·八十一、阶段 81：新版本自动生效（V3.74）

- **背景**：V3.73 隐藏员工「关于」卡片（含检查更新）后，员工端没有任何手动刷新入口；SW 虽有 `skipWaiting` + `clients.claim`，但已打开页面仍运行旧 JS，需再刷新一次才能换新——员工设备会卡旧版（用户手机停 V3.66 即此现象）。
- **方案**：`index.html` 启动脚本——页面在旧 SW 控制下加载（`navigator.serviceWorker.controller` 非空）时监听 `controllerchange`，新 SW 接管瞬间自动 `location.reload()` 一次；`swRefreshing` 防重复；首次打开（无 controller）不刷新，无循环。
- **效果**：本次部署后员工设备手动刷新一次到 V3.74，此后所有新版本自动生效；老板端「检查更新」保留。
- **阶段测试**：`tests/mine-staff-v373.test.js` 新增 S9 源码断言。
- **提交点**：SW 缓存 **v103**。

## 六·八十二、阶段 82：展示身份统一（V3.75）

- **背景**：用户反馈「首页右上角仍然是总控的用户名和头像」——顶栏 / 侧栏 / 电脑端顶栏 / 首页 banner 全部读共享店铺资料 `ctx.settings`，员工共用老板数据 → 显示老板店名与头像。
- **方案**：`core/branding.js` 新增 `api.identity(settings, account)` 作为「当前展示身份」唯一取值处；员工返回自己的 `account.shopName || username` + `account.avatar`，老板与无账号场景仍返回店铺资料。
- **任务清单**：

| # | 任务 | 文件 | 说明 |
|---|---|---|---|
| 1 | identity / isStaffAccount | `js/core/branding.js` | 员工判定三级退化：sync → accounts → ownerId |
| 2 | 顶栏 / 侧栏 / 电脑端顶栏 | `js/app.js` | 统一走 identity；店名 escHtml 转义 |
| 3 | 首页 banner | `js/ui/page-home.js` | 手机端 + 电脑端各 1 处 |
| 4 | 反向污染 | `js/app.js` | `applyAccountToSettings` 对员工跳过，不写共享 settings |

- **阶段测试**：新增 `tests/identity-v375.test.js` 7 项；真机 Playwright 7 项断言全过。
- **提交点**：SW 缓存 **v104**。

## 六·八十三、阶段 83：设置页对员工收敛（V3.76）

- **背景**：用户连续反馈「员工能看到/改到老板的东西」后的系统性排查——设置页的「店铺名称」「价格体系」「备份与恢复」「数据管理」都直接改全店共享数据，员工可改老板店名、重算全店价格、覆盖/清空全店数据。
- **修法**：员工（非数据归属账号）界面隐藏 + 动作层拦截双保险；打印参数（本机）与打开密码、操作日志保留。
- **阶段测试**：新增 `tests/setting-staff-v376.test.js` 7 项；真机 Playwright 14 项断言全过。
- **提交点**：SW 缓存 **v105**。

## 七、风险与注意点

| 风险 | 说明 | 对策 |
| --- | --- | --- |
| QR 识别兼容性 | `BarcodeDetector` 的 `qr_code` 支持因浏览器/系统而异（Chrome/Android 较好；桌面差异大） | 沿用三级降级：实时不行走拍照（ZXing 支持 QR）→ 手输；验收以「条码 + 二维码」双通道兜底为准 |
| 移除 SKU 的引用残留 | `skus` 被 engine/inventory/cart/scan/backup 多处引用，漏改会报错 | 阶段 1 用 grep 全量清理 `skus/\.sku/\.size/\.color` 引用，并以测试兜底 |
| 成本同步与快照 | 进货后同步档案成本，但历史单据成本不能变 | 单据保存即固化 `costSnapshot`；同步只改档案 `cost` 字段，报表读快照 |
| 预置账号经营范围 | 原预置账号绑定「鞋/服装/配饰」分类 | 改为绑定「大家电/小家电/厨电」类型，`categoriesFor/inScope` 沿用 |
| 备份兼容 | 新 JSON 无 `skus/printJobs` 表 | `schema.VERSION` 升为 2，`validateBackup` 校验新结构；旧备份导入给出明确提示 |

---

## 八、交付清单（开发完成后）

1. 可运行项目：`/Users/ybf/Desktop/Home Appliance Wholesale/erp/`（`file://` 双击即用）
2. 全量测试报告：`node --test tests/` 全绿（逐条列出）
3. README.md（运行/备份/部署说明）
4. Git 提交历史（每个功能一个提交，可回溯）
5. 部署说明（免费 https 静态托管方案，供手机扫码使用）

---

## 九、确认方式

- 回复「**确认**」→ 进入开发，每完成一个功能提交一次 Git、测试通过后再推进下一个。
- **文档维护说明（2026-09-16）**：阶段 40–69 的独立章节已补全（此前仅压缩在开头「状态」行中、无法检索）；回滚基线 tag `v3.49-baseline` ~ `v3.61-baseline` 均已推送，任一版本可一键回退。
- 或指出要调整的阶段/任务编号。
- 主流程 6 阶段已完成；V2.2/V2.3/V2.4/V3.1/V3.2 功能（类型自定义、列表即时显示、品牌形象、多店铺头像隔离、账户管理、管理员权限、账户收归、体验优化、云同步增强、客户管理、统一版本号、销售开单体验与单据打印）已全部按增量开发完成并部署上线，全量测试 298/298 通过。

---

*本计划为开发执行依据；PRD 变更需先更新 PRD 再调整本计划。*
