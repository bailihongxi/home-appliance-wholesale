# 2026-09-16

## 项目现状盘点（只读探查，未改代码）

- 当前版本 **V3.61**（SW `appliance-erp-v90`）；三处版本号一致：page-mine.js:569 / sw.js / tests/ui-mine.test.js。
- 全量测试 **804/804 通过**（82 个测试文件，约 31 秒）。
- 源码规模：43 个 JS，约 15906 行；页面路由 14 个（home/product/purchase/sale/exchange/inventory/account/supplier/customer/report/setting/mine/admin + login）。
- 数据模型：8 张表 products/purchases/sales/stocktakes/stockLogs/ledgers/partners/logs（+meta），schema.VERSION=2，类型字典 9 类。
- 部署：main `7907e82` = origin/main；gh-pages 本地 `6a40b0e`（含 V3.61 部署 `f8378cd`），远端 `8cd6813`，线上实测 sw.js=v90、page-mine=V3.61、`data/admin/accounts-sync.json` 返回 200。
- 近期版本：V3.59 多员工账号/权限分级（16 项权限 4 组）→ V3.60 账号云同步 → V3.61 零配置公开拉取（内置密钥 + 公开通道 + 上传前清单确认 + 失败给真实原因）。

### 发现的两个隐患（待用户决定是否需要处理）

1. **Git 仓库被 shallow 化**：`.git/shallow` 存在，main 仅保留最近 3 个提交（grafted at `0b8fde8`）；远端最新 tag 只到 `v3.57-baseline`，**V3.58–V3.61 的回滚基线 tag 缺失**，真出问题无法按版本回退。
2. **文档不同步**：`开发计划_电器版.md` 阶段 40–69 只压缩在开头的「状态」行里，没有独立章节；memory 也停在 2026-09-15，V3.59–V3.61 无记录。

## 已处理：补齐 V3.58–V3.61 回滚基线 tag

- 沙箱内 `git` 协议不通（`github.com` 000/502，fetch/push/ls-remote 全失败），改用
  **`gh api` 直接建远端 ref** 完成（gh 已登录 bailihongxi，scopes 含 repo）：
  - `v3.58-baseline` → `fed5eee51`（V3.58 docs）
  - `v3.59-baseline` → `625955c03`（V3.59 fix：员工共用本店数据不覆盖老板 settings）
  - `v3.60-baseline` → `0b8fde861`（V3.60 docs）
  - `v3.61-baseline` → `7907e82cd`（V3.61 docs，当前 HEAD）
- baseline 口径沿用既有约定 = **该版本最后一个提交**（含 docs/memory 提交）。
- API 建的是轻量 tag（object.type=commit），老 tag 是 annotated，回滚/checkout 用法一致。
- 另核实：gh-pages 本地 `6a40b0e` 相对远端 `8cd6813` 是 behind_by=5（远端有手机端账本同步提交），
  本地不是未推送，线上已是 V3.61 / SW v90。
- 上述「git 不通时用 gh api」的写法已写入 `~/.workbuddy/skills/home-appliance-erp-release/SKILL.md` 8b 节。

## 已处理：补写开发计划阶段 40–69 独立章节

- 原文档 1126 行，阶段 40–69 只压缩在开头「状态」行里（无法检索），现补全为 30 个独立章节 → **1635 行**。
- 每个章节含：目标 / 问题根因（必要时）/ 任务清单表格 / 阶段测试 / 提交点 + SW 版本 + 回滚基线 tag；
  插入位置为「## 七、风险与注意点」之前（即阶段 39 之后）。
- 版本↔SW↔提交点映射**逐版本实测**：取该版本最后一个提交，用 GitHub API 读 `erp/sw.js` 的
  `CACHE` 值，不靠推断（推断会错，例如 V3.52 提交信息里提到「V3.49」会误导）。
  结果：V3.32=v59 … V3.49=v77、V3.50=v79（v78 跳号）、V3.61=v90；提交 sha 见文档各章节。
- 测试数取 PRD 变更记录：582(V3.32) → 804(V3.61)。

### 工具坑（本次复现）

- 该仓库部分文件用 `grep`/`grep -n` 直接搜中文或多行模式会**静默返回空**（"阶段 69"、"V3.61" 都搜不到），改用 `python3` 正则读文件才可靠；`grep -a` 有时可用，优先用 Python。
