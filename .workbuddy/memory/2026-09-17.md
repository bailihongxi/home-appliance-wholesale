# 2026-09-17

## V3.62：新用户分配权限后登录没有任何数据（共用本店数据的云同步走错数据空间）

### 现象与排查
- 用户反馈「新用户已分配权限，但是登录后没有任何数据」。
- **关键排查手法**：云端账号表 `data/admin/accounts-sync.json` 用**内置密钥**
  （`sync.ACCOUNTS_PASSPHRASE = 'appliance-erp-accounts-2026'`，AES-GCM + PBKDF2 150000）
  可**离线解密核对**。Node 里 fetch 该文件 → decrypt → 直接拿到全部账号的
  id/username/ownerId/perms，不用登录用户浏览器就能确认配置是否正确。
- 核对结果：新账号 `pifa`（acct1）`ownerId='admin'`（数据空间正确）、
  权限 2 项 `product_read + data_manage`。**账号与权限都没问题**。
- 云端快照探针：`data/admin/erp-snapshot.json` → 200（老板数据已上传）；
  `data/acct1/erp-snapshot.json` → **404**。

### 根因（三层）
1. **业务数据不在账号表里**：账号表云同步（V3.60/61）只同步登录名/权限/数据空间/密码哈希；
   商品·单据·账本在**各设备本地 IndexedDB**，换设备登录天然为空。
2. **代码缺陷（主因）**：`js/ui/page-mine.js` 的同步配置 key 与快照路径按
   `currentAccount.id` 定位 → 员工（acct1）去找 `data/acct1/erp-snapshot.json`（404），
   而老板的数据在 `data/admin/erp-snapshot.json`（200）→ 员工点「从云端恢复」
   永远报「云端还没有快照」。
3. 快照随行的账户档案写员工自己的 → 会把老板店名/头像覆盖。

### 修复
- `page-mine.js` 新增 `syncAcctId()` = `accounts.dataOwnerId(currentAccount)`，
  替换全部 `loadConfig/saveConfig` 的账号维度（共用→admin，独立→自身 id）。
- 新增 `sharesBossData()` / `isEmptyLedger(ctx)`：`sync-up` 在「共用本店数据 + 本机空账本」时
  **阻断上传**（否则员工一次误点会清空全店云端数据）；非空时二次确认「覆盖全店共享快照」。
- 新增 `syncAccountPublic()`：快照档案取归属账号（老板）。
- 新增 `dataSpaceTip()`：云同步卡片显示「数据空间：共用本店数据（applianceErp_admin）/ 独立」。
- `page-admin.js` 新增 `page.dataSpaceOf(a)` + 账号卡片数据空间徽标；新建账号表单补充说明
  「独立数据空间 = 新账号登录后是一本空账」。

### 发布
- 版本 V3.62（SW v91）；全量 **814/814** 通过（新增 10 项）。
- main `ee9e719`；回滚基线 `v3.61-baseline` 已存在（指向 7907e82）；
  gh-pages `259eab9`（8cd6813..259eab9 快进，`data/admin` 完整保留）。
- 线上校验：`sw.js`=v91、`page-mine.js`=V3.62、`syncAcctId` 出现 10 次、两个 data 文件 200。

### 踩坑
1. **UMD 引用坑（测试）**：`page-mine.js` 的 IIFE 里 `var ERP = root.ERP || {}`，
   若 `globalThis.ERP` 当时不存在，闭包拿的是对象 A，而随后 `root.ERP = root.ERP || {}`
   又指向新对象 B —— 测试里**先 require 再设 `globalThis.ERP.currentAccount` 会取不到**。
   必须在 require 之前先 `globalThis.ERP = globalThis.ERP || {}`。
2. **`sync-up` 是异步的**：内部先 `flushNow(ctx).then(...)` 再 `syncUp`，
   断言是否发起上传必须 `await sleep(5)` 冲刷微任务，同步断言会误判。
3. **`node --test tests/` 不行**，必须 `node --test tests/*.test.js`（package.json 的 test 脚本口径）。
4. **本仓库没有 `origin/gh-pages` 跟踪引用**：`git fetch origin gh-pages` 只写 FETCH_HEAD，
   要 `git reset --hard <FETCH_HEAD 的 sha>`；且 `git reset --hard FETCH_HEAD` 在该 worktree
   会报「Use '--' to separate paths」——直接用 sha 即可。
5. **gh-pages 静态资源有 CDN 缓存**：推完立刻 curl 仍可能是旧版（实测首次 curl 还是 v90/V3.61），
   约 1–2 分钟后复检才变 v91；加 `?cb=$RANDOM` 可提前穿透。
6. 本地 main 与 origin/main 曾出现「同内容不同 sha」的分叉（浅克隆/grafted 历史所致），
   用 `git rebase --onto origin/main <分叉起点> main` 把新提交挑过去即可，**不要 force-push**。
7. 沙箱里 `grep` 工具对部分文件静默返回空，改用 `/usr/bin/grep` 或 Python 读文件。

### 不改代码的临时解法（用户可直接用）
在新用户**那台设备**上先用 `hawsystem` 登录 → 我的 → 云同步 → 从云端恢复
（老板数据落进该设备的 `applianceErp_admin`）→ 退出 → 用新账号登录，即看到同一本账
（因为二者 `dbNameForAccount` 都是 `applianceErp_admin`）。

## V3.63：本机空账本引导（新用户登录不再是一片空白）

- **为什么还要再补一版**：V3.62 只解决了「员工能拉到本店数据」，没解决「他不知道要去拉」。
  新员工在一台新设备登录，看到的仍是一片空白、完全不知道下一步做什么——
  这才是用户最初反馈「登录后没有任何数据」的真实体感。
- **改动**：
  - `js/core/util.js` 新增 `util.isEmptyLedger(ctxOrData)`（商品/进货/销售/账目全空即为空），
    判空逻辑收敛为单一来源；`page-mine.js` 的空库上传保护改为复用它。
  - `js/ui/page-home.js` 新增 `emptyLedgerGuide(ctx)`，插在 `render()` 的提醒区顶部
    （手机端 `mobileHome` 与电脑端 `desktopHome` 共用同一条 `rem`，一次插入两端生效）。按数据空间分流：
    - 共用本店数据：说明「与老板共用同一本账，但这台设备还没拉到数据」+ 两步操作
      （退出 → 管理总控登录 → 我的 → 云同步 → 从云端恢复 → 退出再用本账号登录）+「去云同步」按钮；
    - 独立数据空间：说明是全新空账本 +「去建档 / 导入」按钮。
    - **未登录（无账号上下文）不显示** —— 关键约束，否则大量用 `newCtx()` 的首页单测会凭空多一张卡片。
- **测试**：新增 `tests/empty-ledger-guide-v363.test.js` 6 项；全量 **820/820** 通过。
- **发布**：main `bca54bb`；回滚基线 `v3.62-baseline` → `ee9e719`（git push 502，改用 `gh api` 建远端 ref）；
  gh-pages `c448035`（259eab9..c448035）；线上校验 sw=v92、page-mine=V3.63、page-home 含 emptyLedgerGuide、
  data 两个文件均 200。

### 新增坑
- **git 通道会间歇性 502**（`CONNECT tunnel failed, response 502`）：**多试几次通常就能过**
  （本次 main 第 1 次失败、第 2 次成功；gh-pages 第 1 次失败、第 2 次成功），持续失败才转 `gh api`。
  **绝不 force-push**。
- 循环重试别写 `git push ... | tail -2 && break`：管道返回的是 `tail` 的退出码（恒 0），会误判成功。
  要用 `if git push >log 2>&1; then ... fi`。
- 用 Python 批量替换测试里形如 `sw.includes("var CACHE = 'appliance-erp-vXX';")` 的断言时，
  转义单引号的写法会**静默不匹配**（文件报告 changed 但实际没换），导致两处断言停在旧版本而全量测试红。
  改完版本号务必 `grep -n "v9[12]" tests/*.test.js` 复核三处断言（ui-mine / perf-v358 / 新测试文件）。
- Bash heredoc 里写 Python 时，**Python 字符串内含三引号会炸**（`SyntaxError: invalid syntax`）。
  大段 Markdown 文本用 `cat >> file <<'EOF'` 直接追加最稳，别绕 Python。

## V3.64：首页横幅「已备份」文案真实性（硬编码假备份时间）

- **怎么发现的**：V3.63 做完后看真机截图，发现首页横幅写着「已备份 · 今天 09:12」，
  而同一屏下方的提醒条写着「你已从未备份」——自相矛盾。
- **根因**：`js/ui/page-home.js` 的 `mobileHome` 与 `desktopHome` **各硬编码了一处**
  `<div class="banner-sub">已备份 · 今天 09:12</div>`。
  真实备份时间其实一直存在 `ctx.data.lastBackupAt`（`page-setting.js` 导出备份时写入、
  清空数据时置空），`backupReminder()` 读的就是真实值 → 二者长期不一致。
  **危害**：会让用户以为数据已有备份而放松警惕，属会引发真实数据风险的显示 Bug。
- **改动**：
  - `js/core/util.js` 新增纯函数 `util.backupLabel(lastISO, todayStr)` → `{backed, text}`：
    无记录 → `尚未备份 · 建议尽快导出一份账本`；当天 → `已备份 · 今天 HH:MM`；
    30 天内 → `已备份 · N 天前`；更早 → `上次备份 · YYYY-MM-DD`。
  - `js/ui/page-home.js` 新增 `bannerBackupText(ctx)` 由 `lastBackupAt` 派生，
    两处 `banner-sub` 统一改用（手机端/电脑端一次改两处）。
- **测试**：新增 `tests/backup-banner-v364.test.js` 8 项，其中一条是**交叉断言**：
  「横幅说已备份」与「提醒条说从未备份」不得同时成立（防回归到自相矛盾）。
- **发布**：main `1d55799`；回滚基线 `v3.63-baseline` → `bca54bb`；
  gh-pages `6e7d783`（c448035..6e7d783 快进）；线上校验 sw=v93、page-mine=V3.64、
  page-home 含 `bannerBackupText` ×3、data 两文件 200。

### 通用教训
- **硬编码的"状态文案"是隐患**：凡是会随数据变化的展示文案（备份时间、同步时间、统计数字），
  必须从真实数据派生。审查手法：在 UI 层/模板里 grep 形如 `今天 09:12`、`¥0`、`0 条`
  这类**写死的示例值**——它们在 demo 里看着正常，上线后就是谎言。
- 改完 UI 后**一定要看真机截图**，不要只跑断言。这个 Bug 全量测试 820 项全绿也没发现，
  因为它不是"渲染错"，而是"渲染了一个假数据"。

---

## V3.66 字段级可见性：成本仅老板可见 + 零售/批发逐款独立开关

### 需求与口径（已与用户确认两点）
- **成本可见范围 = 仅老板可见（最严）**：所有非数据归属账号（**含拥有「报表与利润」权限的
  财务/管理型员工**）一律看不到成本。
- **零售/批发价默认都显示**：新增开关默认 `true`，老板按需逐个关闭个别商品的价格；
  **存量商品无需配置即正常显示**（缺省 `true`，不覆盖）。

### 实现要点
- `accounts.canViewCost(acct)` 由「管理总控或报表权限」改为严格 `acct.id === (acct.ownerId || acct.id)`；
  权限文案改「报表与利润（成本仅老板可见）」。
- 新增 `product.isDataOwner()` / `product.visibleToStaff(p, acct, kind)`（`cost`/`retail`/`wholesale`），
  **判定集中到一处**，避免散落各页面。
- 商品新增 `staffShowRetail` / `staffShowWholesale`；`page-product.js` 表单两个独立复选框
  「员工可见零售价 / 员工可见批发价」，互不联动。
- 遮蔽覆盖：档案列表+表单、销售选品/明细、进货选品/明细、报表（成本类指标全 `—`，
  畅销滞销**强制按数量**并隐藏「按毛利」）、进货单打印。
- **遮蔽但仍参与合计**：销售/进货明细单价显 `—`，`costPrice`/单价行仍进合计，
  保证员工开单、进货录入功能不受影响。

### 踩过的坑（重要，易复发）
1. **员工夹具必须显式带 `ownerId:'admin'`**。判定是 `id === (ownerId || id)`，
   没有 ownerId 的账号 == 数据归属方（老板）。旧测试里 cashier/manager/USER 都没写 ownerId，
   于是"员工看不到成本"的断言全红。**正确做法是修夹具，而不是放宽判定函数**。
2. **"无账号上下文"按老板兜底**：`canViewCost(undefined)` 返回 false 会让无账号的单测
   （如 `price-system.test.js` 的 `fresh()`）把成本藏掉。约定 `acct` 为空时按可见处理。
3. **新建与编辑两条路径都要落库**：`product.save` 新建分支写了两个开关、**编辑分支漏写**，
   导致改完开关保存不生效。以后给记录加字段，两条路径一起改。
4. **不要相信会话摘要**：前序摘要声称 `renderForm` 已加开关与成本提示，
   实际 `git diff --stat` 只有 9 行、根本没有。**以 `git diff --stat` / 读真实文件为准**。
5. 报表 `renderTop` 把 `state.topBy` 换成动态 `by` 后，V3.58「只聚合一次」的
   **源码 grep 断言失效** → 断言要跟实现走（改成断言 `productAgg` 一次 + `topProducts` 零次）。

### 测试与发布
- 新增 `tests/field-visibility-v366.test.js` 8 项；更新 3 项旧断言（员工夹具补 ownerId、
  报表权限员工改为不可见成本、聚合次数断言改匹配动态 `by`）。全量 **836/836** 通过。
- 版本号三处同步 V3.66 / SW **v95**（page-mine.js、sw.js、5 个测试文件的版本断言）。
- **发布**：main `bcb2635`（V3.65 记录级同步此前一直未入库，本次随 V3.66 一并提交）；
  回滚基线 `v3.64-baseline` → `cad3edb`；gh-pages `20704b8`（6e7d783..20704b8 快进，
  `data/admin` 完整保留）；线上校验 sw=v95、page-mine=V3.66、data=admin。
- 文档：PRD 加 (160)(161) 两条变更记录；开发计划加阶段 73（V3.65）、阶段 74（V3.66）。

### 通用教训
- **权限要分层看**：V3.65 解决"能看到哪些**记录**"，V3.66 解决"记录里能看到哪些**字段**"。
  加权限功能时先分清是记录级还是字段级，两者实现位置完全不同
  （前者在 `sync.filterSnapshotForAccount`，后者在渲染层的 `visibleToStaff`）。
- **默认值的方向要选对**：遮蔽类开关默认应为"可见"，否则一次升级会让员工
  突然什么价格都看不到，属于线上事故。宁可让老板逐个关，也不要默认全关。

### 补充教训：git push 被墙时**不要用 API 推 commit**
本次 `git push` 遇 `CONNECT tunnel failed, 502`，尝试改用 `gh api` 建 blob/tree/commit 把
docs 提交推上去，结果踩了坑：
- API 建出来的 commit **SHA 与本地不同**（GitHub 会规范化 message，本地 `git commit-tree`
  复现不出同一个 SHA），于是**远端与本地分叉**，后续 `git push` 全部变成 non-fast-forward 被拒。
- 而 API **读不到本地独有的 commit**（`GET /git/commits/<本地sha>` → 404），
  所以也无法把远端指回本地那个 commit。
- **正确做法**：`git push` 502 就是**重试**（实测第 1 次失败、隔一会儿第 5 次成功）。
  `gh api` 只适合**建 tag / 改 ref 指向已存在的远端对象**，不要用它"推送"本地 commit。
- 若已分叉且内容一致：可把远端 ref **force 回退到共同父提交**（内容不丢，本地仍留着那个提交），
  再正常 `git push` 快进。本次即如此收场：远端回退到 `bcb2635` → 本地 1 ahead → 推送成功。

---

## V3.67：账户权限管理页「创建账号无反应」修复（return false 吞掉错误提示）

### 现象
管理员在「账户权限管理 → 新建店铺账号」点「创建账号」，页面毫无变化、无任何提示。

### 复现方法（Playwright 真机，值得复用）
本地起服务 `python3 -m http.server 8766`（**必须用 `run_in_background:true`，
否则 Bash 调用结束进程就被杀**），登录 `hawsystem / admina1b22c333`。
**关键坑：登录页第一个可见 input 是全局搜索框**，用 `input:visible` 会把账号打进搜索框
导致登录失败 —— 必须按 placeholder 定位
（`input[placeholder="请输入登录账号"]` / `input[placeholder="输入密码"]`）。
手机端「账户权限管理」在侧栏里**默认不可见**，要走
「我的 → `[data-act="go"]` 含『权限管理』」进入。

### 根因（两处叠加，很容易只修一处就以为修好了）
1. **校验失败分支写了 `return false`** —— 本框架中 `return false` 的语义是
   **跳过 `afterAction` 重渲染**（V3.47 为防「勾选跳顶」引入）。
   于是 `state.error` 存进内存、页面从不重绘 → **纯静默失败**。
   ✅ 凡是「需要把错误显示给用户」的分支，**必须返回 `true`**。
   ⚠️ `return false` 本身是合法用法（勾选类动作用它保持滚动位置），
   不要一刀切全改，只改设了 `state.error` 的那些分支。
2. **即便重绘也看不到** —— 错误渲染在整页最底部（保存按钮之后），
   而表单在页面上方，账号一多完全不在视野内。
   ✅ 错误要**就地渲染**在表单内部、按钮正上方。

本次共修 7 处，覆盖**创建账号 / 修改保存 / 删除账号 / 账号表上传**四类操作
—— 它们的失败提示此前**全部是静默的**，不只是用户报的那一个。

### 通用教训
- **「点了没反应」类 bug，先查 action 的返回值**，再看错误渲染在页面的哪个位置。
  这类问题单测往往测不出来（断言只查 `state.error` 有没有值，不查页面有没有渲染），
  必须真机点一遍 / 或补「错误出现在 DOM 里」的断言。
- **错误提示的可见性要和「用户视线位置」对齐**：用户在表单底部点按钮，
  提示就必须出现在按钮附近；放在页脚等于没有。
- 改完顺手**排查同类写法**：`grep "state.error = .*return false"` 一次揪出全部 7 处。

---

## V3.68：经手人下拉 —— 补齐 V3.65 遗留（记录归属可改派）

### 缺口（又一次「摘要说做了、实际没做」）
V3.65 只在 `repo.touch` 预留了 `ctx.operatorOverride` 读取口，
**四个表单既没加下拉、也没有任何地方给它赋值** → 归属固定「谁登录归谁」，
老板替员工开的单选不到员工名下，员工永远拉不到。
`bossPhrase` / `makeCredential` 同样只定义未调用（死代码）→ 顺延 V3.69。

### 实现
- `accounts.isDataOwner()` / `accounts.operatorChoices(store, cur)`：
  老板 → 本人 + 共用本店数据的员工（**独立数据空间账号不算**，各是一本账）；
  员工（带 ownerId）→ `[]`，界面**不渲染下拉**（不能改派，也看不到全店名单）。
- 销售 / 进货 / 记账（记一笔）/ 盘点 四表单加下拉，保存前设 `ctx.operatorOverride`、
  保存后**立即清除**（防污染后续记录）。
- 取值链：下拉 → `ctx.operatorOverride` → `repo.touch` 写 `createdBy`
  → `sync.filterSnapshotForAccount` 按人过滤 → 员工拉到自己名下的单。

### 两个真坑（都踩了）
1. **Node 单测里这个功能根本不会被渲染**：无 `localStorage` → `operatorChoices` 返回 `[]`
   → 下拉不出现。所以「全量测试 844 通过」**完全没覆盖新代码**。
   ✅ 结论：依赖浏览器环境（localStorage/账号）的功能，**必须 Playwright 真机点一遍**，
   单测「没报错」不等于「能用」。
2. **进货页新建态是 `state.tab === 'form'`，不是 'new'**（销售是 'new'）。
   我按 'new' 写断言 → 表单没渲染 → 断言「不含下拉」**假通过**。
   ✅ 教训：写「不应存在 X」这类否定断言时，**先断言「容器确实渲染了」**，否则永远绿。

### 验证
Playwright 真机（线上站点同样通过）：建员工 → 老板记一笔选「测试员工」
→ `createdBy=acct1` ✅；不选 → `admin` ✅。全量 **857/857**。

### 文档诚信
上一轮我把 PRD/开发计划里 V3.65 的「经手人下拉」「取数凭证」写成了已完成 —— **照抄了会话摘要，没对代码**。
已更正并标注顺延版本。**铁律：变更记录必须逐条对着代码 grep 核实。**
