# 2026-09-12 工作日志

## V3.47（SW v75）复选框跳顶修复 — 推送部署

- 需求/根因/修复见 `.workbuddy/memory/2026-09-09.md` 的 V3.47 条目。
- 推送：main `af914dd` 已 push origin/main。
- 部署 gh-pages：`git worktree add /tmp/gh-pages-worktree gh-pages` → `reset --hard origin/gh-pages`（保留 data）→ 叠加 erp/ 站点文件（排除 tests/）→ `touch .nojekyll`。
- 关键：推送时远程 gh-pages 已领先 2 个手机端 sync 提交（仅改 data/admin/erp-snapshot.json），改用 `git rebase origin/gh-pages` 把 V3.47 提交叠到最新数据之上，**未 force-push**，保留用户最新手机端账本（快照 784011 字节）。
- 最终 gh-pages HEAD：`8aab06f deploy: V3.47 (SW v75) 修复商品档案复选框勾选后页面跳回顶部`。
- 测试：全量 671/671 通过。
- 部署地址：https://bailihongxi.github.io/home-appliance-wholesale/

## V3.48（SW v76）根治同路由重渲染跳顶 + 全系统搜索框一键清除

**问题1**：用户反馈销售开单选货区点「加入」、新建进货单「按商品加行」点「加入」也会出现页面跳回顶部，说明不是单一页面 bug。
**根因**：`app.js` 的 `render()` 无条件 `window.scrollTo(0,0)`，任何同路由整页重渲染都会跳顶。
**修复**：`render()` 新增 `lastRoute` 路由变化检测；同路由重渲染前保存 `scrollX/scrollY`，渲染后恢复；仅路由切换时才 `scrollTo(0,0)`。一次性根治所有页面内操作（加入、改数量、切价格类型、搜索等）的跳顶问题。

**问题2**：用户要求搜索框增加一键清除小按钮（类似全局搜索框右侧灰色 X），避免逐个字母删除。
**修复**：全系统搜索输入框统一改为 `type="search"`，启用浏览器原生一键清除按钮（有内容时自动显示）。覆盖：
- `ui.searchBar` 组件（商品档案/销售/进货/记账/客户/供应商/退换货列表）
- 新建进货单选货区内联搜索框
- 退换货换货补货搜索框
- 库存盘点录入搜索框（补充 `data-live="1" data-debounce="1"`，清空后即时刷新）
- `base.css` 显式定义 `input[type=search]` / `::-webkit-search-cancel-button` 样式

**测试**：新增 `tests/render-scroll.test.js`（4 项）；ui-searchbar/ui-purchase/ui-inventory/ui-exchange 各新增断言；ui-mine 版本断言升 V3.48；全量 **680/680** 通过。

### V3.48 部署

- 推送：main `99fd692..99af2ce`（2 提交）已 push origin/main。
- 部署 gh-pages：worktree reset → overlay erp/ 站点文件（排除 tests/）→ `.nojekyll`。
- 关键：推送时远程 gh-pages 又新增 2 个手机端账本 sync 提交（`2e7bf5c`、`fa717c1`），改用 `git rebase origin/gh-pages` 把 V3.48 部署提交叠到最新数据之上，**未 force-push**，保留用户最新手机端账本（快照 803071 字节，比 V3.47 时更新）。
- 最终 gh-pages HEAD：`1f5f6fb deploy: V3.48 (SW v76) 根治同路由重渲染跳顶 + 全系统搜索框一键清除`。
- 部署地址：https://bailihongxi.github.io/home-appliance-wholesale/

## V3.49（SW v77）横向滚动容器（.table-wrap）重渲染保持 scrollLeft

**问题**：手机端销售开单「选货区」左右滑动后点「加入」、当前订单列表横向滑动后改数量/切价格等操作，页面会左右跳动、跳回商品列表最左。用户要求不仅修这一个页面，还要排查其他页面一并修复。
**根因**：与 V3.48 同类——V3.48 仅恢复了 window 级滚动，但 `render()` 整页重渲染时 `app.main.innerHTML` 替换 DOM，内部 `.table-wrap`（`overflow-x:auto` 横向滚动容器，全系统唯一的横向滚动容器）的 `scrollLeft` 被重置为 0。
**修复（app.js render()，系统性）**：
- 同路由重渲染前记录各 `.table-wrap` 的 `scrollLeft`（按 DOM 顺序存入 `savedHScroll`）；
- 渲染后按最小数量 `Math.min(wrapsAfter.length, savedHScroll.length)` 对齐写回，仅同路由时恢复（路由切换不把旧页滚动带到新页）；
- 覆盖销售开单/进货单/商品档案/记账/退换货/库存盘点等所有页面表格的左右滑动保持。
- 暴露 `app._savedHScroll` 便于运行时观测。

**测试**：`tests/render-scroll.test.js` 新增 3 项（查询 `.table-wrap` 并记录/恢复 scrollLeft、暴露 `app._savedHScroll`）；ui-mine 版本断言升 V3.49；全量 **683/683** 通过。
**部署（2026-09-12 用户「推送部署」）**：
- 推送：main `9c069b0..001b146` 已 push origin/main。
- 部署 gh-pages：`git worktree add /tmp/gh-pages-worktree gh-pages` → `reset --hard origin/gh-pages`（保留 data）→ 叠加 erp/ 站点文件（排除 tests/）→ `.nojekyll`。本次远程 gh-pages 无新 sync 提交，直接快进推送（未 force-push）。
- 最终 gh-pages HEAD：`a876b52 deploy: V3.49 (SW v77) 横向滚动容器(.table-wrap)重渲染保持 scrollLeft`；快照 803071 字节完好。
- 部署地址：https://bailihongxi.github.io/home-appliance-wholesale/

## V3.50（SW v78）选货区两行卡片式排版（手机/电脑无需横向滚动）— 代码完成，待部署（Bash 工具临时不可用）

**需求**：用户先用样式图确认方向后，要求把「销售开单 → 选货区」每行商品从单行多列（商品/批发/零售/库存/加入，手机需左右滑）改为两行卡片：第一行品牌+型号，第二行类型/单位 + 批:¥xx / 零:¥xx，库存与加入按钮位置大小不变；并要求「固定未修改前的 V3.49 为回滚基准 + 单独把本次选货区改动作为一个版本，方便留用/回滚」。

**实现（统一改三个选货区，均为「选货/加行」语义，手机均会横向滚动）**：
- `erp/js/ui/page-sale.js` 选货区：去掉 `<table class="tbl tbl-striped">`，改为 `<div class="pick-list">` + `pick-item`（第一行 `pick-name` 品牌+型号；第二行 `pick-sub`：类型/单位 + `批:` + `零:`；右侧 `pick-side`：库存 `pick-stock` + 加入按钮 `data-act="pick-product"`）。
- `erp/js/ui/page-purchase.js` 按商品加行：同样 `pick-list` 卡片，第二行显示为 `成本:¥xx`（进货无批/零，只有档案成本）。
- `erp/js/ui/page-exchange.js` 换货补货选货区：同样 `pick-list` 卡片，第二行 `批:`/`零:`。
- `erp/css/base.css` 新增 `.pick-list`/`.pick-item`/`.pick-main`/`.pick-name`/`.pick-sub`/`.pick-price`/`.pick-side`/`.pick-stock`（含 `.pick-stock.low` 红色低库存）。

**测试**：新增 `tests/pick-list.test.js`（4 项：销售/进货/换货选货区均为两行卡片式 + CSS 含对应类）；`ui-sale.test.js`/`ui-purchase.test.js` 原 `tbl tbl-striped` 断言改为 `pick-list`+`pick-sub`（未删除，按工作流更新）；`ui-mine.test.js` 版本断言升 V3.50。**注意：全量测试因 Bash 不可用暂未运行（待恢复后跑 `node --test tests/*.test.js` 确认）。**

**版本**：`sw.js` CACHE 升 `appliance-erp-v78`；`page-mine.js` 版本升 V3.50；`PRD_家电版.md`/`开发计划_电器版.md` 文档版本升 V3.50 + PRD 新增 V3.50 变更记录（145）。

**回滚基准**：V3.49 已上线（gh-pages `a876b52`）。计划用 `git tag v3.49-baseline` 固定未修改前版本（待 Bash 恢复后打 tag + commit V3.50 + 推送 + 部署）。

**待办（Bash 恢复后）**：① `git tag v3.49-baseline`；② 跑全量测试；③ commit V3.50；④ push main；⑤ worktree 叠加部署 gh-pages（保留 data/，若远程有 sync 提交则 rebase 后快进，不 force-push）。
