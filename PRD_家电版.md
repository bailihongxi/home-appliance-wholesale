# PRD · 家电批发进销存记账软件（电器版 · V2 简化商品模型）

- 文档版本：**V3.5**（统一版本号：自 V3.1 起 PRD 与开发计划文档版本号统一，软件「我的 → 关于」同步一致；本版升至 V3.5）
- 日期：2026-09-04
- 状态：**已确认开发中**（D1–D6 全部定案；V2.3 补充管理员账号与账户权限管理；V2.4 汇总账户管理收归管理员 + 体验优化 + 云同步增强；V3.1 新增客户功能页面并统一版本号；V3.2 销售开单体验与单据打印；V3.3 记账头部单行 + 库存统计卡压缩 + 三页搜索模块重排 + 侧栏折叠与导航重排 + 手机端适配修复；V3.4 手机库存卡再缩 + 打印版本选择与关闭操作栏 + 整体蓝色配色 + 跨端库存合并同步；V3.5 库存卡高度贴合内容 + 云同步账户/系统参数增强 + 价格体系，全部已开发上线）
- 适配母版：桌面《Shoes and clothing ERP》（鞋服店进销存 V3）
- 确认进度（2026-09-02）：**D1 成本口径 ✅ 已确认 · D2 库存来源 ✅ 已确认 · D3 扫码保留 ✅ 已确认 · D4 类型字典 ✅ 已确认（预设可选择 + 自定义可输入） · D5 预警阈值 ✅ 已确认（默认 3） · D6 管理员账号 ✅ 已确认（方案 A：admin / 初始密码 admina1b22c333，管理全部账号经营范围）**。
- 重大变更（2026-09-02）：**电器无需编码、无需商品属性**。商品按单一信息建模：**品牌、型号（列表分两列展示）、类型、单位、成本、批发价、零售价、库存、备注**；另保留**原厂条码/二维码扫码**定位商品（D3）。销售时每行可**选择按批发价或零售价成交**。
- V2.2 变更（2026-09-02 开发中补充）：**① 类型字段支持预设选择 + 自定义输入；② 新建商品后档案列表必须即时显示（显示本账号全部商品，新类型自动并入经营范围）；③ 页面 icon / 应用 logo / 网页名称可自定义（默认已换电器版图标）；④ 多店铺创建账号可自行设定登录名、密码与头像，数据按店铺独立隔离（`erp_<账号id>` 库）**。
- V2.3 变更（2026-09-02 待开发）：**⑤ 新增管理员账号 `admin`（初始密码 `admina1b22c333`），登录后可进入「权限管理」管理全部账号的经营范围（9 类商品分类勾选），普通账号无此入口**。
- V2.4 变更（2026-09-03 已开发上线）：**⑥ 账户管理收归管理员——登录页取消账号编辑/删除/新建按钮，账户的新建/修改/删除统一在管理员「权限管理」页操作（admin 自身不可改删）；⑦ 体验优化——备注列超长省略+悬停查看全文、我的页常用入口图标与首页统一（开单改手推车）、商品档案斑马纹交替行底色、取消手机/本地版右上角铃铛与头像图标；⑧ 云同步增强——403 错误细分诊断+「测试连接」预检、上传前 gzip 压缩+超限拦截、内容指纹智能同步（无变更则跳过上传/恢复）**。
- V3.1 变更（2026-09-03 已开发上线）：**⑨ 新增「客户」功能页面——电脑端左侧导航 + 手机端「我的 → 常用入口」双入口；客户可独立建档、搜索、编辑、删除（有余额/有销售记录禁止删除）、一键跳转记账中心收款；⑩ 统一版本号——PRD、开发计划、软件「我的 → 关于」统一为 V3.1**。
- V3.2 变更（2026-09-03 已开发上线）：**⑪ 销售开单页布局改造——收款模块移到页面底部横排（选货区 + 当前订单两列在上、收款整行在下，非右侧窄列），并重新排布为一行紧凑式（收款方式 / 实收·余款·欠款 / 备注·按钮同一行，宽度与两列等宽，降低模块高度）；⑫ 左侧菜单「销售开单」更名「销售管理」——进入默认销售列表管理视图（与进货管理同等级同布局），右上角「销售开单」按钮进入开单页，首页开单大按钮 / 我的页常用入口 / 扫码加单等所有开单按键统一直达开单视图；⑬ 新增销售单 / 进货单打印功能——半张 A4（A5 148×210mm）、明细多页自动分页且每页重复表头，单据详情弹层加「打印」按钮**。
- V3.3 变更（2026-09-03 已开发上线）：**⑭ 记账中心头部单行重排——标题 + 应付合计 + 应收合计 + 流水/应付/应收 切换按钮合并为同一行，统计改紧凑横排胶囊、切换按钮改标准按钮样式并激活高亮（桌面端一行、窄屏自动换行）；⑮ 库存管理顶部 4 个统计卡（商品数/总库存/资金占用/低库存预警）两度压缩——内边距/卡片高度减半、桌面保持 4 列并收紧间距、数字（13→24px）与图标（17→30px）放大近两倍；并修复手机端 2 列自适应屏幕宽度（`minmax(0,1fr)` 防内容撑出屏幕、卡片改竖排标签上数字下保证长金额完整显示）；⑯ 商品档案 / 进货管理 / 销售管理 三页搜索模块按记账中心·库存管理风格重排——电脑端：搜索框 + 筛选下拉同一行、日期单独一行；手机端改回多行（搜索框+扫码一行、筛选下拉换行第二行）便于点选；⑰ 电脑端左侧导航栏可折叠——侧栏顶部折叠按钮，收起仅保留图标（60px 窄栏）、点击展开/收缩且 localStorage 记忆；导航顺序重排为上至下：首页→进货管理→销售管理→档案管理→库存管理→记账中心→报表与利润→退换货→供应商→客户→我的→账户权限管理（商品档案导航显示名「档案管理」，页面标题仍为「商品档案」）；⑱ 修复手机端销售开单收款模块乱套——覆盖基础 4 列网格改为纵向堆叠（收款方式三输入框等宽并排、实收/余款/欠款一行、取消/保存按钮等宽并排、备注独占整行加长）；我的 → 常用入口「开单」改为「销售」并直接进入销售管理列表页；⑲ 最终统一版本号 V3.3**。
- V3.5 变更（2026-09-04 已开发上线）：**㉖ 手机版库存管理页顶部 4 个统计卡高度与内容贴合——根因 label 被 flex 拉伸/多余内边距撑到 113px 致卡片 140px，给 `.stat-grid-compact` 的 label/value 锁定内容尺寸（`flex:0 0 auto; height:auto; min-height:0; padding:0`），卡片收缩至 ~38px 贴合数字，文字/数字字号完全不变；㉗ 云同步增强——快照新增脱敏账户档案（店铺名/头像/经营范围，**不含密码哈希/令牌**），「同步到云端」自动携带当前账户档案，「从云端恢复」后自动写回账户设置，手机端登录同一账户即可对齐店铺名/头像/经营范围；确认系统整体参数（settings：店铺名/经营范围/库存阈值/利润率等）随快照同步（记录级合并保留两端）；各账户同步配置 key 与云路径按账户隔离（`applianceErp.sync.config.<acctId>` / `data/<acctId>/erp-snapshot.json`），本地命名空间 `applianceErp.*`、IndexedDB `applianceErp_<acctId>`、快照自标识 `appliance-erp` 均与鞋服母版（`erp.*`/`erp_*`/`shoeErp`）完全隔离，不与其他项目公用数据库；㉘ 价格体系——系统设置新增整体利润率（批发利润率默认 20%、零售利润率默认 35%，随云快照同步），新建/编辑商品只填成本、批发/零售价留空时自动按 `成本×(1+利润率)` 生成并**取整到元（不含小数点）**，自定义价格保留；商品档案新增「💰价格体系」页：可修改整体利润率、并提供「一键更新全部商品价格」将全部商品批发/零售价按各自成本与最新利润率统一重算并取整到元；㉙ 电脑端搜索模块下拉宽度微调（方案B）——`.search-bar .select` 增加 `flex:0 0 auto; min-width:130px`，下拉不再缩成内容宽度，手机端仍保持独占一行不受影响；㉚ 最终统一版本号 V3.5（SW v28）**。

---

## 0. 本版变更说明（请重点确认）

### 0.1 相对 V1（方案A纯换皮）的变更

**移除的能力（因"无需编码、无需商品属性"）：**
1. ❌ 自动编码体系：商品编号（款号）、SKU id、条码自动生成，全部移除。
2. ❌ 颜色 × 规格 属性矩阵：SKU 子层移除，商品即单一 SKU（一个品牌+型号 = 一件商品）。
3. ❌ 自动编码生成、条码标签打印（蓝牙/电脑）及其界面与测试。
4. ✅ **扫码保留（D3 已确认）**：手机摄像头 / 扫码枪扫描**机身原厂条形码或二维码**定位商品；开单时可扫码加行。识别沿用原项目三级降级（实时预览 → 拍照识别 → 手输），**同时支持条形码（EAN-13 等）与二维码（QR Code）**。
5. ❌ 编码相关验收条目移除；扫码相关验收改为「原厂条码/二维码定位商品」口径（见第 10 节 10.6）。

**新增/修改的能力：**
1. ✅ 商品档案改为 **9 字段单商品模型**：品牌 / 型号 / 类型 / 单位 / **成本（进价）** / 批发价 / 零售价 / 库存 / 备注（+可选原厂条码）。
2. ✅ **单位字段**（台 / 件 / 套 / 个…）。
3. ✅ **成本（进价）列**：商品档案直接维护成本；进货单带出可改，保存后同步更新档案成本（D1 已确认）。
4. ✅ **批发价 / 零售价双售价**：开单时每行**可选按批发价或零售价成交**（可改价）。
5. ✅ **品牌、型号在列表中分两列独立展示**。
6. ✅ 库存为**商品级单一数量**（不再分颜色×规格），由单据实时联动。

**完整保留的能力（进销存核心，不改）：**
- 进货 → 库存 → 销售 → 记账 → 欠款 → 利润 的完整闭环
- 退换货红冲、单据作废回滚、赠送管理、盘点调整
- 记账中心（供应商/客户应收应付）、三层利润报表、首页经营看板
- 一键备份 / 导入恢复 / 打开密码 / 操作日志
- V3 多店铺多账号（数据隔离、经营范围过滤、GitHub 云同步）
- 商品 CSV 批量导入、手机+电脑双端响应式、离线可用

### 0.2 数据模型对比

| 项目 | 原项目（鞋服） | V1 电器版 | **本版 V2（确认采用）** |
| --- | --- | --- | --- |
| 商品层级 | 款 → 颜色×尺码 SKU | 商品编号 → 颜色×规格 SKU | **单层商品**（无子 SKU） |
| 商品标识 | 款号 X001 | 商品编号 D001 | **品牌 + 型号**（内部自增 ID 主键，界面不显示编码） |
| 商品字段 | 名称/类别/颜色/号码/进价/售价 | 品牌/型号/类别/颜色/规格/进价/售价 | **品牌/型号/类型/单位/成本/批发价/零售价/库存/备注**（品牌、型号列表分两列展示；+可选**原厂条码**） |
| 库存粒度 | 颜色×尺码矩阵 | 颜色×规格矩阵 | **商品级单一数量** |
| 售价 | 单售价 | 单售价 | **批发价 + 零售价 双价** |
| 编码/条码/打印 | 有 | 有 | **无自动编码、无标签打印；扫码保留**（扫原厂条码/二维码定位商品） |

### 0.3 V2.2 功能完善（2026-09-02，已开发并同步至本文档）

开发过程中确认/新增的 4 项能力，均已实现并通过测试：

1. **类型字段：预设选择 + 自定义输入（D4 落地）** —— 商品"类型"不再只是固定下拉，改为**可从预设 9 类下拉建议中选择，也可直接输入自定义类型**（如"净水器""集成灶"）。下拉建议 = 预设类型 + 本账号已用过的类型；自定义类型保存后自动并入账号经营范围，后续再次输入时有建议。
2. **新建商品后档案列表即时显示** —— 列表**显示本账号全部商品**（用户已创建的商品必须可见，不被经营范围过滤隐藏）；保存经营范围外的自定义类型时，该类型**自动并入账号经营范围**并持久化，保证列表可见与下拉建议一致。
3. **页面 icon / 应用 logo / 网页名称可自定义** ——
   - 默认图标已由鞋服版（T 恤+运动鞋）更换为**电器版**（冰箱+空调+洗衣机+ERP）；
   - 「我的 → 店铺资料」可上传**店铺头像（logo）**与**修改店名**；店名即时联动**网页标题**与顶栏 logo，头像即时联动**浏览器标签页 favicon** 与应用内 logo；
   - 未自定义时统一回退电器版默认图标/「我的电器店」。
4. **多店铺登录：登录名/密码/头像自定 + 数据独立隔离** ——
   - 登录页「＋ 新建店铺账号」可自行设定**登录账号、店铺名称、密码、头像**（上传图片）；
   - 各店铺数据存独立库 **`erp_<账号id>`**，**数据互不共有**（登录哪店只见哪店数据）；
   - 每个店铺均可使用全部功能（建档/进货/销售/库存/报表等），经营范围默认全部分类开放。

### 0.4 V2.3 管理员账号与账户权限管理（2026-09-02，已确认待开发）

1. **管理员账号 `admin`** ——
   - 预置管理员账号：登录名 `admin`、店铺名「管理总控」、**初始密码 `admina1b22c333`**（登录后可在「我的」修改密码/店名/头像）；
   - 管理员也拥有**独立数据空间** `erp_admin`，可正常使用全部功能，也可用于自己的账务；
   - 登录页列表显示管理员账号（带头像/店名），与普通店铺账号一致。
2. **账户权限管理（方案 A：经营范围管理）** ——
   - 管理员登录后，「我的」页出现「**权限管理**」入口（**普通账号不显示**）；
   - 权限管理页：列出**全部账号**（含预置店铺与自建店铺），管理员可为每个账号**勾选经营范围**（9 类商品分类：冰箱/洗衣机/空调/电视/厨房电器/生活小家电/数码影音/配件耗材/其他），或设为「全部分类」；
   - 保存后**即时写入账号** `scopeCategories`；该账号下次登录 / 使用（建档、销售建议、类型建议）按新经营范围生效；
   - 管理员**不能删除自己**；不限制管理员本身经营范围（默认全部分类）。
3. **权限含义** —— 经营范围 = 该店铺可经营/可见的商品分类（与 D4 类型字典一致）；被移除范围的类型，该账号已建档商品仍保留显示（列表显示本账号全部商品），但新建/建议不再出现该分类。

### 0.5 V2.4 账户管理收归管理员（2026-09-03，已开发上线）

**登录页只做账号选择登录，账户管理全部收归管理员统一操作：**

1. **登录页精简** —— 取消账号卡片后的**编辑 / 删除按钮**与**「＋ 新建店铺账号」按钮**；登录页改为**纯账号选择 + 密码登录**；未选中账号时提示「账户的新建 / 修改 / 删除由管理员（管理总控）统一管理」。
2. **权限管理页升级为账户管理总控** —— 管理员登录后「我的 → 账户权限管理」：
   - **新建店铺账号**：登录名 / 店铺名称 / 初始密码 / 头像；
   - 每个店铺账号卡片带 **「修改」**（改登录名 / 店名 / 重置密码 / 头像）与 **「删除」**（二次确认 + 清理该账号数据空间 `erp_<账号id>`）按钮；
   - 管理员自身账号（管理总控）**不可修改、不可删除**（系统必保）；
   - 保留原有**经营范围 9 类勾选**管理。
3. **权限边界** —— 只有管理员（admin）能新建 / 修改 / 删除任何账号；普通店铺账号只能登录使用，无任何账户管理能力。

### 0.6 V2.4 体验优化与云同步增强（2026-09-03，已开发上线）

**体验优化：**
1. **商品档案备注列过长优化** —— 备注列固定最大宽度（200px）+ 超长自动省略号（`…`），不再撑宽表格；完整备注放 `title` 属性，**鼠标悬停可查看全文**；空备注显示「-」。手机版 / 电脑版共用同一列表，同步生效。
2. **我的页「常用入口」图标与首页「快捷入口」统一** —— 进货🚚 / 商品📦 / 库存🗄️ / 记账📒 / 报表📈 / 退换货🔁 全部与首页一致；**开单统一为「手推车」🛒 样式**；供应商👤 / 设置⚙为我的页独有，保留。
3. **商品档案「斑马纹」交替行底色** —— 偶数行浅灰底 + 行分隔线，长列表不易看错行；手机版 / 电脑版共用同一表格样式。
4. **取消右上角图标按钮** —— 手机版首页 banner 与本地/电脑版顶栏右上角的**铃铛 🔔 与头像（ERP 图标）按钮全部移除**；保留店名与全局搜索。

**云同步增强：**
5. **403 错误细分诊断** —— 同步失败时细分原因：**限流**（`rate limit` / `X-RateLimit-Remaining=0`）→ 提示等待重置；**权限不足**（缺 Contents 写权限）→ 指引生成带「Contents: Read and write」的 Token；其余 403 给出通用指引。401 / 404 / 409 / 422 同步细化。
6. **「测试连接」预检** —— 同步设置面板新增「测试连接」按钮，一键诊断 Token 有效性 + 仓库可访问性（401 / 403 / 404 / 200 各状态细分），定位同步失败原因。
7. **上传大小控制（gzip 压缩 + 超限拦截）** —— 上传前将快照 **gzip 压缩** 后再 AES-GCM 加密（信封升级 **v2**，标记 `comp:gzip`），上传体积显著减小（JSON 通常压缩 70%–90%），缓解限流；压缩不可用 / 失败时自动降级明文加密（兼容 v1 快照解密）；加密后超过 **GitHub 单文件 100MB** 上限直接拒绝、不发起请求；同步成功提示显示**压缩后实际上传大小 + 「已压缩」标记**。
8. **智能同步（内容指纹比对）** —— 同步 / 恢复前对快照业务内容（排除每次打包的动态时间戳）计算 **SHA-256 内容指纹**：
   - **同步到云端**：本地与云端指纹一致 → **跳过上传**（不产生新提交）；有新增 / 修改 → 正常压缩上传；
   - **从云端恢复**：本地与云端指纹一致 → **提示「本地与云端一致，无需恢复」**，不覆盖本地；有差异 → 才用云端覆盖。

### 0.7 V3.1 客户功能页面 + 统一版本号（2026-09-03，已开发上线）

1. **新增「客户」功能页面** —— 把「客户」从「开单时的即时新建」升级为可独立管理：
   - **入口**：电脑端**左侧列表导航「客户」** + 手机端「我的 → 常用入口 → 客户」双入口；
   - **列表**：客户名称 / 电话 / 备注 / **应收余额**（>0 客户欠我应收、<0 我方多收、=0 已结清），支持按名称/电话搜索；
   - **新增 / 编辑**：名称（必填，按「名称 + 类型=customer」去重）、电话、备注；
   - **删除**：有未结清往来余额或已有销售记录的客户**禁止删除**（避免账目断裂）；
   - **收款**：一键跳转**记账中心对该客户收款**（复用既有挂账能力，deep-link `#/account?collect=<id>`）；
   - 客户数据与供应商共用 `partners` 表（`type=customer`），余额由销售/收款单据驱动。
2. **统一版本号** —— PRD、开发计划、软件「我的 → 关于」的版本号自本版起统一为 **V3.1**（软件「关于」同步显示 V3.1）。

### 0.8 V3.2 销售开单体验与单据打印（2026-09-03，已开发上线）

**① 销售开单页布局改造（收款模块底部横排）**
- 电脑端开单页从「三列」改为「**上部两列（选货区 + 当前订单）+ 底部整行收款**」：选货区与当前订单列等高、撑满可用高度；**收款模块不再挤在右侧窄列**，放到网页底部（选货区与当前订单的下边），横跨整行，宽度与上部两列总宽度一致；
- 收款模块内部**重新排布为一行紧凑式**：收款方式（微信/现金/支付宝，各定宽 88px）＋ 实收 / 余款处理 / 欠款（三个指标同一行，不再三行堆叠）＋ 备注 / 取消·保存并出单（靠右）——显著降低模块高度；挂账客户下拉仅在欠款时另起一行显示；
- 手机端保持单列堆叠，不受影响。

**② 左侧菜单更名「销售管理」+ 开单入口统一**
- 左侧菜单「销售开单」更名「**销售管理**」，进入后默认显示**销售列表管理视图**（与进货管理同等级同布局：筛选 + 列表 + 页头操作）；
- 销售管理页**右上角「＋ 销售开单」按钮**进入开单视图；列表每行保留查看/退货/作废；
- **系统中所有开单按键统一跳转销售开单视图**：首页「开单」大按钮、我的页「常用入口 → 开单」、扫码加单，均直达开单页（`#/sale?tab=new` 直达后自动清除参数，不影响页内操作）。

**③ 销售单 / 进货单打印功能**
- 单据详情弹层新增「**打印**」按钮（销售管理 / 进货管理的单据详情），使用正常打印机打印；
- 打印尺寸为**半张 A4（A5 竖版 148×210mm）**；
- **明细多时自动分多页**：每页重复表头（`thead` 分页重复）、单行不跨页折断（`page-break-inside: avoid`）；
- 打印模板字段：店铺名、单据标题、单号、日期、客户/供应商、明细表（销售：品牌/型号/单位/价格类型/单价/数量/金额，赠送行标注且金额为 0；进货：品牌/型号/单位/成本/数量/金额）、应收/实收/欠款（或合计/已付/欠款）、备注、制单人 / 经手 / 客户签字、打印时间；
- 通过独立打印窗口输出，与既有 40×30mm 条码标签打印互不干扰。

---

### 0.9 V3.3 界面优化与适配（2026-09-03，已开发上线）

**① 记账中心头部单行重排**
- 「记账中心」标题 + 应付合计 + 应收合计 + 「流水 / 应付 / 应收」切换按钮合并为**同一行**；
- 应付/应收合计改为**紧凑横排胶囊**（不再是大卡片占多行）；切换按钮改**标准按钮样式**，当前激活项**主色高亮**（btn-primary），一眼识别当前页签；
- 桌面端单行不换行；窄屏/手机端自动换行不溢出。

**② 库存统计卡压缩 + 手机端自适应**
- 顶部 4 个统计卡（商品数 / 总库存 / 资金占用 / 低库存预警）两度压缩：内边距减半、卡片高度减半、桌面保持 4 列并收紧间距（gap 5→3px）、数字放大近两倍（13→24px）、图标放大近两倍（17→30px）；
- **手机端 2 列自适应屏幕宽度**：列宽 `minmax(0,1fr)` 强制不超出屏幕（修复长金额撑出屏幕外）；卡片改**竖排**（标签在上、数字在下），长金额完整显示不截断。

**③ 三页搜索模块重排（参考记账中心 / 库存管理风格）**
- **商品档案**：搜索框 + 状态下拉 → 同一行；
- **进货管理**：第 1 行搜索框 + 供应商下拉，第 2 行起止日期；
- **销售管理**：第 1 行搜索框 + 类型下拉，第 2 行起止日期；
- **手机端改回多行**（搜索框 + 扫码一行、筛选下拉换行第二行、日期单独一行），便于手机点选；电脑端保持单行/双行设计。

**④ 电脑端左侧导航栏可折叠 + 导航顺序重排**
- 侧栏顶部新增**折叠/展开按钮**：点击收起为**仅图标窄栏**（60px），再点恢复完整文字；折叠状态 localStorage 记忆，刷新/切换页面保持；
- 导航顺序（上至下）：**首页 → 进货管理 → 销售管理 → 档案管理 → 库存管理 → 记账中心 → 报表与利润 → 退换货 → 供应商 → 客户 → 我的 → 账户权限管理**；
- 「商品档案」在侧栏显示为「**档案管理**」，页面内标题仍为「商品档案」。

**⑤ 手机端销售开单收款模块修复 + 常用入口调整**
- 修复手机端收款模块乱套：覆盖基础 `.pay-grid` 4 列网格为**纵向堆叠**——收款方式（微信/现金/支付宝）三输入框等宽并排、实收 / 余款处理 / 欠款 一行、取消 / 保存并出单 两按钮等宽并排、备注框独占整行加长；
- 我的 → 常用入口「开单」改为「**销售**」，点击**直接进入销售管理列表页**（不再跳到开单表单）。

**⑥ 版本号统一为 V3.3**：PRD、开发计划、软件「我的 → 关于」三处一致。

---

### 0.10 V3.4 手机库存卡再缩 + 打印增强 + 蓝色配色 + 跨端库存合并（2026-09-04，已开发上线）

**① 手机版库存管理顶部四统计卡高度缩小一半**
- `min-height 34px → 17px`、内边距压缩为 `1px 6px`、标签字号 10px、数值字号 15px；
- 保持 2 列 `minmax(0,1fr)` 自适应屏幕宽度防溢出、竖排（标签上数字下）防长金额截断。

**② 单据打印增加版本选择（版本1 不带价格 / 版本2 带价格）**
- **版本 1（不带价格）**：纯清单——仅 `# 品牌 型号 单位 数量` + 件数合计，无单价 / 成本 / 金额 / 应收实收欠款；适合给出货单、验收单；
- **版本 2（带价格）**：完整单据——含价格类型（批发/零售）、单价、金额（销售）；成本、金额（进货）；应收/实收/欠款合计；
- 销售管理、进货管理**单据详情弹层**提供「**打印带价 / 打印无价**」两个按钮自行选择。

**③ 打印页操作栏（关闭可返回）**
- 打印窗口顶部新增**吸顶操作栏**：「打印」+「关闭」按钮；
- 解决**手机端打印页占满全屏只能杀软件退出**的问题——点「关闭」执行 `window.close()` 返回上一页；
- `@media print` 操作栏自动隐藏，不会印在单据上。

**④ 整体 UI 配色改为蓝色**
- 主色 `#2563EB`、强调 `#3B82F6`、banner `#60A5FA`、深色 `#1D4ED8`、浅底 `#EFF6FF`；
- 清除全部旧青绿色硬编码（含首页图表 SVG 渐变/线条、浏览器主题色 theme-color、按钮阴影/聚焦环）；
- CSS 变量名沿用 `--c-mint-*` / `--c-primary-*`（仅改色值），避免改动全站引用。

**⑤ 电脑版与手机版库存数量不一致——根因修复（跨端合并同步）**
- **根因**：原「从云端恢复」为**全量覆盖**——双端各自改动时后上传方覆盖云端，先改方恢复后其库存独立更新被覆盖丢失，导致两端库存数量不一致；
- **修复**：从云端恢复改为**记录级合并**（`merge` 模式）——
  - 同主键记录两边都有 → 取**较新**（`updatedAt || createdAt || date`）；
  - 云端独有记录 → 加入本地；本地独有记录 → 保留（**不再丢失**）；
  - 库存变动（`changeStock`）写入商品 `updatedAt`，使库存变化可跨端按时间合并；
  - `settings` 本地为底、云端字段优先，本地独有键保留；
- 效果：从云端恢复后，两端各自新增的商品/单据都在，同一商品库存收敛为较新一端的值，两端库存数量一致且不丢数据。

**⑥ 版本号统一为 V3.4**：PRD、开发计划、软件「我的 → 关于」三处一致（SW v27）。

---

### 0.11 V3.5 库存卡高度贴合 + 云同步账户/系统参数 + 价格体系 + 搜索下拉宽度（2026-09-04，已开发上线）

**① 手机版库存统计卡高度与内容贴合（宽度/字号不变）**
- 根因：flex 竖排布局中 label 被拉伸、带多余内边距，撑到 113px 致卡片 140px；
- 修复：`.stat-grid-compact` 的 label/value 锁定内容尺寸（`flex:0 0 auto; height:auto; min-height:0; padding:0`），卡片收缩至 ~38px 贴合数字，**文字与数字字号完全不变**。

**② 云同步增强：系统整体参数与账户设置同步**
- 快照新增**脱敏账户档案**（店铺名 / 头像 / 经营范围，**不含密码哈希与令牌**）；
- 「同步到云端」自动携带当前账户档案，「从云端恢复」成功后自动写回账户设置——手机端登录同一账户即可对齐店铺名 / 头像 / 经营范围；
- 确认**系统整体参数**（settings：店铺名 / 经营范围 / 库存阈值 / 利润率等）随快照同步（记录级合并保留两端）；
- **账户隔离**：各账户同步配置 key（`applianceErp.sync.config.<acctId>`）与云路径（`data/<acctId>/erp-snapshot.json`）按账户隔离；本地命名空间 `applianceErp.*`、IndexedDB `applianceErp_<acctId>`、快照自标识 `appliance-erp` 均与鞋服母版（`erp.*` / `erp_*` / `shoeErp`）完全隔离——**不与其它项目公用数据库**。

**③ 价格体系（系统整体利润率）**
- 系统设置新增两项**整体利润率**：批发利润率（默认 **20%**）、零售利润率（默认 **35%**），随云快照同步；
- **只填成本自动定价**：新建 / 编辑商品时批发价、零售价**留空**，保存后自动按 `成本 ×（1+利润率）` 生成，并**取整到元（不含小数点）**；用户填写则**保留自定义**不覆盖，只填其一则另一个自动生成；
- 商品档案新增「**💰 价格体系**」页：可修改整体利润率（保存利润率）；提供「**一键更新全部商品价格**」——按各自成本与最新利润率把**全部商品**批发价 / 零售价统一重算并取整到元（已有自定义价格也被覆盖，实现整体统一）。

**④ 电脑端搜索模块下拉宽度微调（方案B）**
- 背景：V3.3 三页搜索模块重排把下拉菜单移入 `searchBar` 与搜索框同行，电脑端下拉被压缩成内容宽度；
- 处理：`.search-bar .select` 增加 `flex:0 0 auto; min-width:130px`，下拉保持固定宽度不再缩成内容宽度；**手机端保持「下拉独占一行」的多行样式不受影响**（`flex:1 1 100%`）。

**⑤ 版本号统一为 V3.5**：PRD、开发计划、软件「我的 → 关于」三处一致（SW v28）。

---

## 1. 产品目标和使用场景

### 1.1 产品目标

为单店经营的**家电批发/零售店**提供一套**零服务器、零费用、手机+电脑通用**的进销存记账软件，解决手工记账"账分多处、库存靠脑记、赠送说不清、利润算不清"的问题。

- 所有单据（进货、销售、赠送、退货、盘点）保存时**自动联动**库存、收支流水、欠款三本账，不需要手工二次登记。
- 商品档案简单直接：**只填品牌、型号、类型、单位、成本、批发价、零售价、库存、备注**，不需要懂编码。
- 数据 100% 存在用户自己的设备上，不依赖任何服务器、账号和年费。
- 手机开单（高频、要快），电脑管理（录进货、对账、看报表），同一套数据。

### 1.2 使用场景

| 场景 | 设备 | 说明 |
| --- | --- | --- |
| 顾客到店买电器 | 手机 | **扫机身条码/二维码**（或搜索品牌型号）→ 定位商品 → 确认数量与价格（批发/零售）→ 选收款方式 → 保存出单，10–20 秒 |
| 开单给批发客户 | 手机 | 开单选「批发价」，挂账或收款，自动记入客户台账 |
| 开单给零售顾客 | 手机 | 开单选「零售价」，微信/现金/支付宝收款 |
| **采购到货** | 手机/电脑 | 录进货单（选商品 + 数量 + 进货单价），确认入库，未付款自动挂供应商欠款 |
| 送样机 / 自用 / 破损出库 | 手机 | 同一张销售单中把明细标记为"赠送"，按成本记账不计收入 |
| 顾客赊账 / 给供应商结款 | 手机 | 开单选"欠款"挂客户台账；付款登记冲减应付 |
| 关店前对账 | 手机/电脑 | 看今日概览，一键备份 |
| 月底看经营情况 | 电脑 | 月销售额、毛利、净利参考、畅销滞销排行、库存资金占用 |
| 换手机 / 换电脑 | 两端 | 导出备份文件→新设备导入恢复 |

### 1.3 运行环境约束

- 一部手机 + 一台联网电脑，**不购买任何服务器/域名/云主机**。
- 纯网页应用（HTML + CSS + 原生 JS，零依赖零构建），本地文件双击即可运行，**离线可用**。
- 手机通过浏览器"添加到主屏幕"以类原生 App 方式使用。
- 本版**不依赖蓝牙**（标签打印已移除）；**扫码依赖摄像头**：实时预览扫码需 https 或 localhost 环境，离线/无摄像头时自动降级为「拍照识别 + 手输」，不影响其他功能。

---

## 2. 整体页面框架和导航结构

单一应用，两套导航布局，按屏幕宽度自动切换（断点：768px）。

### 2.1 手机端（<768px）：底部 Tab 导航

```
底部 Tab：首页 · 开单 · 库存 · 报表 · 我的
```

- **首页**：今日概览 + 快捷入口（见第 3 节）。
- **开单**：销售开单页（含搜索加行、赠送标记、批发/零售价、收款方式），日常最高频入口。
- **库存**：库存查询（按品牌/型号/类型）、预警、盘点入口。
- **报表**：日/月销售、利润、排行（手机只读简版）。
- **我的**：二级功能入口 + 系统设置：**新品建档**、进货管理、销售记录、赠送记录、记账中心、供应商/客户、商品档案、备份与恢复、密码锁、关于。

### 2.2 电脑端（≥768px）：左侧栏导航 + 内容区

左侧栏**可折叠**：点击顶部折叠按钮收起为仅图标窄栏（60px），再点展开；折叠状态记忆（localStorage）。

```
左侧栏（上至下顺序）：🔌 我的电器店（店名可自定义）
  首页
  进货管理
  销售管理（销售开单经右上角按钮进入）
  档案管理（= 商品档案）
  库存管理
  记账中心
  报表与利润
  退换货
  供应商
  客户
  我的
  账户权限管理（仅管理员 admin 可见入口）
内容区：顶部统计条 + 功能表格/表单区 + 操作按钮
```

### 2.3 通用规则

- 所有列表页统一：搜索框 + 筛选（日期/类型/往来单位）+ 分页（每页 ≤ 300 条）+ 行操作（查看/编辑/作废）。
- 所有表单：必填项校验、金额保留两位小数、单号自动生成且不可编辑。
- 单据只能"修改（仅未结清/当日）"或"作废留痕"，**不做物理删除**。
- 搜索框统一支持文字搜索（品牌 / 型号 / 类型），回车即搜。

---

## 3. 首页（经营看板）的内容结构

首页 = 数据聚合 + 行动入口，自上而下：

1. **今日概览卡片**：今日销售额（不含赠送、退货已冲减）/ 今日销量（台数）/ 今日毛利（售价−进价快照）/ 库存预警款数
2. **主行动按钮**：「🛒 立即开单」（最大按钮）
3. **快捷入口网格**：**➕ 新品建档** · 🚚 进货 · 📋 库存 · 💰 记账 · 📊 报表
4. **待处理事项条**（黄底提醒）：库存预警 N 款 / 客户欠款超期提醒（>15 天）/ 供应商未结欠款汇总
5. **备份提醒条**：当天未备份时显示「⚠ 今日尚未备份，点此一键备份」
6. 电脑端额外显示：本月销售额 / 本月毛利 / 净利参考 / 客户欠款总额四张月度卡 + 近 7 日销售柱状图 + 畅销款 TOP5。

---

## 4. 每个模块的用途

| 模块 | 用途（一句话） |
| --- | --- |
| 商品档案 | 商品主数据：品牌、型号、类型、单位、批发价、零售价、库存、备注 |
| 进货管理 | 录进货单、管理供应商应付欠款、入库记账 |
| 销售管理 | 快速开单（搜索加行）、批发/零售价成交、收款、退货红冲 |
| 赠送管理 | 样品机/自用/破损出库的登记与成本统计 |
| 库存管理 | 商品级实时库存、预警、盘点、变动追溯 |
| 记账中心 | 收支流水（自动+手工）、供应商应付款、客户应收款台账 |
| 报表与利润 | 日/月/年销售、三层利润、畅销滞销排行、库存资金占用 |
| 数据安全 | 一键备份导出/导入恢复、打开密码、操作留痕 |

---

## 5. 每个模块第一版最必要的数据和操作

### 5.1 商品档案（product）★核心变更

**商品为单一信息，无编码、无属性、无 SKU 子层。**

| 字段 | 必填 | 说明 |
| --- | --- | --- |
| **品牌** | ✅ | 如 海尔 / 格力 / 美的 / 九阳（列表中独立一列展示） |
| **型号** | ✅ | 如 BCD-470 / KFR-35GW / YY-668（列表中独立一列展示） |
| **类型** | ✅ | 商品分类，**可从预设下拉建议选择：冰箱 / 洗衣机 / 空调 / 电视 / 厨房电器 / 生活小家电 / 数码影音 / 配件耗材 / 其他，也可直接输入自定义类型**（如 净水器 / 集成灶）（D4 已确认，V2.2） |
| **单位** | ✅ | 台 / 件 / 套 / 个 / 组 |
| **成本（进价）** | ✅ | 进货成本单价（元，两位小数），用于利润计算；进货时可覆盖，保存后同步更新为本商品最新成本（D1 已确认） |
| **批发价** | ✅ | 面向批发客户/渠道的销售价（元，两位小数） |
| **零售价** | ✅ | 面向零售顾客的销售价（元，两位小数） |
| **库存** | 只读 | 由进货/销售/退货/盘点单据实时联动，不可手改（D2 已确认） |
| **备注** | 否 | 规格参数、能效等级、功率、保修说明等自由文本 |
| **原厂条码** | 否 | 机身**条形码 / 二维码解码内容**，可录多条（逗号或换行分隔）；手机/扫码枪扫到即定位该商品（D3 已确认保留） |

**主键与唯一性：**
- 系统内部自增 ID 作为主键（**界面不显示任何编码**）。
- 新建时校验"品牌 + 型号"重复 → 提示"该品牌型号已存在"，不重复建档。
- 型号允许相同（不同品牌下），"品牌 + 型号"整体作为对外识别标识。

**操作：**
- 新增、编辑、停售（不删数据）、按品牌/型号/类型/关键字搜索、列表分页、**CSV 批量导入**存量商品（字段：品牌/型号/类型/单位/成本/批发价/零售价/备注，可选期初库存列）。
- **期初库存**：建档时可选填"期初库存"，保存后生成一条期初盘点调整记录并计入库存。
- **列表显示规则（V2.2）**：列表**显示本账号全部商品**（含自定义类型、经营范围外类型），新建保存后**即时显示**；保存经营范围外的类型时自动并入账号经营范围（下拉建议与列表保持一致）。

### 5.2 进货管理（purchase）

| 字段 | 说明 |
| --- | --- |
| 单号 / 日期 | 单号自动（如 P20260902-001） |
| 供应商 | 必选（可即时新建） |
| 明细[] | 商品 + 数量 + **进货单价（成本）** + 小计 |
| 总金额 / 已付金额 | 自动合计 / 手工填 |
| 欠款金额 | 总金额−已付，自动计算 |
| 备注 | 选填 |

**操作**：新建进货单（搜索选商品 + 填数量 + 进货单价**自动带出档案成本，可改**）、修改（仅未结清欠款的单）、作废留痕、按日期/供应商筛选查询；确认保存时自动：库存+N、生成进货支出流水（按已付额）、供应商应付+N，并把该商品**档案成本同步更新为本次进货单价**（历史单据的成本仍用快照，不受影响）。

> 说明（D1 已确认）：商品档案维护「成本」列；进货单带出可改，保存后同步档案成本。利润成本取**单据进价快照**，历史报表不随档案改动而变化。

### 5.3 销售管理（sale）

> V3.2 更新：左侧菜单更名「**销售管理**」，进入默认销售列表管理视图；右上角「＋ 销售开单」进入开单页；开单页收款模块放底部整行、一行紧凑排布；单据可打印。
> V3.4 更新：单据打印支持「**打印带价 / 打印无价**」双版本；打印页带吸顶「打印 / 关闭」操作栏（手机端可关闭返回）。

| 字段 | 说明 |
| --- | --- |
| 单号 / 日期 | 单号自动（如 S20260902-001） |
| 客户 | 选填（挂账欠款时必填） |
| 明细[] | 商品 + 数量 + **售价（按批发价或零售价，可改）** + 进价快照 + 类型（销售/赠送） |
| 折扣/抹零 | 整单级 |
| 收款方式 | 微信 / 现金 / 支付宝 / 欠款（可组合） |
| 已收金额 / 欠款金额 | 欠款自动 = 应收−已收 |
| 赠送原因 | 明细为赠送时必选：样品机/自用/破损 |

**页面结构（电脑端）：**
- **销售管理（列表）**：页头标题「销售管理」+ 右上角「＋ 销售开单」；筛选（日期 / 类型 / 关键词搜索单号·客户·品牌·型号）+ 单据列表（单号/日期/客户/数量/应收/实收/欠款/状态/操作），每行「查看 / 打印 / 退货 / 作废」；
- **销售开单（表单）**：上部两列（**选货区** + **当前订单 / 应收合计 / 整单折扣**）+ **底部收款整行**（收款方式三输入 / 实收·余款处理·欠款一行 / 备注 / 取消·保存并出单；挂账客户下拉欠款时另起一行）；
- 单据详情弹层可**打印**（半张 A4，多页分页，见 0.8）。

**开单交互：**
- 搜索框输入品牌/型号 → 下拉选商品 → 该行自动带出单位、批发价、零售价。
- 每行一个**价格切换**：默认零售价，点一下切批发价（或直接改价）；显示成交单价。
- **扫码加行**：搜索框旁 📷 扫机身原厂条码/二维码 → 定位商品自动加入明细（未建档提示先建档）。
- 支持连续加多行、单行改价、整单折扣。
- 保存时自动：库存−N、收入流水/赠送成本流水、客户应收更新。
- **销售退货**（红冲）：原单退货 → 库存+N、收入冲减；按日期/类型查询。
- **打印**：单据详情「打印」按钮 → 正常打印机输出半张 A4 单据，明细多页自动分页。

### 5.4 赠送管理

**数据**：依附于销售单明细（类型=赠送 + 赠送原因），不单独建表；另存赠送记录汇总视图（日期、商品、数量、成本、原因）。

**操作**：开单时一键把明细切换为"赠送"；赠送记录列表按月份/原因筛选；月末赠送成本合计（报表用）。

### 5.5 库存管理（inventory）

> V3.3 更新：顶部 4 统计卡紧凑化（内边距/高度减半、桌面 4 列、手机 2 列自适应防溢出）；列表斑马纹区分差行。
> V3.4 更新：**手机版顶部 4 统计卡高度再缩小一半**（min-height 17px，压缩内边距与字号），保持 2 列自适应。

| 字段 | 说明 |
| --- | --- |
| 库存键 | 商品 ID（品牌+型号） |
| 当前数量 | 由单据流水实时累计，不直接手改（期初库存走盘点调整） |
| 预警阈值 | 按商品设置（默认 3 台，见待确认项 D5） |
| 变动流水[] | 日期、单据号、方向（+/−）、数量、余额（每次变动留痕） |

**操作**：按品牌/型号/类型/预警状态查询；**扫码查库存**（扫原厂条码/二维码 → 定位商品 → 显示库存）；预警列表（低于阈值）；盘点（录入实盘数 → 差异生成"盘点调整单"留痕）；点击某商品查看全部出入明细。

### 5.6 记账中心（ledger / partner）

**数据**

- 流水：日期、类型（销售收入/进货支出/赠送成本/房租/水电/人工/物流/其他）、金额、关联单据号、备注；单据自动生成的流水不可手改。
- 往来单位（供应商/客户同表）：名称、电话、类型、应收/应付余额、欠款明细、还款记录。

**操作**：手工记一笔收支（费用类）；流水查询（日期/类型）；供应商应付清单与付款登记；客户应收清单与收款登记；还款后余额自动冲减。

### 5.7 报表与利润（口径不变）

- 销售收入 = 销售单实收 + 挂账欠款 − 退货
- 销售成本 = Σ 售出数量 × 进价快照（**进价快照 = 该销售明细对应进货/最近进价**，沿用原机制）
- 毛利 = 销售收入 − 销售成本；毛利率 = 毛利 ÷ 收入
- 赠送成本 = Σ 赠品数量 × 进价快照（单列，不计入销售成本）
- 净利参考 = 毛利 − 赠送成本 − 费用支出（房租/水电/人工/物流/其他）
- 库存资金占用 = 当前库存 × 最新进价

**操作**：日/月/年报表查询；畅销/滞销 TOP5（按毛利/按销量）；导出 CSV（Excel 可开）。

### 5.8 数据安全

**数据**：操作日志（时间、动作、单据号）；备份文件结构见第 7 节。

**操作**：一键导出备份、导入恢复（覆盖前二次确认）、设置/修改打开密码、查看操作日志。

### 5.9 品牌形象与多店铺账号（V2.2 新增）

**品牌形象（页面 icon / logo / 网页名称）：**
- 默认应用图标为**电器版**（冰箱+空调+洗衣机+ERP，192/512/1024 三档，用于 favicon / PWA / 锁屏）。
- 「我的 → 店铺资料」可上传**店铺头像（logo）**：即时更新应用内 logo（首页 banner / 顶栏 / 侧栏）与**浏览器标签页 favicon**。
- 「我的 → 店铺资料」可修改**店铺名称**：即时联动**网页标题**（`店名 · 页面名`）、顶栏与登录页店名。
- 未自定义时统一回退默认（电器图标 / 「我的电器店」）。

**多店铺账号（登录名 / 密码 / 头像 / 数据隔离）：**
- 登录页展示本机全部店铺账号（头像 + 店名 + 经营范围）；预置 3 店：大家电店 / 小家电店 / 厨电店（初始密码 000000）。
- 「＋ 新建店铺账号」可自行设定：**登录账号（2–20 位字母/数字/下划线）、店铺名称、密码（≥4 位）、店铺头像（选填，上传图片）**；最多 10 个账号。
- 每个店铺**均可使用全部功能**（建档 / 进货 / 销售 / 库存 / 记账 / 报表 / 备份等）；自建账号经营范围默认**全部分类开放**。
- **数据独立隔离**：每账号独立数据库 **`erp_<账号id>`**，登录哪店只见哪店数据，**各店铺数据互不共有**；账号列表（登录名/店名/头像）存本机 localStorage。

**管理员账号与账户权限管理（V2.3）：**
- 预置**管理员账号 `admin`**（店铺名「管理总控」，初始密码 `admina1b22c333`，登录后可在「我的」修改密码/店名/头像）；拥有独立数据空间 `erp_admin`。
- 管理员登录后，「我的」页出现「**权限管理**」入口；普通账号无此入口。
- 权限管理页：列出全部账号，管理员可为每个账号**勾选经营范围**（9 类分类或「全部分类」），保存即时写入该账号 `scopeCategories`。
- 管理员**不能删除自己**；自身经营范围默认全部分类。

**账户管理收归管理员（V2.4）：**
- 登录页**取消账号卡片的编辑 / 删除按钮与「＋ 新建店铺账号」按钮**，登录页只做账号选择 + 密码登录，并提示「账户的新建 / 修改 / 删除由管理员（管理总控）统一管理」。
- 账户管理统一收归管理员「**账户权限管理**」页：管理员可**新建**（登录名/店名/初始密码/头像）、**修改**（登录名/店名/重置密码/头像）、**删除**（二次确认 + 清理数据空间）任意店铺账号，并管理其经营范围。
- 管理员自身账号**不可修改、不可删除**（系统必保）。

**客户管理（V3.1）：**
- 「客户」为独立功能页：电脑端左侧导航 + 手机端「我的 → 常用入口」双入口。
- 列表展示客户名称 / 电话 / 备注 / **应收余额**（>0 应收、<0 我方多收、=0 已结清），支持按名称/电话搜索。
- 新增 / 编辑客户：名称必填（按「名称 + type=customer」去重）、电话、备注。
- 删除保护：有未结清余额或已有销售记录的客户**禁止删除**。
- 收款：一键跳转记账中心对该客户收款（`#/account?collect=<id>`），复用挂账与收款能力。

---

## 6. 首页和每个模块之间的关系

首页不存任何数据，全部是**实时聚合 + 入口分发**；规则：**单据是唯一事实来源**。首页与报表的每个数字都必须能通过"点击下钻"追溯到对应单据；任何模块都不允许出现与单据流水矛盾的静态数字。

数据链路：
```
商品（品牌+型号，单一库存）
  └──被引用──→ 进货单 / 销售单 ──保存时──→ 库存增减 + 流水记账 + 欠款更新
                                        └──────→ 报表汇总 + 首页聚合
```

---

## 7. 本地数据文件、备份和恢复要求

- 主体数据存 **IndexedDB**（库名 `erp_<账号id>`），设置项（密码哈希、界面偏好）存 localStorage；统一数据版本号 `schemaVersion`。
- 一键导出为**单个 JSON 文件**，命名：`电器店备份_YYYYMMDD_HHmm.json`；内容含全部表数据 + `schemaVersion` + 导出时间 + 单据计数摘要。
- 导入校验文件结构合法、版本兼容；恢复为**整体覆盖**，执行前二次确认；完成后自动核对单据计数摘要并留痕。
- 备份纪律：当天未导出→首页提醒条；每周建议异地备份一份；清缓存/换设备前必须先导出。

---

## 8. 第一版必须完成的功能

1. 商品档案：8 字段建档（品牌/型号/类型/单位/批发价/零售价/库存/备注）、重复校验、停售、搜索、CSV 批量导入、期初库存；**类型可选择可自定义输入（V2.2）**；**列表显示本账号全部商品，新建后即时显示（V2.2）**。
2. 进货管理：进货单录入/修改/作废、数量+进货单价、供应商欠款自动挂账。
3. 库存管理：商品级实时库存、变动流水、低库存预警、盘点调整。
4. 销售管理：手机快速开单（搜索加行、批发/零售价切换、改价、整单折扣、组合收款、赠送标记）、退货红冲。
5. 赠送管理：明细级赠送标记 + 原因、赠送记录视图、赠送成本统计。
6. 记账中心：单据自动流水 + 手工费用记账、供应商付款登记、客户收款登记、余额自动冲减。
7. 报表：日/月销售、毛利/净利参考、赠送成本、畅销滞销 TOP5、库存资金占用、CSV 导出。
8. 数据安全：一键备份导出、导入恢复（二次确认）、打开密码、操作日志。
9. 响应式：手机（底部 Tab）与电脑（左侧栏）双布局，同一数据。
10. 离线可用：本地双击打开即可完成全部核心操作。
11. **扫码能力（D3 保留）**：商品档案录入**原厂条码**（条形码/二维码解码内容，可多条）；手机摄像头 / 扫码枪扫描定位商品，用于**扫码查库存**与**开单扫码加行**；识别**三级降级**（① 实时预览 → ② 拍照识别 → ③ 手输），同时支持**条形码（EAN-13 / CODE128 等）与二维码（QR Code）**；扫到未建档码提示先建档。
12. **品牌形象自定义（V2.2）**：默认电器版图标；「我的 → 店铺资料」上传头像（logo）即时联动应用内 logo 与浏览器 favicon；修改店名即时联动网页标题与顶栏。
13. **多店铺账号与数据隔离（V2.2）**：登录页创建账号可设定登录名/密码/**头像**；每账号独立库 `erp_<账号id>`，数据互不共有；每店可用全部功能。
14. **管理员账号与账户权限管理（V2.3）**：预置管理员账号 `admin`（初始密码 `admina1b22c333`）；管理员登录后「我的」页有「权限管理」入口，可管理全部账号的经营范围（9 类勾选/全部分类），普通账号无此入口。
15. **账户管理收归管理员（V2.4）**：登录页取消账号编辑/删除/新建按钮，只做账号选择登录；账户的新建/修改/删除统一由管理员在「账户权限管理」页操作，admin 自身不可改删。
16. **商品档案备注列优化（V2.4）**：备注列超长省略号 + 悬停查看完整内容，不再撑宽表格。
17. **我的页常用入口图标与首页统一（V2.4）**：进货🚚/商品📦/库存🗄️/记账📒/报表📈/退换货🔁 与首页一致；开单统一为手推车🛒。
18. **商品档案斑马纹交替行底色（V2.4）**：偶数行浅灰底 + 行分隔线，长列表防看错行（手机版/电脑版）。
19. **取消右上角图标按钮（V2.4）**：手机版 banner 与本地/电脑版顶栏的铃铛 🔔 与头像按钮移除。
20. **云同步 403 诊断 + 测试连接（V2.4）**：同步失败细分限流/权限不足并给出修复指引；同步设置面板「测试连接」一键预检 Token/仓库。
21. **上传大小控制（V2.4）**：上传前 gzip 压缩（信封 v2）显著减小体积；超过 100MB 上限拦截；提示压缩后上传大小。
22. **智能同步（V2.4）**：内容指纹比对（SHA-256 排除动态时间戳），本地与云端一致时跳过上传/恢复，有变更才同步。
23. **客户管理（V3.1）**：独立「客户」功能页（左侧导航 + 我的页常用入口双入口）；客户列表（应收余额）/新增/编辑/搜索/删除保护（有余额或销售记录禁止删除）/一键跳转记账中心收款。

---

## 9. 第一版暂时不做的功能

1. ❌ **自动编码 / 条码标签打印**（无编码，标签打印随编码移除）。
2. ❌ 多员工账号、权限分级、操作审批流。
3. ❌ 会员体系、积分、储值、优惠券。
4. ❌ 多仓库管理。
5. ❌ 商品图片上传与图片管理。
6. ❌ 供应商对账单自动生成/打印排版、短信微信催款提醒。
7. ❌ 采购建议、智能补货等算法功能。
8. ❌ 大件**序列号 / 一物一码**登记（空调、冰箱等）。
9. ❌ **保修 / 三包登记**与到期提醒。
10. ❌ **安装 / 配送 / 师傅派单**跟踪。
11. ❌ **以旧换新 / 补贴抵扣**计价。

---

## 10. 验收标准（可实测验证，验收时逐条打勾）

### 10.1 商品建档

- [ ] 新建商品，只填 品牌=海尔 / 型号=BCD-470 / 类型=冰箱 / 单位=台 / 成本=2000 / 批发价=2200 / 零售价=2699 → 保存成功，列表出现该商品，**品牌、型号分两列独立显示**，**界面不出现任何编码**。
- [ ] 再建 品牌=海尔 / 型号=BCD-470 → 提示"该品牌型号已存在"，不重复建档。
- [ ] 品牌=格力 / 型号=BCD-470（同型号不同品牌）→ 允许建档，两条互不干扰。
- [ ] 建档时填期初库存 5 台 → 保存后库存=5，生成一条期初盘点调整记录。
- [ ] CSV 导入 10 行商品（含成本列）→ 全部入库，重复品牌型号被跳过并提示。
- [ ] **类型自定义（V2.2）**：新建商品类型输入预设中没有的"净水器" → 保存成功，列表显示该类型；再次新建商品时下拉建议含"净水器"。
- [ ] **新建后即时显示（V2.2）**：新建商品（含自定义/经营范围外类型）保存后，列表**立即显示**该商品；商品类型自动并入经营范围，列表不受经营范围过滤隐藏。

### 10.2 核心业务闭环

- [ ] 新商品 + 进货 3 台（进货单价 2000）→ 库存 3；开销售单 2 台（零售价 2699）→ 库存 1；流水出现收入记录。
- [ ] 同一销售单 1 行销售 + 1 行赠送：应收只含销售行；保存后库存各 −1；赠送行生成"赠送成本"流水，不生成收入流水。
- [ ] 销售退货：对已保存单据执行退货 → 库存 +N、当期销售收入按退货金额冲减、生成退货红冲流水。
- [ ] 进货欠款：进货 10000 元、已付 6000 → 该供应商应付 4000；登记付款 4000 → 余额归零。
- [ ] 客户挂账：开单 3000 元选"欠款" → 客户应收 +3000；收款登记 3000 → 余额归零。
- [ ] 欠款单据未结清前可修改，作废后所有联动（库存/流水/欠款）同步回滚，且留操作日志。

### 10.3 批发/零售双价

- [ ] 开单加行默认显示零售价 2699；点价格切换 → 变为批发价 2200；直接改价 → 按改后价成交。
- [ ] 同一商品同日分别按批发价和零售价出单 → 收入分别按各自成交价计入，报表一致。

### 10.4 利润口径

- [ ] 构造已知数据（进价 2000、零售 2699 卖 10 台、赠 1 台、费用 500）：报表显示销售收入 26990、销售成本 20000、毛利 6990、赠送成本 2000、净利参考 4490，全部与手算一致。
- [ ] 改动商品档案当前进价后，历史报表数字不变（进价快照生效）。

### 10.5 库存与预警

- [ ] 阈值设 3，库存降到 2 → 出现在预警列表并计入首页预警数。
- [ ] 盘点录入实盘数与账面不符 → 生成盘点调整单，库存更新且差异留痕。

### 10.6 扫码（D3 已确认保留）

- [ ] 商品档案录入原厂条码（如 `6920154321001`）→ 手机扫码该条形码 → 1 秒内定位该商品并显示商品卡（品牌型号、单位、单价、库存）。
- [ ] 商品档案录入机身**二维码**内容 → 手机扫码该二维码 → 定位该商品（与条形码结果一致）。
- [ ] 一个商品录入多条原厂码（机身条码 + 二维码）→ 扫任一码均定位同一商品。
- [ ] 开单页扫码 → 定位商品自动加入销售单，价格默认零售价、可切换批发价。
- [ ] 扫到未建档的码 → 提示"未找到对应商品，请先建档"。
- [ ] 断网 / 无摄像头环境：拍照识别或手输条码 → 定位结果与实时扫码一致。
- [ ] 扫码定位准确率：连续扫 20 个不同商品的码，成功定位 ≥19 次。

### 10.7 备份与恢复 / 双端与性能 / 数据安全

- [ ] 导出 JSON → 清空浏览器数据 → 导入恢复 → 商品/单据/库存/欠款/流水与导出前完全一致。
- [ ] 导入损坏文件或非备份 JSON → 明确报错，不破坏现有数据。
- [ ] 375px 手机：底部 Tab 正常，20 秒内完成一次开单（搜索、选商品、确认价、收款、保存）。
- [ ] 1280px 电脑：左侧栏 + 看板正常，可完成进货单录入与报表导出。
- [ ] 预置 5000 条单据：列表分页加载 <1 秒，开单搜索无肉眼可见卡顿。
- [ ] 断网状态（本地文件打开）：全部功能可用。
- [ ] 设置打开密码后：重新打开需输密码，错误密码无法进入。

### 10.8 品牌形象与多店铺（V2.2）

- [ ] 默认图标为电器版（非鞋服 T 恤/运动鞋样式）；「我的 → 店铺资料」上传头像后，首页/顶栏 logo 与浏览器标签页 favicon 同步变为自定义头像。
- [ ] 「我的 → 店铺资料」修改店名为"西安电器总店" → 网页标题立即变为"西安电器总店 · 页面名"。
- [ ] 登录页「＋ 新建店铺账号」：设定登录账号/店名/密码/上传头像 → 创建成功，账号出现在登录列表并带头像。
- [ ] 新店铺登录后可使用全部功能（成功建档/进货/开单）；新店铺默认全部分类开放。
- [ ] 数据隔离：店铺 A 建档"海尔 BCD-200"，店铺 B 内**看不到** A 的商品；切回 A 仍见 A 数据，双向互不可见。

### 10.9 管理员账号与账户权限管理（V2.3）

- [ ] 登录页出现管理员账号 `admin`（店铺名「管理总控」）；用初始密码 `admina1b22c333` 登录成功。
- [ ] 管理员登录后，「我的」页出现「权限管理」入口；**普通店铺账号登录后不显示**该入口。
- [ ] 权限管理页列出全部账号（含预置 3 店 + 自建账号 + 管理员）。
- [ ] 将「厨电店」经营范围改为仅「厨房电器 / 生活小家电」并保存 → 该账号登录后建档的类型下拉建议、经营范围仅含所选分类。
- [ ] 将某自建账号设为「全部分类」→ 该账号经营范围恢复全部分类。
- [ ] 管理员修改某账号经营范围后，该账号**下次登录**按新经营范围生效（数据不丢失，已建档商品仍显示）。
- [ ] 管理员自身经营范围默认全部分类，且**无法删除自己**。

### 10.10 账户管理收归管理员（V2.4）

- [ ] 登录页账号卡片**无编辑 / 删除按钮、无「＋ 新建店铺账号」按钮**，只能点账号 + 输密码登录；未选中账号时提示由管理员统一管理。
- [ ] 管理员登录 → 「我的 → 账户权限管理」：可**新建**账号（登录名/店名/初始密码/头像）→ 创建成功出现在登录列表。
- [ ] 管理员可**修改**某店铺账号（改名/改登录名/重置密码）→ 保存后生效；可**删除**该账号（二次确认）→ 账号与数据空间清理。
- [ ] 管理员自身账号（管理总控）**无修改 / 删除按钮**，不可被改删。
- [ ] 普通店铺账号登录后**无任何账户管理入口**（看不到新建/修改/删除）。

### 10.11 体验优化与云同步增强（V2.4）

- [ ] **备注列**：录入超长备注 → 列表该列以省略号截断显示、不撑宽表格；鼠标悬停显示完整备注；无备注显示「-」。
- [ ] **常用入口图标**：我的页常用入口图标与首页快捷入口一致（进货🚚/商品📦/库存🗄️/记账📒/报表📈/退换货🔁），开单为手推车🛒。
- [ ] **斑马纹**：商品档案列表奇数/偶数行底色交替，行间有分隔线；手机版与电脑版一致。
- [ ] **右上角图标**：手机版与本地/电脑版页面右上角**无铃铛与头像图标按钮**；店名与搜索保留。
- [ ] **403 诊断**：同步失败返回 403 时，提示区分「限流（等待重置）」与「权限不足（Token 缺 Contents 写权限）」并给出修复指引。
- [ ] **测试连接**：同步设置面板点「测试连接」→ 正常返回「连接成功（仓库可访问）」或明确失败原因（Token 无效/仓库不存在/权限不足）。
- [ ] **上传大小控制**：同步到云端成功提示显示**压缩后上传大小 + 「已压缩」**；数据超上限（100MB）时明确拒绝并提示清理。
- [ ] **智能同步**：连续两次内容未变更时点「同步到云端」→ 第二次提示「本地与云端一致，无需重新上传」且云端无新提交；内容变更后再同步 → 正常上传。
- [ ] **智能恢复**：本地与云端一致时点「从云端恢复」→ 提示「本地与云端一致，无需恢复」，本地数据不被覆盖；本地有差异时 → 用云端覆盖本地。

### 10.12 客户管理（V3.1）

- [ ] 电脑端左侧导航出现「客户」；手机端「我的 → 常用入口」出现「客户」图标（🤝），均可进入客户页。
- [ ] 客户页点「＋ 新增客户」：填名称/电话/备注 → 保存成功，列表显示该客户（type=customer），且**不会出现在供应商列表**。
- [ ] 新增空名称客户 → 提示「请填写客户名称」，不创建。
- [ ] 新增同名客户（已有同名客户）→ 提示「已存在同名客户」，不重复创建；同名但为供应商的允许创建为客户。
- [ ] 编辑客户：改名称/电话/备注 → 保存后列表更新。
- [ ] 删除保护：客户有应收余额 → 提示「请先结清再删除」；客户已有销售记录 → 提示「已有销售记录，无法删除」；两者皆无 → 删除成功。
- [ ] 客户页点「收款」→ 跳转记账中心并预选该客户收款（应收清单），可完成收款登记。
- [ ] 软件「我的 → 关于」显示版本号 **V3.1**（与 PRD / 开发计划统一）。

### 10.13 销售开单体验与单据打印（V3.2）

- [ ] 电脑端开单页：**收款模块在网页底部整行横排**（非右侧窄列），宽度与上部「选货区 + 当前订单」两列总宽度一致；收款方式三输入 / 实收·余款·欠款 / 备注·按钮**同一行紧凑排布**，模块不再过高。
- [ ] 开单页挂账（记欠款）时，「客户（挂账对象）」下拉 + 即时新建在收款区**另起一行**显示，不挤压收款一行。
- [ ] 左侧菜单显示「**销售管理**」（非"销售开单"）；点击进入默认显示**销售列表管理视图**，与进货管理同等级同布局。
- [ ] 销售管理页右上角「＋ 销售开单」→ 进入开单页；**首页开单大按钮 / 我的页「开单」入口 / 扫码加单**均直达开单页（新开一张单），不落在列表页。
- [ ] 销售管理页打开任一单据「查看」弹层 → 有「**打印**」按钮；进货管理页任一单据「查看」弹层同样有「打印」按钮。
- [ ] 点「打印」弹出独立打印窗口，页面尺寸为**半张 A4（A5 竖版 148×210mm）**；打印内容含店铺名、单号、日期、客户/供应商、明细表、应收/实收/欠款（进货为合计/已付/欠款）、备注、制单/经手/签字、打印时间。
- [ ] 明细较多时，打印**自动分多页**，每页重复表头；单行不被拆分到两页。
- [ ] 软件「我的 → 关于」显示版本号 **V3.2**（与 PRD / 开发计划统一）。

### 10.14 界面优化与适配（V3.3）

- [ ] 记账中心头部：标题 + 应付合计 + 应收合计 + 「流水 / 应付 / 应收」按钮**同一行**；两个合计为紧凑横排胶囊；当前切换按钮**主色高亮**（btn-primary）；桌面单行、窄屏自动换行。
- [ ] 库存管理顶部 4 统计卡：内边距 / 高度 / 间距减半，桌面 4 列；数字（24px）与图标（30px）明显放大。
- [ ] 手机端库存统计卡：2 列自适应屏幕宽度（不溢出屏幕外）、卡片竖排（标签上数字下）、长金额完整显示。
- [ ] 商品档案搜索模块：搜索框 + 状态下拉**同一行**。
- [ ] 进货管理 / 销售管理搜索模块：**第 1 行搜索框 + 筛选下拉、第 2 行起止日期**。
- [ ] 手机端三页搜索模块：搜索框 + 扫码一行、筛选下拉换行第二行、日期单独一行（多行便于点选）。
- [ ] 电脑端左侧导航：顶部折叠按钮，点击收起为**仅图标窄栏（60px）**、再点展开；折叠状态刷新后保持。
- [ ] 电脑端左侧导航顺序（上至下）：首页 → 进货管理 → 销售管理 → **档案管理** → 库存管理 → 记账中心 → 报表与利润 → 退换货 → 供应商 → 客户 → 我的 → 账户权限管理。
- [ ] 手机端销售开单收款模块：收款方式三输入框等宽并排、实收/余款/欠款一行、取消/保存按钮等宽并排、备注独占整行——不出现乱套错位。
- [ ] 我的 → 常用入口第一格显示「**销售**」，点击**直接进入销售管理列表页**（非开单表单）。
- [ ] 软件「我的 → 关于」显示版本号 **V3.3**（与 PRD / 开发计划统一）。

---

## 11. 待你拍板的决策清单（确认时逐条回复即可）

| 编号 | 决策点 | 结论 / 推荐方案 | 状态 |
| --- | --- | --- | --- |
| D1 | **成本（进价）口径** | 商品档案设「成本」列（默认进价）；进货单带出可改，保存后同步更新档案成本；利润取单据进价快照 | ✅ 已确认（增列） |
| D2 | **库存来源** | 由单据实时联动 + 建档可填「期初库存」（走盘点调整记录） | ✅ 已确认 |
| D3 | **扫码去留** | **保留**：商品档案录「原厂条码/二维码内容」，手机/扫码枪扫机身码定位商品，开单可扫码加行 | ✅ 已确认 |
| D4 | **类型字典** | 冰箱/洗衣机/空调/电视/厨房电器/生活小家电/数码影音/配件耗材/其他；**预设下拉可选择 + 可直接输入自定义类型**（自定义类型自动并入经营范围） | ✅ 已确认（V2.2 落地） |
| D5 | **预警阈值** | 默认 3 | ✅ 已确认（按推荐） |
| D6 | **管理员账号** | 预置 `admin`（店铺名「管理总控」），**初始密码 `admina1b22c333`**；登录后「我的 → 权限管理」可管理全部账号的经营范围（方案 A） | ✅ 已确认（V2.3） |

> 说明：D1–D3 已按你的确认写入本文档；D4 类型字典于 V2.2 落地为「预设选择 + 自定义输入」，D5 按推荐默认 3，D6 管理员账号按方案 A（`admin` / `admina1b22c333`）均已确认。另确认项：**品牌/型号列表分两列展示、销售可切换批发/零售价**均已写入。

---

*本 PRD 为开发唯一依据；确认后按它整理开发计划（分阶段、每阶段含测试与验收），经你确认后再进入开发。需求变更请先更新本文档。*
